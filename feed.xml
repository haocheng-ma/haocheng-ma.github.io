<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://mahaocheng.me/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mahaocheng.me/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-14T13:58:44+00:00</updated><id>https://mahaocheng.me/feed.xml</id><title type="html">blank</title><subtitle>Haocheng&apos;s academic webpage. </subtitle><entry><title type="html">Pre-Silicon Side-Channel Evaluation: From Physics to Diffusion</title><link href="https://mahaocheng.me/blog/2026/pre-silicon-side-channel-evaluation/" rel="alternate" type="text/html" title="Pre-Silicon Side-Channel Evaluation: From Physics to Diffusion"/><published>2026-05-10T00:00:00+00:00</published><updated>2026-05-10T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2026/pre-silicon-side-channel-evaluation</id><content type="html" xml:base="https://mahaocheng.me/blog/2026/pre-silicon-side-channel-evaluation/"><![CDATA[<p><em>EMSim grew out of my Ph.D. work at Tianjin University. Ya Gao, whose research I supervise, led the later AI-accelerated work on EMSim+ and AIPS; I am a co-author. Seen together, the four projects follow a clear progression, with each one tackling a limitation of the previous approach.</em></p> <h2 id="why-pre-silicon-security-evaluation">Why pre-silicon security evaluation?</h2> <p>When a cryptographic chip operates, logic-cell transitions produce power consumption and electromagnetic (EM) emanations that depend on the data being processed. Side-channel analysis (SCA) uses this physical information to recover secrets such as cryptographic keys without attacking the algorithm itself.</p> <p>Security evaluation methods fall into two broad classes. Attack-based methods reproduce an attacker’s attempt to recover sensitive information. Correlation electromagnetic analysis (CEMA), for example, derives hypothetical intermediate values from known plaintexts and key guesses, maps them to hypothetical EM emanations with a leakage model, and computes their Pearson correlation with measured EM traces. Measurement to Disclosure (MTD), the minimum number of traces required to recover the key, measures attack difficulty. Leakage-based methods instead test whether two data populations differ statistically; Test Vector Leakage Assessment (TVLA) is the best-known example. Because TVLA can report leakage that a practical attack does not exploit, the EMSim results discussed here rely primarily on attack-based CEMA.</p> <p>Conventional security evaluation takes place after fabrication. If first silicon leaks, changes to the register-transfer-level (RTL) design or physical layout require another manufacturing cycle. Pre-silicon security evaluation moves this feedback into the design stage: simulate EM or power traces from design data, apply the same analysis methods used after fabrication, and revise the design before tape-out. The simulated data must match measurements in the relevant time and spatial domains while scaling to the trace counts required at higher security levels.</p> <h2 id="the-simulation-challenge">The simulation challenge</h2> <p>Take an AES core through a typical sequence of revisions: layout and initial evaluation, the addition of masking or a modified power grid, and eventually migration to another process node. Each change requires a new assessment of information leakage. The simulator must preserve the temporal features of EM or power traces, retain the spatial distribution needed for localized EM analysis, and generate enough traces for the selected security level. Four bottlenecks dominate this process.</p> <ol> <li> <p><strong>Layout-level EM simulation is computationally expensive.</strong> It derives logic cells, metal interconnect, parasitics, and transient currents from the physical layout, then calculates the magnetic field at observation points above the die. This physical information improves simulation accuracy but raises the cost of generating large EM datasets.</p> </li> <li> <p><strong>A simplified physical simulation still runs once per trace.</strong> EMSim is 32× faster than ConvEM, but every additional plaintext or mask value still requires current analysis and EM computation.</p> </li> <li> <p><strong>A learned model incurs a per-design training cost.</strong> A model trained for the baseline AES layout needs new input-output sample pairs when the layout, countermeasure, or fabrication process changes. Training from scratch at every iteration reduces the benefit of rapid large-scale data generation.</p> </li> <li> <p><strong>Power side-channel evaluation needs transient power traces.</strong> Learned models for thermal or voltage-drop analysis usually predict average power, whereas SCA depends on nanosecond-scale, data-dependent transients. Synopsys PrimeTime PX (PTPX) can generate transient power traces, but its cost grows with circuit size and trace length.</p> </li> </ol> <h2 id="emsim--layout-level-em-simulation">EMSim — layout-level EM simulation</h2> <p>EMSim starts from the physical layout and consists of three stages: data preparation, current analysis, and EM computation. Data preparation builds a layout database through the RTL-to-GDS design flow. Current analysis derives the transient current distribution across the physical layout. EM computation then converts those currents into magnetic-field data at arbitrary observation points. Unlike a behavioral model based only on RTL or a gate-level netlist, the physical layout includes the fabrication process, cell locations, metal geometry, and parasitic parameters needed to approximate the behavior of fabricated silicon.</p> <p>The simplifications in EMSim follow from two physical effects: current aggregation and metal shielding. Logic-cell transitions are the source of EM information, while the metal interconnect is the radiation carrier. Cell currents aggregate in the on-chip power network, producing relatively large transient currents in the top-level power grid. EM emanations from lower metal layers are attenuated by the upper layers. EMSim therefore focuses on the transient currents and magnetic field of the top-level power grid, retaining the dominant contributors while reducing computation.</p> <p>It uses three techniques.</p> <p><strong>Device-model approximation</strong> represents logic-cell transitions as cell-level current-source excitations. Dynamic gate-level simulation first records cell transitions. Cell-level power analysis then obtains transient power from the lookup tables in the Liberty library and divides it by the supply voltage to derive transient current. This replaces the nonlinear equations of deep-submicron device models with piecewise-linear current lookup tables.</p> <p><strong>Parasitic-network reduction</strong> uses those predetermined cell-level current sources to remove signal interconnects from the parasitic-network model while retaining the on-chip power network needed to calculate transient currents in the top-level power grid. The smaller system reduces both nodal-analysis cost and convergence problems in SPICE simulation.</p> <p><strong>GPU parallelization</strong> accelerates EM computation. EMSim divides each metal wire into equal-area rectangular subregions, discretizes their magnetic-field contributions at each observation point, and expresses the cross-product and distance-matrix operations as multidimensional arrays. CPU mode uses NumPy; GPU mode uses CUDA-backed CuPy. Their compatible interfaces allow the matrix solver to switch between the two platforms.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_pipeline.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_pipeline.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. The EMSim layout-level EM simulation flow. Device-model approximation and parasitic-network reduction lower the cost of current analysis; GPU parallelization accelerates EM computation. Schematic based on <a href="/assets/pdf/TIFS_2023_EMSim.pdf">Ma et al., TIFS 2023</a>. </div> <p>EMSim applies the Biot-Savart law to calculate the magnetic flux density at an observation point. After the $i$th metal wire is divided into rectangular subregions, Equation (3-8) of the thesis can be written in the following discrete form:</p> \[\mathbf{B}_{t,p} \;\approx\; \frac{\mu_0}{4\pi} \sum_i \sum_j \sum_k \frac{\mathbf{J}_{i,t} \times \hat{\mathbf{r}}_{i,j,k,p}} {r_{i,j,k,p}^{2}}\,\Delta S_{i,j,k}\] <p>Here, $\mathbf{J}<em>{i,t}$ is the current density of metal wire $i$ at time $t$; $\hat{\mathbf{r}}</em>{i,j,k,p}$ and $r_{i,j,k,p}$ are the direction and distance from a subregion to observation point $p$; and $\Delta S_{i,j,k}$ is the subregion area. Summing the vector contribution of every subregion gives an approximation of the magnetic flux density at the observation point. Finer subdivision more closely approximates the surface integral but increases computation. In a measurement, changing magnetic flux induces a voltage in the near-field probe coil, so EMSim also converts its field result into a probe output signal.</p> <p>The thesis validates EMSim with S-Box and AES chips designed and fabricated in SMIC 180 nm CMOS, considering simulation accuracy, security-evaluation accuracy, and computational cost. The simulated and measured data achieved more than 74% time-domain accuracy and up to 98% spatial accuracy; the reported accuracy in evaluating information-leakage risk was 93%. EMSim was 32× faster than ConvEM. These results apply to the experimental circuits, process, and near-field measurement setup in the thesis and should not be extrapolated to arbitrary layouts or probe configurations.</p> <h2 id="emsim-gan--gan-based-evaluation-acceleration">EMSim+ (GAN) — GAN-based evaluation acceleration</h2> <p>EMSim still performs current analysis and EM computation for every trace. EMSim+ (ICCAD 2023) reformulates chip-level EM simulation as a paired image-to-image translation problem: EMSim first produces a small set of input-output sample pairs, those pairs train a generative adversarial network, and the trained generator synthesizes the large magnetic-field dataset required for security evaluation.</p> <p>The model input comprises a <strong>cell-current distribution</strong>, a <strong>power-grid distribution</strong>, and a time sequence. The cell-current distribution accumulates the transient current of each logic cell into its corresponding layout tile. The power-grid distribution uses the locations of metal wires and supply ports to describe the impedance characteristics of the top-level power grid. The output sample is the spatial magnetic-field distribution simulated by EMSim. These data are normalized before model training.</p> <p>The model is based on Pix2Pix. Its U-Net generator extracts spatial features from the cell-current and power-grid distributions, fuses them with the time feature, and outputs a synthetic magnetic-field distribution. During adversarial training, the discriminator distinguishes simulated magnetic-field distributions from synthetic ones. During risk quantification, the trained generator synthesizes the required number of magnetic-field samples, which are then analyzed with CEMA or another security-evaluation method.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_gan.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_gan.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. The EMSim+ generative adversarial network. The generator synthesizes a magnetic-field distribution from the cell-current distribution, power-grid distribution, and time sequence; the discriminator distinguishes synthetic data from samples generated by EMSim. Adapted from <a href="/assets/pdf/ICCAD_2023_EMSim+.pdf">Gao et al., ICCAD 2023</a>. </div> <p>Generator inference replaces repeated current analysis and EM computation at large dataset sizes, but model training still requires EMSim input-output sample pairs. The larger the evaluation dataset, the easier it is to amortize data preparation and training.</p> <p>For the four evaluated cryptographic circuits — AES, Kyber, a processor with an AES instruction-set extension, and a masked AES implementation — the paper reports a speedup of more than <strong>242×</strong> over EMSim at one million EM traces. In a separate experiment, the model was trained with measurements from a fabricated 180 nm AES-128 chip. The synthetic and measured magnetic-field distributions reached 99.5% NCC and 94.2% SSIM; MTD was about 265 traces for EMSim+ and 173 for the measurements. Both datasets allowed key recovery, but that does not make their statistical properties identical.</p> <p>The trained model, however, remains tied to the design that supplied the paired data.</p> <h2 id="emsim-gantl--transfer-across-related-designs">EMSim+ (GAN+TL) — transfer across related designs</h2> <p>Training from scratch gives every modified AES layout the same startup cost. The TIFS 2024 extension uses transfer learning to reuse a model trained on the baseline design.</p> <p>The GAN is first trained on a source design. For a target design, its pretrained parameters are copied, the discriminator is frozen, and the generator is fine-tuned with target-design samples. The experiment used 500 target pairs rather than the 1,000 used for training from scratch, cutting training time by roughly half. This demonstrates reuse within the evaluated design family; it does not show that 500 samples will suffice for an arbitrary circuit.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_tl.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_tl.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 3. The EMSim+ transfer-learning flow: train on source-design data $D_s$ to synthesize source magnetic-field distributions $T_s$, then freeze the discriminator and fine-tune the generator on target-design data $D_t$ to synthesize target distributions $T_t$. Adapted from <a href="/assets/pdf/TIFS_2024_EMSim+.pdf">Gao et al., TIFS 2024</a>. </div> <p>The source model was trained on an AES design in SMIC 180 nm. The four target designs were two masked AES variants (<code class="language-plaintext highlighter-rouge">AES_mask_1</code> and <code class="language-plaintext highlighter-rouge">AES_mask_2</code>), an AES layout with added power-grid strips (<code class="language-plaintext highlighter-rouge">AES_pg</code>), and the AES benchmark reimplemented in SMIC 55 nm (<code class="language-plaintext highlighter-rouge">AES_55nm</code>). Against magnetic-field distributions simulated by EMSim, the transferred models exceeded 99.5% in both NCC and structural similarity (SSIM). CEMA on the synthetic EM traces also reflected the results of different leakage models: for example, a first-order model did not reveal the key in <code class="language-plaintext highlighter-rouge">AES_mask_1</code>, whereas a toggle-count model targeting internal S-Box gates did. All four target-design comparisons use EMSim as the reference; the silicon comparison in the previous section applies only to the baseline AES experiment.</p> <p>Transfer learning does not change generator inference speed; it reduces the cost of target-design data preparation and model training. Including target-sample generation and fine-tuning, the paper reports a 113.37–149.23× speedup over EMSim at 100,000 EM traces. With a suitable pretrained source model, the estimated speedup at one million traces is 282.04–483.98×.</p> <p>The three EMSim projects address the EM side channel. AIPS extends learned simulation to the power side channel.</p> <h2 id="aips--diffusion-based-power-simulation">AIPS — diffusion-based power simulation</h2> <p>AIPS (TCHES 2026) models the transient power caused by data-dependent logic transitions. PTPX can generate reference power traces at nanosecond resolution, but its cost grows with design size and trace length. Earlier learned power estimators largely targeted average power and therefore missed the transient features required for SCA.</p> <p>AIPS uses transient power traces generated by PTPX as training targets. Its conditioning features come from two design sources. A value change dump (VCD) is divided into fixed time windows and converted into per-cell toggle counts. The standard-cell library supplies power lookup-table values and load capacitance. AIPS aligns these features with PTPX samples and learns to generate the transient power for each time window.</p> <p>AIPS uses diffusion because GAN training can be unstable or collapse onto a limited set of output modes. This is especially problematic when rare, data-dependent variations carry information leakage. Diffusion replaces adversarial training with a fixed denoising objective and tends to cover the output distribution more broadly. Toggle activity and standard-cell library features condition the denoising process, tying each synthetic power trace to the simulated workload.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/aips_diffusion.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/aips_diffusion.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 4. The AIPS diffusion-model training flow. VCD toggle activity and standard-cell library features provide the condition; the model learns to denoise $x_T$ into $x_0$ using transient power traces generated by PTPX as the reference. Adapted from <a href="/assets/pdf/TCHES_2026_AIPS.pdf">Gao et al., TCHES 2026</a>. </div> <p>The conditional reverse-diffusion step is:</p> \[p_\theta(\mathbf{x}_{t-1} \mid \mathbf{x}_t, \mathbf{c}) \;=\; \mathcal{N}\!\left(\boldsymbol{\mu}_\theta(\mathbf{x}_t, t, \mathbf{c}),\; \boldsymbol{\Sigma}_\theta(\mathbf{x}_t, t, \mathbf{c})\right)\] <p>At each reverse step, the model uses the current power trace $\mathbf{x}<em>t$, diffusion step $t$, and conditioning features $\mathbf{c}$ to estimate a distribution for the less noisy $\mathbf{x}</em>{t-1}$. Repeating the process produces a synthetic transient power trace. Because diffusion sampling is iterative, a fair cost comparison must include inference as well as the one-time cost of PTPX data generation and model training.</p> <p>The paper evaluates AIPS separately on five targets:</p> <ul> <li><strong>AES</strong> (the baseline cipher, unmasked)</li> <li><strong>Kyber</strong> (a NIST-selected post-quantum key-encapsulation mechanism)</li> <li><strong>Two masked AES variants</strong> (used to evaluate higher-order SCA)</li> <li><strong>A RISC-V core</strong> (a general-purpose processor executing AES, not a dedicated crypto block)</li> </ul> <p>Across the tested settings, AIPS improved power-trace similarity over the GAN baseline and reproduced the SCA results obtained with PTPX data, including first-order attacks on AES and Kyber, higher-order analysis of masked AES, and key-byte recovery on the RISC-V core. These conclusions apply to the leakage models and attack methods used in the paper; they do not establish that every leakage order or implementation detail is preserved. On efficiency, the paper reports:</p> <ul> <li><strong>4.14–42.44× faster than PTPX at 1M traces</strong>, including data preparation and training</li> <li><strong>Up to 10⁴× per-trace speedup once training is amortized</strong></li> <li><strong>About 1K or fewer training traces</strong> in the reported minimum-data experiments</li> <li><strong>A larger throughput advantage as trace length increases</strong></li> </ul> <h2 id="putting-the-four-projects-together">Putting the four projects together</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/lineage.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/lineage.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 5. The four-paper lineage: layout-level EM simulation → GAN-based evaluation acceleration, per-design training → transfer across related designs, and the EM side channel → the power side channel (with GAN → diffusion in the final step). </div> <p>Three changes define the progression. EMSim+ trains a GAN with a small EMSim dataset and then synthesizes a much larger magnetic-field dataset. Transfer learning reuses the model when the AES design or fabrication process changes. AIPS moves from the EM side channel to the power side channel, and from GAN-synthesized magnetic-field distributions to diffusion-generated transient power traces.</p> <table> <thead> <tr> <th>Paper</th> <th>Side channel</th> <th>Core method</th> <th>Scope</th> <th>Training data</th> <th>Speedup vs. reference flow</th> <th>Validated on</th> </tr> </thead> <tbody> <tr> <td>EMSim</td> <td>EM</td> <td>device-model approximation + parasitic-network reduction + GPU parallelization</td> <td>per layout</td> <td>n/a</td> <td>32× over ConvEM</td> <td>S-Box and AES chips in SMIC 180 nm</td> </tr> <tr> <td>EMSim+ (GAN)</td> <td>EM</td> <td>conditional GAN</td> <td>per trained design</td> <td>1K pairs in experiments</td> <td>&gt;242× over EMSim at 1M traces</td> <td>Four crypto circuits; separate 180 nm AES silicon study</td> </tr> <tr> <td>EMSim+ (GAN+TL)</td> <td>EM</td> <td>GAN + transfer learning</td> <td>related designs/nodes</td> <td>500 target pairs</td> <td>113–149× at 100K; estimated 282–484× at 1M</td> <td>AES source + four AES-derived targets, 180 nm and 55 nm</td> </tr> <tr> <td>AIPS</td> <td>Power</td> <td>conditional diffusion</td> <td>trained/evaluated per target</td> <td>about 1K traces or fewer</td> <td>4.14–42.44× over PTPX at 1M; up to 10⁴× inference-only</td> <td>AES, Kyber, masked AES (×2), and RISC-V</td> </tr> </tbody> </table> <p>The limitations are just as important as the speedups. The learned models depend on reference traces and on the leakage models and attack methods used for validation. Transfer was demonstrated within an AES-centered design family, and AIPS was trained and evaluated separately for each target rather than as a universal model. Pre-silicon side-channel security evaluation still requires trade-offs among simulation accuracy, reference-data cost, model transferability, side-channel coverage, and security-evaluation accuracy.</p> <h2 id="open-problems">Open problems</h2> <p><strong>Masked and post-quantum designs at scale.</strong> AIPS covers Kyber and two masked AES variants, but the experiments do not establish coverage for higher masking orders, signature schemes such as ML-DSA or Falcon, or complete systems-on-chip. Those settings raise open questions about training-set size, leakage orders, background switching activity, and diffusion sampling cost.</p> <p><strong>Closing the loop between evaluation and protection.</strong> Security evaluation can locate leakage hotspots or validate attack results, but it does not automatically identify the paths along which sensitive information leaks or produce a countermeasure. PathFinder (DAC 2022 · <a href="/assets/pdf/DAC_2022_PathFinder.pdf">PDF</a>) and Formal Path (TECS 2025 · <a href="/assets/pdf/TECS_2025_Formal_Path.pdf">PDF</a>) are our work on leakage-path identification and logic-level obfuscation. Connecting leakage-path analysis and targeted hardening to rapid reevaluation remains an open problem.</p> <h2 id="code">Code</h2> <ul> <li><a href="https://github.com/jinyier/EMSim"><code class="language-plaintext highlighter-rouge">github.com/jinyier/EMSim</code></a> — EMSim and EMSim+ implementations.</li> <li><a href="https://github.com/jinyier/AIPS"><code class="language-plaintext highlighter-rouge">github.com/jinyier/AIPS</code></a> — AIPS implementation.</li> </ul> <h2 id="references">References</h2> <ol> <li>H. Ma, M. Panoff, J. He, Y. Zhao, Y. Jin. EMSim: A Fast Layout Level Electromagnetic Emanation Simulation Framework for High Accuracy Pre-Silicon Verification. <em>IEEE Transactions on Information Forensics and Security</em>, 2023. <a href="/assets/pdf/TIFS_2023_EMSim.pdf">PDF</a></li> <li>Y. Gao, H. Ma, J. Kong, J. He, Y. Zhao, Y. Jin. EMSim+: Accelerating Electromagnetic Security Evaluation with Generative Adversarial Network. <em>IEEE/ACM ICCAD</em>, 2023. <a href="/assets/pdf/ICCAD_2023_EMSim+.pdf">PDF</a></li> <li>Y. Gao, H. Ma, Q. Zhang, X. Song, Y. Jin, J. He, Y. Zhao. EMSim+: Accelerating Electromagnetic Security Evaluation With Generative Adversarial Network and Transfer Learning. <em>IEEE Transactions on Information Forensics and Security</em>, 2024. <a href="/assets/pdf/TIFS_2024_EMSim+.pdf">PDF</a></li> <li>Y. Gao, H. Ma, T. Zhang, J. He, Y. Zhao, M. Stojilović, Y. Jin. AIPS: AI-Based Power Simulation for Pre-Silicon Side-Channel Security Evaluation. <em>IACR Transactions on Cryptographic Hardware and Embedded Systems</em>, 2026. <a href="/assets/pdf/TCHES_2026_AIPS.pdf">PDF</a></li> <li>H. Ma. Research on Pre-Silicon Security Evaluation and Protection Techniques for Cryptographic Chip. Ph.D. Thesis, Tianjin University, 2023. <a href="/assets/pdf/PhD_Thesis_2023.pdf">PDF</a></li> </ol>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[From layout-level EM simulation to GANs, transfer learning, and diffusion-based power modeling.]]></summary></entry><entry xml:lang="zh"><title type="html">Pre-Silicon Side-Channel Evaluation: From Physics to Diffusion</title><link href="https://mahaocheng.me/blog/2026/pre-silicon-side-channel-evaluation/zh/" rel="alternate" type="text/html" title="Pre-Silicon Side-Channel Evaluation: From Physics to Diffusion"/><published>2026-05-10T00:00:00+00:00</published><updated>2026-05-10T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2026/pre-silicon-side-channel-evaluation/pre-silicon-side-channel-evaluation.zh</id><content type="html" xml:base="https://mahaocheng.me/blog/2026/pre-silicon-side-channel-evaluation/zh/"><![CDATA[<p><em>EMSim 源于我在天津大学的博士研究。后续的 EMSim+ 与 AIPS 由我指导的高雅主导，我作为共同作者参与。把四项工作放在一起看，可以看到一条清楚的演进路线：每项工作都在解决上一种方法留下的问题。</em></p> <h2 id="为什么要开展硅前安全测评">为什么要开展硅前安全测评？</h2> <p>密码芯片运行时，逻辑单元的翻转活动会产生与处理数据相关的功耗和电磁辐射。侧信道分析（Side-Channel Analysis，SCA）利用这些物理信息恢复密钥等敏感数据，而不直接攻击密码算法本身。</p> <p>安全测评通常采用两类方法。基于攻击的评估方法直接模拟攻击者恢复敏感信息；例如，相关电磁分析（Correlation Electromagnetic Analysis，CEMA）根据已知明文和猜测密钥构造假设中间值，经信息泄露模型映射为假设电磁辐射，再与实际电磁曲线计算皮尔逊相关系数。破解密钥所需的最小曲线数量（Measurement To Disclosure，MTD）可用于衡量攻击难度。基于信息泄露的评估方法则判断数据分组之间是否存在统计差异，例如测试向量泄露评估（TVLA）。TVLA 可能报告无法被实际攻击利用的泄露，因此本文涉及 EMSim 的测评结果时，以 CEMA 的攻击结果为主要依据。</p> <p>传统安全测评集中在硅后阶段。如果首版芯片存在信息泄露，修改寄存器传输级（RTL）设计或物理版图后还要重新经历制造环节。硅前安全测评把这一反馈移到芯片设计阶段：从设计数据仿真电磁曲线或功耗曲线，采用与硅后测评相同的分析方法量化信息泄露风险，再据此修改设计。难点在于，仿真结果既要在时域和空间域上接近实际测量，又要满足高安全等级测评对大规模数据量的要求。</p> <h2 id="仿真的四个瓶颈">仿真的四个瓶颈</h2> <p>以一个 AES 核的迭代过程为例：先完成布局布线和初次测评，再加入掩码或修改电源网格，最后迁移到另一个工艺节点。每次改动之后，都需要重新量化信息泄露风险。仿真方法既要保留电磁曲线或功耗曲线的时域特征，也要在涉及局部电磁分析时保留空间分布，还要生成足够数量的曲线。这个过程主要受四个瓶颈限制。</p> <ol> <li> <p><strong>版图级电磁仿真的计算成本高。</strong> 仿真需要从物理版图获得逻辑单元、金属互连、寄生参数和瞬态电流，再计算芯片表面任意观测点的磁场。物理信息提高了仿真精度，也增加了大规模生成电磁曲线的成本。</p> </li> <li> <p><strong>模型简化后的电磁仿真仍需逐条计算。</strong> EMSim 相比 ConvEM 提升了 32 倍的时间效率，但每增加一个明文或掩码值，仍要重新执行电流分析和电磁计算。</p> </li> <li> <p><strong>学习模型存在逐设计训练的成本。</strong> 为基准 AES 版图训练的模型，在版图、防护方案或制造工艺变化后，需要新的输入输出样本对。每次迭代都从头训练，会削弱快速生成大规模测评数据的优势。</p> </li> <li> <p><strong>功耗侧信道测评需要瞬态功耗曲线。</strong> 面向热分析或电压降分析的学习模型通常预测平均功耗，而侧信道分析依赖纳秒尺度、与数据相关的瞬态变化。Synopsys PrimeTime PX（PTPX）可以生成瞬态功耗曲线，但计算成本会随电路规模和曲线长度增长。</p> </li> </ol> <h2 id="emsim版图级电磁仿真">EMSim：版图级电磁仿真</h2> <p>EMSim 从物理版图出发，主要包括数据准备、电流分析和电磁计算三个环节。数据准备环节由 RTL 到 GDS 的芯片设计流程建立版图数据库；电流分析环节获得物理版图上的瞬态电流分布；电磁计算环节再依据电磁场理论，将瞬态电流转化为任意观测点的磁场数据。与只使用 RTL 或门级网表的行为模型相比，物理版图还包含制造工艺、单元位置、金属布线尺寸和寄生参数，因此更接近实际芯片的电磁行为。</p> <p>EMSim 的理论依据来自电流聚合效应和金属屏蔽效应。逻辑单元的翻转活动是电磁信息的来源，金属互连是辐射载体。逻辑单元产生的电流在片上电源网络中聚合，使顶层电源网格流过较大幅度的瞬态电流；较低金属层产生的电磁辐射则受到上层金属屏蔽。因此，EMSim 重点计算顶层电源网格的瞬态电流和磁场，在保留主导因素的同时降低计算成本。</p> <p>具体采用三项技术。</p> <p><strong>器件模型近似</strong>将逻辑单元的翻转活动等效为单元级电流源激励。动态门级仿真先记录逻辑单元的翻转活动，单元功耗分析再根据 Liberty 库中的功耗查找表得到瞬态功耗，并除以电源电压得到瞬态电流。这样，深亚微米器件模型中的非线性方程求解被转换为分段线性的电流查找表。</p> <p><strong>寄生网络约减</strong>利用已经确定的单元级电流源，从寄生网络模型中排除信号互连线，保留获得顶层电源网格瞬态电流所需的片上电源网络。这样既减少了节点方程的计算量，也降低了 SPICE 仿真的收敛难度。</p> <p><strong>GPU 并行计算</strong>用于电磁计算环节。EMSim 将金属线划分为等面积的矩形子区域，离散求解各子区域对观测点的磁场贡献，并把叉乘矩阵和距离矩阵的运算表示为多维数组。CPU 模式使用 NumPy，GPU 模式使用基于 CUDA 的 CuPy；两者接口兼容，可以切换矩阵求解平台。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_pipeline.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_pipeline.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> 图 1. EMSim 的版图级电磁仿真流程。器件模型近似和寄生网络约减降低电流分析成本，GPU 并行计算加速电磁计算。示意图依据 <a href="/assets/pdf/TIFS_2023_EMSim.pdf">Ma 等，TIFS 2023</a> 绘制。 </div> <p>EMSim 依据毕奥-萨伐尔定律计算观测点的磁感应强度。将第 $i$ 条金属线划分为矩形子区域后，论文公式（3-8）可写成以下离散形式：</p> \[\mathbf{B}_{t,p} \;\approx\; \frac{\mu_0}{4\pi} \sum_i \sum_j \sum_k \frac{\mathbf{J}_{i,t} \times \hat{\mathbf{r}}_{i,j,k,p}} {r_{i,j,k,p}^{2}}\,\Delta S_{i,j,k}\] <p>其中，$\mathbf{J}<em>{i,t}$ 是第 $i$ 条金属线在时刻 $t$ 的电流密度，$\hat{\mathbf{r}}</em>{i,j,k,p}$ 和 $r_{i,j,k,p}$ 分别表示子区域到观测点 $p$ 的方向和距离，$\Delta S_{i,j,k}$ 是子区域面积。将所有子区域的贡献矢量相加，即得到观测点的近似磁感应强度。子区域划分越细，离散结果越接近面积分，但计算量也越大。实际测量时，变化的磁通量在近场探头线圈中感应出电压信号，因此 EMSim 还会把磁场结果转换为探头接收信号。</p> <p>论文采用中芯国际 180 nm CMOS 工艺设计并制造 S-Box 和 AES 芯片，从仿真精度、测评准确度和计算成本三个方面验证 EMSim。仿真结果与实测数据的时域准确度高于 74%，空间域准确度高达 98%，对信息泄露风险的测评准确度为 93%；相对于 ConvEM，EMSim 的时间效率提升了 32 倍。这些结果适用于论文中的实验电路、工艺和近场测量设置，不能直接外推到任意版图与探头配置。</p> <h2 id="emsimgan基于生成对抗网络的测评优化">EMSim+（GAN）：基于生成对抗网络的测评优化</h2> <p>EMSim 仍需逐条执行电流分析和电磁计算。EMSim+（ICCAD 2023）把芯片电磁仿真转换为配对图像翻译问题：先由 EMSim 生成少量输入输出样本对，训练生成对抗网络，再由生成器快速合成安全测评所需的大规模磁场数据。</p> <p>模型输入由<strong>单元电流分布</strong>、<strong>电源网格分布</strong>和时间序列组成。单元电流分布将各逻辑单元的瞬态电流累加到对应版图网格；电源网格分布依据金属线与供电端口的位置描述顶层电源网格的阻抗特性。输出样本是 EMSim 仿真的空间磁场分布。在论文的测评优化流程中，这些数据经过归一化后用于模型训练。</p> <p>模型以 Pix2Pix 为基础。U-Net 生成器提取单元电流分布和电源网格分布的空间特征，并与时间特征融合，输出合成磁场分布；判别器在对抗训练中区分真实磁场分布与合成磁场分布。进入风险量化环节后，只需使用训练好的生成器合成规定数量的磁场数据，再结合 CEMA 等安全评估方法量化信息泄露风险。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_gan.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_gan.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> 图 2. EMSim+ 的生成对抗网络模型：生成器根据单元电流分布、电源网格分布和时间序列合成磁场分布，判别器区分合成数据与 EMSim 生成的真实样本。改绘自 <a href="/assets/pdf/ICCAD_2023_EMSim+.pdf">Gao 等，ICCAD 2023</a>。 </div> <p>生成器推理取代了大规模数据量下反复进行的电流分析和电磁计算，但模型训练仍需要 EMSim 提供输入输出样本对。测评数据量越大，前期数据准备和模型训练成本越容易被摊销。</p> <p>论文评估了四种密码电路：AES、Kyber、带 AES 指令扩展的处理器，以及掩码 AES。在一百万条电磁曲线时，EMSim+ 相对 EMSim 的加速超过 <strong>242×</strong>。另一项独立实验使用一颗 180 nm AES-128 芯片的实测数据训练模型：合成磁场分布与实测磁场分布的 NCC 为 99.5%，SSIM 为 94.2%；破解密钥所需的最小曲线数量（MTD）在 EMSim+ 数据上约为 265，在实测数据上约为 173。两组数据都能恢复密钥，但不能据此认为它们的统计特性完全一致。</p> <p>不过，训练后的模型仍与提供配对数据的设计绑定。</p> <h2 id="emsimgantl在相关设计间迁移">EMSim+（GAN+TL）：在相关设计间迁移</h2> <p>如果每个修改后的 AES 版图都要从头训练，每次迭代都要重复支付前期成本。TIFS 2024 的扩展引入迁移学习，复用基准设计上已经训练好的模型。</p> <p>首先在源设计上训练 GAN。面对目标设计时，复制预训练参数，冻结判别器，再用目标设计样本微调生成器。实验使用 500 个目标样本对，从头训练则使用 1,000 个样本对，训练时间大约减半。这个结果说明模型能在论文评估的设计族中复用，但不能推断任意电路都只需 500 个样本即可完成迁移。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_tl.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/emsim_tl.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> 图 3. EMSim+ 迁移学习流程：先用源设计数据 $D_s$ 训练模型以合成源设计磁场分布 $T_s$，随后冻结判别器，用目标设计数据 $D_t$ 微调生成器以合成目标设计磁场分布 $T_t$。改绘自 <a href="/assets/pdf/TIFS_2024_EMSim+.pdf">Gao 等，TIFS 2024</a>。 </div> <p>源模型来自中芯国际 180 nm 工艺的 AES 设计。四个目标设计包括两个掩码 AES 变体（<code class="language-plaintext highlighter-rouge">AES_mask_1</code> 和 <code class="language-plaintext highlighter-rouge">AES_mask_2</code>）、增加电源网格金属条的 AES 版图（<code class="language-plaintext highlighter-rouge">AES_pg</code>），以及在中芯国际 55 nm 工艺下重新实现的 AES（<code class="language-plaintext highlighter-rouge">AES_55nm</code>）。以 EMSim 仿真的磁场分布为参考，迁移模型的 NCC 和结构相似性（SSIM）均超过 99.5%。对合成电磁曲线执行 CEMA，也能反映不同信息泄露模型的攻击结果：例如，一阶模型无法从 <code class="language-plaintext highlighter-rouge">AES_mask_1</code> 恢复密钥，而针对 S-Box 内部逻辑门的翻转计数模型可以。这里四个目标设计均以 EMSim 为参照；上一节的硅片实测对比只适用于基准 AES 实验。</p> <p>迁移学习不会改变生成器的推理速度；它降低的是目标设计的数据准备和模型训练成本。计入目标样本生成与微调后，论文报告在十万条电磁曲线时相对 EMSim 加速 113.37–149.23×；在已有合适源模型的前提下，一百万条电磁曲线时的估算加速比为 282.04–483.98×。</p> <p>前三项工作都面向电磁侧信道。AIPS 则把学习型仿真扩展到功耗侧信道。</p> <h2 id="aips用扩散模型生成瞬态功耗">AIPS：用扩散模型生成瞬态功耗</h2> <p>AIPS（TCHES 2026）对数据相关逻辑翻转产生的瞬态功耗进行建模。PTPX 可以生成纳秒分辨率的参考功耗曲线，但成本会随设计规模和曲线长度增长。此前的学习型功耗估算方法大多面向平均功耗，无法保留侧信道分析所需的瞬态特征。</p> <p>AIPS 以 PTPX 生成的瞬态功耗曲线为训练目标，条件特征来自两类设计数据。首先，将值变化转储（VCD）文件切分为固定时间窗口，并统计各逻辑单元的翻转次数；其次，从标准单元库提取功耗查找表数值和负载电容。AIPS 将这些特征与 PTPX 样本对齐，学习生成各时间窗口的瞬态功耗。</p> <p>AIPS 采用扩散模型，是因为 GAN 训练可能不稳定，也可能只覆盖有限的输出模式。当少见的数据相关变化承载信息泄露时，这个缺点尤其突出。扩散模型将对抗训练换成固定的去噪目标，通常能覆盖更完整的输出分布。在 AIPS 中，翻转活动与标准单元库特征作为去噪条件，使每条合成功耗曲线与被仿真的工作负载对应。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/aips_diffusion.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/aips_diffusion.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> 图 4. AIPS 的扩散模型训练流程：VCD 翻转活动与标准单元库特征共同构成条件，模型学习把 $x_T$ 逐步去噪为 $x_0$，并以 PTPX 生成的瞬态功耗曲线为参考。改绘自 <a href="/assets/pdf/TCHES_2026_AIPS.pdf">Gao 等，TCHES 2026</a>。 </div> <p>条件反向扩散步骤为：</p> \[p_\theta(\mathbf{x}_{t-1} \mid \mathbf{x}_t, \mathbf{c}) \;=\; \mathcal{N}\!\left(\boldsymbol{\mu}_\theta(\mathbf{x}_t, t, \mathbf{c}),\; \boldsymbol{\Sigma}_\theta(\mathbf{x}_t, t, \mathbf{c})\right)\] <p>在每个反向步骤中，模型根据当前功耗曲线 $\mathbf{x}<em>t$、扩散步骤 $t$ 和条件特征 $\mathbf{c}$，估计噪声更少的 $\mathbf{x}</em>{t-1}$ 的分布。重复这个过程，最终得到一条合成瞬态功耗曲线。由于扩散采样需要多次迭代，比较效率时既要计算推理成本，也要计算一次性的 PTPX 数据生成和模型训练成本。</p> <p>论文分别在五个目标上评估 AIPS：</p> <ul> <li><strong>AES</strong>：无掩码的基准密码核</li> <li><strong>Kyber</strong>：NIST 选定的后量子密钥封装机制</li> <li><strong>两个掩码 AES 变体</strong>：用于测试高阶侧信道分析</li> <li><strong>一个 RISC-V 核</strong>：由通用处理器执行 AES，而非专用密码模块</li> </ul> <p>在论文的实验设置下，AIPS 相比 GAN 基线提高了功耗曲线的相似性，并复现了基于 PTPX 数据得到的侧信道分析结果，包括针对 AES 和 Kyber 的一阶攻击、对掩码 AES 的高阶分析，以及在 RISC-V 核上的密钥字节恢复。这些结论只适用于论文采用的信息泄露模型和攻击方法，不能证明所有泄露阶数或实现细节都得到保留。效率方面，论文报告：</p> <ul> <li><strong>在一百万条功耗曲线时比 PTPX 快 4.14–42.44×</strong>，包含数据准备和训练成本</li> <li><strong>训练成本摊销后，单条功耗曲线最高加速 10⁴×</strong></li> <li><strong>最小数据量实验使用约一千条或更少的训练曲线</strong></li> <li><strong>曲线越长，吞吐量优势越明显</strong></li> </ul> <h2 id="四项工作的关系">四项工作的关系</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/lineage.png" sizes="95vw"/> <img src="/assets/img/2026-05-10-pre-silicon-side-channel-evaluation/lineage.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> 图 5. 四篇论文形成的研究路线：版图级电磁仿真 → 基于生成对抗网络的测评优化，逐设计训练 → 在相关设计间迁移，以及电磁侧信道 → 功耗侧信道（同时由生成对抗网络转向扩散模型）。 </div> <p>这条路线经历了三次变化。EMSim+ 用少量 EMSim 样本训练生成对抗网络，再合成大规模磁场数据；迁移学习在 AES 设计或制造工艺变化后复用已有模型；AIPS 则从电磁侧信道转向功耗侧信道，并由生成对抗网络合成磁场分布改为用扩散模型生成瞬态功耗曲线。</p> <table> <thead> <tr> <th>工作</th> <th>侧信道类型</th> <th>核心方法</th> <th>适用范围</th> <th>训练数据</th> <th>相对参考流程的加速</th> <th>验证对象</th> </tr> </thead> <tbody> <tr> <td>EMSim</td> <td>电磁</td> <td>器件模型近似 + 寄生网络约减 + GPU 并行计算</td> <td>每个版图</td> <td>不适用</td> <td>相对 ConvEM 提升 32×</td> <td>中芯国际 180 nm 的 S-Box 与 AES 芯片</td> </tr> <tr> <td>EMSim+（GAN）</td> <td>电磁</td> <td>条件生成对抗网络</td> <td>每个已训练设计</td> <td>实验使用 1K 样本对</td> <td>一百万条电磁曲线时相对 EMSim &gt;242×</td> <td>四种密码电路；另含 180 nm AES 芯片实测实验</td> </tr> <tr> <td>EMSim+（GAN+TL）</td> <td>电磁</td> <td>生成对抗网络 + 迁移学习</td> <td>相关设计与工艺节点</td> <td>500 个目标样本对</td> <td>十万条时 113–149×；一百万条时估算 282–484×</td> <td>AES 源设计及四个 AES 衍生目标，覆盖 180 nm 与 55 nm</td> </tr> <tr> <td>AIPS</td> <td>功耗</td> <td>条件扩散模型</td> <td>分别训练并评估各目标</td> <td>约 1K 条或更少</td> <td>一百万条功耗曲线时相对 PTPX 为 4.14–42.44×；纯推理最高 10⁴×</td> <td>AES、Kyber、两个掩码 AES 与 RISC-V</td> </tr> </tbody> </table> <p>这些加速数字之外，方法的适用边界同样重要。学习模型依赖参考曲线以及用于验证的信息泄露模型和攻击方法；迁移学习只在以 AES 为中心的设计族中得到验证；AIPS 也是针对不同目标分别训练和测评，并非适用于所有设计的通用模型。硅前侧信道安全测评仍需在仿真精度、参考数据成本、模型迁移能力、侧信道类型和测评准确度之间取舍。</p> <h2 id="开放问题">开放问题</h2> <p><strong>掩码和后量子设计的大规模测评。</strong> AIPS 已覆盖 Kyber 和两个掩码 AES 变体，但这些实验没有证明它能覆盖更高阶掩码、ML-DSA 或 Falcon 等签名方案，以及完整的片上系统。这些场景仍需研究训练集规模、信息泄露阶数、背景翻转活动和扩散采样成本。</p> <p><strong>形成安全测评与防护的闭环。</strong> 安全测评可以定位信息泄露热点或验证攻击结果，却不会自动识别敏感信息的泄露路径，也不会直接给出防护方案。PathFinder（DAC 2022 · <a href="/assets/pdf/DAC_2022_PathFinder.pdf">PDF</a>) 和 Formal Path（TECS 2025 · <a href="/assets/pdf/TECS_2025_Formal_Path.pdf">PDF</a>) 是我们在安全溯源和逻辑级混淆方面的工作。如何把泄露路径识别、靶向增强与快速重新测评连接起来，仍需进一步研究。</p> <h2 id="代码">代码</h2> <ul> <li><a href="https://github.com/jinyier/EMSim"><code class="language-plaintext highlighter-rouge">github.com/jinyier/EMSim</code></a> — EMSim 与 EMSim+ 的实现。</li> <li><a href="https://github.com/jinyier/AIPS"><code class="language-plaintext highlighter-rouge">github.com/jinyier/AIPS</code></a> — AIPS 的实现。</li> </ul> <h2 id="参考文献">参考文献</h2> <ol> <li>H. Ma, M. Panoff, J. He, Y. Zhao, Y. Jin. EMSim: A Fast Layout Level Electromagnetic Emanation Simulation Framework for High Accuracy Pre-Silicon Verification. <em>IEEE Transactions on Information Forensics and Security</em>, 2023. <a href="/assets/pdf/TIFS_2023_EMSim.pdf">PDF</a></li> <li>Y. Gao, H. Ma, J. Kong, J. He, Y. Zhao, Y. Jin. EMSim+: Accelerating Electromagnetic Security Evaluation with Generative Adversarial Network. <em>IEEE/ACM ICCAD</em>, 2023. <a href="/assets/pdf/ICCAD_2023_EMSim+.pdf">PDF</a></li> <li>Y. Gao, H. Ma, Q. Zhang, X. Song, Y. Jin, J. He, Y. Zhao. EMSim+: Accelerating Electromagnetic Security Evaluation With Generative Adversarial Network and Transfer Learning. <em>IEEE Transactions on Information Forensics and Security</em>, 2024. <a href="/assets/pdf/TIFS_2024_EMSim+.pdf">PDF</a></li> <li>Y. Gao, H. Ma, T. Zhang, J. He, Y. Zhao, M. Stojilović, Y. Jin. AIPS: AI-Based Power Simulation for Pre-Silicon Side-Channel Security Evaluation. <em>IACR Transactions on Cryptographic Hardware and Embedded Systems</em>, 2026. <a href="/assets/pdf/TCHES_2026_AIPS.pdf">PDF</a></li> <li>H. Ma. Research on Pre-Silicon Security Evaluation and Protection Techniques for Cryptographic Chip. Ph.D. Thesis, Tianjin University, 2023. <a href="/assets/pdf/PhD_Thesis_2023.pdf">PDF</a></li> </ol>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[四篇论文梳理密码芯片硅前侧信道安全测评的发展：从版图级电磁仿真，到生成对抗网络、迁移学习与基于扩散模型的瞬态功耗仿真。]]></summary></entry><entry><title type="html">virtCCA: Measured Boot and Attestation Demystified</title><link href="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/" rel="alternate" type="text/html" title="virtCCA: Measured Boot and Attestation Demystified"/><published>2026-01-21T00:00:00+00:00</published><updated>2026-01-21T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation</id><content type="html" xml:base="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/"><![CDATA[<p>💭 A quick note: virtCCA’s measured-boot and remote-attestation features were added gradually as the product matured. The implementation now spans several components, while the documentation is scattered across sources written at different times. This article brings those pieces together into one architectural view.</p> <h2 id="introduction">Introduction</h2> <p>Huawei virtCCA is a confidential-computing system built on the TrustZone and Secure EL2 features of Kunpeng processors. It implements the main ideas of Arm CCA on existing hardware: applications run in a hardware-isolated Trusted Execution Environment (TEE) whose state can be verified.</p> <p>virtCCA also provides a migration path toward native CCA hardware without requiring changes to existing applications. See <a href="https://arxiv.org/abs/2306.11011">virtCCA: Virtualized Arm Confidential Compute Architecture with TrustZone</a> and the <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_19_0003.html">Confidential Computing TEE Suite Technical White Paper</a> for details.</p> <p>Hardware isolation draws the trust boundary. Measured Boot and Attestation make the state within that boundary verifiable, as shown in Figure 1:</p> <ul> <li> <p>Measured Boot: following the rule “measure before execute,” virtCCA hashes the state of its Trusted Computing Base (TCB) while the platform and Confidential VM (CVM) boot. It records the resulting measurements in protected registers.</p> </li> <li> <p>Attestation: a hardware-protected Attestation Key signs those measurements and places them in an Attestation Report. A verifier uses the report to establish the identity and measured state of the CVM and its platform.</p> </li> </ul> <p>The result is a chain of trust from the hardware Root of Trust (RoT) to the CVM. A Relying Party can check remotely that a workload is running in a CVM on a genuine virtCCA platform and that its measurements match an accepted security baseline.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. Measured Boot and Attestation </div> <p>💡 The Trusted Computing Base (TCB) is the hardware, firmware, and software that the system depends on for security. In virtCCA it includes hardware such as the CPU and RoT, firmware such as TF-A and the TMM, and critical guest code such as UEFI, GRUB, and the OS kernel. A vulnerability or unauthorized change in any of these components can break the system’s confidentiality or integrity.</p> <h2 id="measured-boot">Measured Boot</h2> <p>The virtCCA architecture separates a CVM from the underlying hardware and firmware that host it. This corresponds to the distinction between a Realm and the CCA Platform in the Arm CCA specification (see <a href="https://developer.arm.com/documentation/DEN0096/latest">Arm CCA Security Model 1.0</a>). Measured Boot therefore has two linked parts, shown in Figure 2: Platform Measurement and CVM Measurement. A CVM measurement is useful only when the platform reporting it can also be trusted.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. Platform Measurement and CVM Measurement </div> <h3 id="platform-measurement">Platform Measurement</h3> <p>Platform Measurement passes trust from one boot stage to the next. During hardware initialization, the BSBC serves as the Core Root of Trust for Measurement (CRTM). The HSM running on the InSE provides the Root of Trust for Storage (RTS) and Root of Trust for Reporting (RTR), with measurements held in the InSE’s protected internal SRAM (Figure 2). The boot order follows the Arm CCA firmware model: Trusted Subsystems → Application PE → Monitor → RMM. The component boundaries differ from native Arm CCA because virtCCA works within the architecture of existing Kunpeng hardware.</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Measured Components</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Description</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>BSBC</strong> (BootROM Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Secure boot code embedded in the on-chip ROM</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ESBC</strong> (External Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Secure boot code stored in off-chip non-volatile memory</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>InSE</strong> (Integrated Secure Element)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">An on-chip secure subsystem with physical-security protection</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>HSM</strong> (Hardware Security Module)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">High-security firmware running on InSE; provides key management, secure storage, and Platform-token generation</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMU</strong> (Intelligence Management Unit)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Intelligent management unit responsible for hardware management, monitoring, and secure boot</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IPU</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ATF</strong> (Arm Trusted Firmware)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Low-level secure firmware running at the highest exception level (EL3)</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>TMM</strong> (TrustZone Management Monitor)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">A lightweight secure-monitoring component responsible for the CVM lifecycle, memory isolation, and interface dispatch</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>UEFI</strong> (Unified Extensible Firmware Interface)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Platform boot firmware that initializes hardware, performs security checks, and boots the system</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMF AP</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> </tbody> </table> </div> <p>💡 Roots of Trust (RoTs) are components whose incorrect behavior cannot be detected by another component in the same trust model. The Root of Trust for Measurement (RTM) measures software and sends the values to the RTS. The Root of Trust for Storage keeps those values in access-controlled, tamper-resistant storage. The Root of Trust for Reporting turns the stored measurements into an attestation report. The CRTM is the first code to run after reset and starts this chain.</p> <h3 id="cvm-measurement">CVM Measurement</h3> <p>virtCCA supports two ways to boot a CVM (Figure 3): Direct Kernel Boot and Virtual Firmware Boot.</p> <ul> <li>Direct Kernel Boot: the hypervisor (QEMU/KVM) loads the OS kernel and initial RAM filesystem (initramfs) directly, without virtual firmware or a bootloader.</li> <li>Virtual Firmware Boot: virtual firmware (UEFI) initializes the virtual hardware and loads a bootloader such as GRUB; the bootloader then loads the kernel and initramfs. The rest of this article calls this path UEFI Boot.</li> </ul> <p>Direct Kernel Boot starts quickly and uses fewer resources, which suits lightweight container environments such as Kata Containers. UEFI Boot works with existing OS images and more complex configurations, including multiple kernels and boot entries. It also behaves more like a conventional VM from the cloud management layer, making it a better fit for general-purpose IaaS instances.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 3. Direct Kernel Boot and Firmware Boot </div> <p>Both paths start from the same security rule: assets supplied by the host are untrusted. Before the CVM uses host-supplied code, data, or configuration, the TMM—or an RTM to which it delegates the work—must measure it. Those measurements are extended into registers protected by the TMM. Section A7.1 of the <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a> defines two kinds of register: the Realm Initial Measurement (RIM) and Realm Event Measurements (REMs). The TMM calculates the RIM from the CVM’s initial register and memory state. Later boot stages extend the REMs; for example, UEFI measures the GRUB image before loading it.</p> <h4 id="direct-kernel-boot">Direct Kernel Boot</h4> <p>For Direct Kernel Boot, virtCCA follows the measurement model in the Arm CCA RMM specification. The TMM is closed source, so the description below uses the corresponding RMM terminology. The three operations are summarized in the table. Their definitions appear in sections B4.3.9.4 (<code class="language-plaintext highlighter-rouge">RMI_REALM_CREATE</code> initialization of RIM), B4.3.12.4 (<code class="language-plaintext highlighter-rouge">RMI_REC_CREATE</code> extension of RIM), and B4.3.1.4 (<code class="language-plaintext highlighter-rouge">RMI_DATA_CREATE</code> extension of RIM) of the <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a>.</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px; width: 20%;">Phase</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Description</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Create a Realm</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>A CCA Realm corresponds to a virtCCA CVM.</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REALM_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_realm_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>flags</strong>: <code>lpa2</code> (LPA2 enabled), <code>sve</code> (SVE enabled), <code>pmu</code> (PMU enabled)</li> <li><strong>s2sz</strong>: requested IPA width</li> <li><strong>sve_vl</strong>: requested SVE vector length</li> <li><strong>num_bps</strong>: number of breakpoints</li> <li><strong>num_wps</strong>: number of watchpoints</li> <li><strong>pmu_num_ctrs</strong>: requested number of PMU counters</li> <li><strong>hash_algo</strong>: algorithm used to measure the initial state of the Realm</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Create a REC</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>A REC is the execution context associated with a Realm vCPU.</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REC_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_rec_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>gprs</strong> (general-purpose registers): the host supplies the initial values of registers 0–7; the remaining registers are zeroed</li> <li><strong>pc</strong> (program counter): the primary REC starts at the Realm's <code>LOADER_START_ADDR</code>; the PCs of other RECs are zeroed</li> <li><strong>flags</strong>: includes <code>runnable</code>, which indicates whether a REC may execute; the primary REC is runnable</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Create a Data Granule</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>Copies content such as the kernel or initramfs from a Non-secure Granule supplied by the caller.</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_DATA_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_data_granule_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>ipa</strong>: IPA at which the DATA Granule is mapped in the Realm</li> <li><strong>flags</strong>: indicates whether to measure the DATA Granule's contents</li> <li><strong>content</strong>: the hash of the DATA Granule's contents, or zero when the flags mark those contents as unmeasured</li> </ul> </td> </tr> </tbody> </table> </div> <h4 id="virtual-firmware-boot">Virtual Firmware Boot</h4> <p>When virtCCA added support for UEFI Boot, Arm CCA had not yet defined a measurement specification for this path. virtCCA therefore designed its own scheme, drawing on the trusted-boot mechanisms used by TCG and TDX. UEFI Boot is harder to measure than Direct Kernel Boot because its boot media and configuration can vary. The scheme has to preserve evidence of those choices without assuming a single fixed sequence.</p> <p>🍵 For technical details on Intel TDX’s Trusted Boot, see this companion article: <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a>.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 4. Measurement in UEFI Boot </div> <p>The possible UEFI boot paths are numerous, but the inputs that determine their security state fall into a smaller set. virtCCA measures boot variables, image files, boot order, and other security-sensitive inputs, then records the measurements in an Event Log. This gives the verifier a reproducible account of the path that was actually taken rather than requiring every valid path to be enumerated in advance.</p> <p>Before the kernel is loaded, UEFI and the bootloader measure critical state through <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> and extend the results into the REMs. The protocol records the same events in the Confidential Computing Event Log (CCEL), exposed through an ACPI table. A verifier can replay that log, reconstruct the REM values, and compare them with the values in the attestation token. Matching values show that the log corresponds to the measured boot sequence; policy evaluation still determines whether that sequence is acceptable.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 5. Measured Boot Components in EDK2 </div> <p>Figure 5 shows the components that implement measurement in virtCCA’s UEFI firmware. <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> provides the hashing and event-log interface for a confidential-computing guest. Following the <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html">UEFI Specification 2.10</a>, the implementation maps the conventional uses of TPM PCRs to virtCCA REMs, as summarized below. The <code class="language-plaintext highlighter-rouge">CcaTcg2Dxe.c</code> driver handles DXE-phase measurements, while <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootLib</code> measures PE images and the GPT partition table. The implementation is carried by patches 97–106 (“Support measurement when UEFI Boot CVM”) in <a href="https://atomgit.com/src-openeuler/edk2/blob/openEuler-24.03-LTS-SP2/edk2.spec">src-openeuler/edk2</a>. The Measured Boot Flow section of <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a> describes the analogous flow in more detail.</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TPM PCR</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TDX</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">virtCCA/CCA</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Typical Usage of Measurement Registers</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">MRTD</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RIM</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Virtual firmware code (corresponds to PCR[0])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">1, 7</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[0]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Firmware configuration (corresponds to PCR[1,7])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">2~6</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[1]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM1</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Components loaded by firmware, such as the OS loader (corresponds to PCR[4,5])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">8~15</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[2]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM2</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">OS components such as the kernel, initrd, and applications (corresponds to PCR[8–15]); exact usage depends on the OS</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">16+</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[3]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM3</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Reserved for special-purpose use</td> </tr> </tbody> </table> </div> <h2 id="attestation">Attestation</h2> <p>virtCCA uses delegated attestation: the platform and the CVM each produce part of the evidence instead of relying on one component to report on the entire stack. As Figure 2 shows, a virtCCA Token contains two sub-tokens, the Platform Token and the CVM Token. The HSM in the InSE generates the Platform Token, which the TMM caches in secure memory. The TMM generates the CVM Token and packages the two together.</p> <h3 id="authenticity">Authenticity</h3> <p>Each component signs its own sub-token with a dedicated key. The keys form a derivation chain: the component responsible for the preceding sub-token derives the key used for the next one. The sub-tokens must also be bound to each other; otherwise, an attacker could replace one with evidence from another system. virtCCA prevents this by placing the hash of the successor’s public key in the predecessor sub-token’s Challenge field.</p> <p>Two keys matter here. The CCA Platform Attestation Key (CPAK) signs the Platform Token. The HSM derives it from a root key and platform parameters, and Huawei’s Root CA and subordinate CA issue the corresponding certificate. The Realm Attestation Key (RAK) signs the CVM Token. During TMM initialization, the HSM derives the RAK from the root key, the virtCCA platform measurement, and other parameters.</p> <p>🍵 The virtCCA TMM is closed source, so its exact interaction with the HSM is not publicly visible. The <a href="https://trustedfirmware-m.readthedocs.io/projects/tf-m-extras/en/latest/partitions/delegated_attestation/delegated_attest_integration_guide.html">Delegated Attestation Service Integration Guide</a> describes the corresponding integration pattern for obtaining a RAK and Platform Token.</p> <h3 id="attestation-flow">Attestation Flow</h3> <p>The <a href="https://gitcode.com/openeuler/virtCCA_sdk">virtCCA SDK</a> provides the user-space interfaces needed to integrate remote attestation (Figure 6). Its <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/samples">Attestation Demo</a> shows the complete exchange. A server inside the CVM obtains the device certificate and virtCCA Token through the TSI interface and returns them to a client. The client is the Local Verifier: it parses the evidence, validates the certificate chain and token signatures, compares the measurements with reference values, and applies its appraisal policy.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 6. Component Details in virtCCA SDK </div> <p>🍵 The SDK repository also contains examples for <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/full-disk-encryption">Full Disk Encryption</a> and virtCCA’s integration with <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/rats-tls">RATS-TLS</a>.</p> <p>The sequence below follows the Attestation Demo. For deployment instructions, see <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_16_0036.html">Enabling Remote Attestation</a>.</p> <div id="mermaid-target"></div> <script src="https://cdn.jsdelivr.net/npm/mermaid@11.12.2/dist/mermaid.min.js"></script> <script>document.addEventListener("DOMContentLoaded",function(){mermaid.initialize({startOnLoad:!1});const e="\nsequenceDiagram\n    autonumber\n    \n    participant C as Verifier (Client)\n    participant S as Attester (Server)\n    participant SDK as TSI SDK / Driver\n\n    Note over S, SDK: [ TEE / Confidential VM Boundary ]\n\n    rect rgb(240, 244, 255)\n        Note over C, SDK: Phase 1: Evidence & Identity Collection\n        C->>S: Request Identity Certificate (CPAK)\n        S->>SDK: get_dev_cert()\n        SDK-->>S: Return CPAK Certificate\n        S-->>C: Deliver CPAK Certificate\n\n        C->>C: Generate Fresh Nonce (Challenge)\n\n        C->>S: Request Attestation Evidence (virtCCA Token)\n        S->>SDK: get_attestation_token(nonce)\n        SDK-->>S: Return Signed Evidence\n        S-->>C: Deliver Signed Evidence\n\n        alt UEFI Boot\n            C->>S: Request Event Log (CCEL)\n            S-->>C: Send CCEL Data\n        end\n    end\n\n    rect rgb(255, 251, 235)\n        Note over C: Phase 2: Appraisal & Verification\n        Note right of C: Input: Reference Values, Endorsements, Appraisal Policy\n\n        C->>C: Verify Token Signature based on Endorsements\n        C->>C: Verify Token Claims based on Reference Values\n\n        alt Direct Kernel Boot\n            Note right of C: Match RIM against Reference Values\n        else UEFI Boot\n            Note right of C: Parse & Replay CCEL<br/>Compare Token REMs with Replayed REMs<br/>Extract & Verify Firmware States\n        end\n        \n        C->>C: Apply Appraisal Policy for Final Attestation Result\n        C->>S: Attestation Result (Standardized Claims regarding Trustworthiness)\n    end\n    ",n=document.getElementById("mermaid-target");mermaid.render("graph-id-1",e).then(({svg:e})=>{n.innerHTML=e})["catch"](e=>{console.error("Mermaid rendering failed:",e),n.innerHTML="<p style='color:red;'>Mermaid diagram failed to render \u2014 please check the console.</p>"})});</script> <h2 id="summary">Summary</h2> <p>Measured boot and remote attestation are often presented as a neat story about making trust visible. The engineering is less neat. It stretches from the chip through firmware and the OS to applications and infrastructure, while standards and the surrounding ecosystem continue to evolve. After all that work, the visible result is simply evidence that a verifier can compare with reference values and policy. It may not look exciting, but producing trustworthy evidence across the whole stack takes a great deal of work. 🥲😅</p> <p>virtCCA has implemented part of that path, though there is still plenty to improve. Keep going, CCA folks.</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[How Huawei virtCCA measures its platform and confidential VMs for remote attestation.]]></summary></entry><entry xml:lang="zh"><title type="html">virtCCA: Measured Boot and Attestation Demystified</title><link href="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/zh/" rel="alternate" type="text/html" title="virtCCA: Measured Boot and Attestation Demystified"/><published>2026-01-21T00:00:00+00:00</published><updated>2026-01-21T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/virtcca-measured-boot-attestation.zh</id><content type="html" xml:base="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/zh/"><![CDATA[<p>💭 碎碎念：关于 virtCCA 的度量启动和远程证明，其特性随商业化进程分阶段落地，导致相关代码散落在各个组件中，文档也比较碎片化。趁此机会，本文将从架构层面进行统一的梳理与分析，希望能帮大家理清脉络，省去翻阅零散旧资料的周折。</p> <h2 id="introduction">Introduction</h2> <p>Huawei virtCCA 是基于鲲鹏处理器 TrustZone 与 Secure EL2 特性打造的机密计算方案。它在现有硬件上先行实践了 ARM CCA 架构理念，通过构建“基于硬件且可验证”的可行执行环境（TEE），为运行中的应用和数据提供硬件隔离，防止任何未经授权的访问或篡改。</p> <p>作为向原生 CCA 硬件平滑演进的桥梁，virtCCA 实现了业务应用的零改造迁移。它在 CCA 硬件大规模普及前，为企业提供了兼具高性能、高安全性与前瞻兼容性的实践方案。详见 <a href="https://arxiv.org/abs/2306.11011">virtCCA: Virtualized Arm Confidential Compute Architecture with TrustZone</a> 与 <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_19_0003.html">机密计算TEE套件技术白皮书</a>。</p> <p>如图 1 所示，硬件隔离机制界定了信任边界，度量启动（Measured Boot）与远程证明（Attestation）通过密码学方式赋予该边界可验证的状态可信：</p> <ul> <li> <p>度量启动：遵循“度量先于执行”的原则，在 virtCCA 平台和机密虚拟机（CVM） 的启动过程中，通过哈希度量记录 TCB 的完整性状态，并将度量结果存储到不可篡改寄存器中。</p> </li> <li> <p>远程证明：利用硬件保护的认证密钥（Attestation Key）对度量结果进行数字签名，生成具有证据效力的证明报告（Attestation Report），证明 CVM 及底层平台是真实、安全且未被篡改的。</p> </li> </ul> <p>两者协同配合，构建了从底层硬件信任根（RoT）到上层 CVM 的完整信任链，这使得依赖方（Relying Party）能够远程确信：目标负载运行在真实 virtCCA 平台的 CVM 内，且符合预期安全基准，从而满足机密计算的信任验证需求。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 1. Measured Boot and Attestation </div> <p>💡 TCB (Trusted Computing Base) ：为了维持系统安全性而必须被信任的所有硬件、固件及软件组件的集合。这既包含 CPU、RoT 等硬件和 TF-A、TMM 等固件，也涵盖 Guest OS 侧的关键代码，如 UEFI、Bootloader（Grub）以及操作系统内核（Kernel）。如果 TCB 中的任何一个组件出现了漏洞或被恶意篡改，整个系统的安全性（机密性、完整性）都将无法得到保证。</p> <h2 id="measured-boot">Measured Boot</h2> <p>virtCCA 架构由 CVM 及其底层支撑环境 virtCCA 平台（硬件与固件集合）共同构成，这在逻辑上映射了 Arm CCA 规范中 Realm 与 CCA Platform 的实体关系（参考 <a href="https://developer.arm.com/documentation/DEN0096/latest">Arm CCA Security Model 1.0</a> ）。基于这种实体解耦，完整的度量启动由相互衔接的平台度量和 CVM 度量组成，如图 2 所示，也只有当平台度量通过验证，其承载的 CVM 度量才具备实质性的安全意义。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 2. Platform Measurement and CVM Measurement </div> <h3 id="platform-measurement">Platform Measurement</h3> <p>平台度量遵循“链式信任传递”原则，在硬件初始化阶段，BSBC 充当了核心度量信任根 (CRTM) ，运行 HSM 的 InSE 则作为存储信任根（RTS）和报告信任根（RTR），确保度量结果存储于 InSE 内部受保护的 SRAM 中，如图 2 所示。在流程设计上，virtCCA 对标 Arm CCA 的固件引导规范（即 Trusted Subsystems → Application PE → Monitor → RMM）。但在具体的工程实现上，受限于现有鲲鹏硬件的实现架构，相关功能组件的划分与 Arm CCA 存在差异，如下表所示。</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Measured Components</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Descriptions</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>BSBC</strong> (BootROM Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">内置于芯片内 ROM 的安全启动代码</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ESBC</strong> (External Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">存储在芯片外非易失性存储的安全启动代码</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>InSE</strong> (Integrated Secure Element)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">芯片内置的安全子系统，具备物理安全防护能力</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>HSM</strong> (Hardware Security Module)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">运行在 InSE 上的高安固件，提供密钥管理、安全存储和 Platform token 生成等功能</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMU</strong> (Intelligence Management Unit)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">智能管理单元，用于硬件管理、监控和安全启动</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IPU</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ATF</strong> (Arm Trusted Firmware)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">运行在最高特权级 (EL3) 的底层安全固件</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>TMM</strong> (TrustZone Management Monitor)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">轻量安全监控组件，负责 CVM 的生命周期、内存隔离和接口调用</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>UEFI</strong> (Unified Extensible Firmware Interface)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">平台启动固件，负责初始化硬件、执行安全校验并引导系统</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMF AP</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> </tbody> </table> </div> <p>💡 RoTs 是必须被信任的系统组件，因为无法检测它的非预期行为：（1）度量信任根（RTM）计算软件的度量值，并将度量值发送给 RTS；（2）存储信任根是具备访问控制且防篡改的安全存储；（3）报告信任根上报 RTS 的记录内容，e.g., 度量值，呈现形式是证明报告；（4）CRTM 是系统复位后执行的初始代码，构成信任链的起点。</p> <h3 id="cvm-measurement">CVM Measurement</h3> <p>针对不同的业务场景，virtCCA 为 CVM 提供了两类启动方式：直接内核启动（Direct Kernel Boot）和虚拟固件启动（Virtual Firmware Boot），如图 3 所示。</p> <ul> <li>直接内核启动：由 Hypervisor（QEMU/KVM）直接加载操作系统内核（kernel）和初始内存文件系统（initramfs），跳过虚拟固件和启动引导程序。</li> <li>虚拟固件启动：通过虚拟固件（UEFI）初始化硬件，加载启动引导程序（bootloader）如 GRUB，再由 bootloader 加载 kernel 和 initramfs，后文统称为 UEFI 启动。</li> </ul> <p>直接内核启动的优势是启动速度快、资源占用小，适用于轻量化容器环境（如 Kata Container）。UEFI 启动的优势是兼容现有 OS 镜像，支持多内核、多引导管理等复杂启动项，能保持与普通虚拟机一致的云管服务，适用于通用型虚拟机（如 IaaS 云实例）。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 3. Direct Kernel Boot and Firmware Boot </div> <p>尽管引导路径各异，其底层均遵循统一的安全原语：Host-provided assets are untrusted。任何源自 Hypervisor 侧的输入（包括代码、数据及配置）在被执行或使用前，必须由 TMM 或其委托的 RTM 完成强制性度量。这些度量结果最终被扩展至受 TMM 保护的特定寄存器中。参考 <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a>（A7.1 Realm measurements）的定义，上述寄存器包括 RIM（Realm Initial Measurement）和 REM（Realm Event Measurement），其中 RIM 由 TMM 直接计算，记录初始的寄存器和内存状态，而 REM 则由后续 RTM 计算，例如 UEFI 度量加载的 GRUB。</p> <h4 id="direct-kernel-boot">Direct Kernel Boot</h4> <p>采用直接内核启动拉起 CVM 时，virtCCA 的度量逻辑遵循 Arm CCA 的 RMM 规范，由于 virtCCA 的 TMM 是闭源代码，本节将沿用 RMM 语义描述度量流程，下表给出了度量流程的三个阶段，细节可以参考 <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a> 的 B4.3.9.4 RMI_REALM_CREATE initialization of RIM、B4.3.12.4 RMI_REC_CREATE extension of RIM 和 B4.3.1.4 RMI_DATA_CREATE extension of RIM。</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px; width: 20%;">Phase</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Description</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a Realm</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>CCA Realm &lt;-&gt; virtCCA CVM</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REALM_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_realm_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>flags</strong>: <code>lpa2</code> (LPA2 enabled), <code>sve</code> (SVE enabled), <code>pmu</code> (PMU enabled)</li> <li><strong>s2sz</strong>: requested IPA width</li> <li><strong>sve_vl</strong>: requested SVE vector length</li> <li><strong>num_bps</strong>: number of breakpoints</li> <li><strong>num_wps</strong>: number of watchpoints</li> <li><strong>pmu_num_ctrs</strong>: requested number of PMU counters</li> <li><strong>hash_algo</strong>: algorithm used to measure the initial state of the Realm</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a REC</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>REC is a execution context which is associated with Realm vCPU</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REC_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_rec_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>gprs</strong> (general-purpose registers): host provides the initial vaule of registers 0-7, others are zeros</li> <li><strong>pc</strong> (program counter): the pc of primary REC is set to <code>LOADER_START_ADDR</code> of Realm, others are zeros</li> <li><strong>flags</strong>: <code>runnable</code>, whether REC is eligible for execution, primary REC is set to eligible</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a Data Granule</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>Copies contents (e.g., kernel, initramfs) from a Non-secure Granule provided by the caller</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_DATA_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_data_granule_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>ipa</strong>: IPA at which the DATA Granule is mapped in the Realm</li> <li><strong>flags</strong>: whether to measure DATA Granule contents</li> <li><strong>content</strong>: hash of contents of DATA Granule, or zero if flags indicate DATA Granule contents are unmeasured</li> </ul> </td> </tr> </tbody> </table> </div> <h4 id="virtual-firmware-boot">Virtual Firmware Boot</h4> <p>针对基于 UEFI 固件启动的 CVM 场景，鉴于彼时 Arm CCA 的度量规范仍尚未定义，virtCCA 提前进行了技术预研与方案落地。不同于流程相对固定的直接内核启动，UEFI 启动链条涉及多样的启动介质与动态配置，其路径表现出较强的非确定性。virtCCA 参考了 TCG 与 TDX 的可信启动机制，构建了一套可确定、可度量、可审计的引导链路。</p> <p>🍵 关于 Intel TDX 的 Trusted Boot 技术细节，可参阅另外一篇文章 <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a>。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 4. Measurement in UEFI Boot </div> <p>如图 4 所示，尽管 UEFI 的启动组合路径繁杂，但影响系统安全状态的关键因素相对固定。virtCCA 将启动变量、镜像文件以及启动顺序等所有安全敏感状态进行统一度量，通过构建可复现、可验证的事件日志（Event Log），来覆盖任意的 UEFI 启动组合。</p> <p>具体而言，在内核载入前的预启动环境中（跨越 UEFI 与 bootloader 阶段），所有关键状态均通过 EFI_CC_MEASUREMENT_PROTOCOL 进行度量，并扩展到 REM 中。同时，该协议将每次度量操作记录至 ACPI 表中的 CCEL（Confidential Computing Event Log）。这种设计支持 CVM 通过日志重放机制验证 REM 的真实性，从而确保了整条信任链的端到端可审计。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 5. Measured Boot Components in EDK2 </div> <p>virtCCA 在 UEFI 固件中的度量组件如图 5 所示。其中，EFI_CC_MEASUREMENT_PROTOCOL 封装了机密计算环境下的底层度量逻辑。遵循 <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html">UEFI Specification 2.10</a> 规范，该实例提供哈希计算和事件日志的能力，并确立了从传统的 TPM PCR 到 virtCCA REM 的语义映射（见下表）。CcaTcg2Dxe.c 驱动负责处理 DXE 阶段的度量事务，DxeTpm2MeasureBootLib 库则负责 PE 镜像及 GPT 分区表的完整性度量，代码可参阅 <a href="https://atomgit.com/src-openeuler/edk2/blob/openEuler-24.03-LTS-SP2/edk2.spec">src-openeuler/edk2</a> 的 patch97-patch106（Support measurement when UEFI Boot CVM），流程可参考 <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a> 的 Measured Boot Flow 章节。</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TPM PCR</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TDX</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">virtCCA/CCA</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Typical Usage of Measurement Registers</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">MRTD</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RIM</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Virtual firmware code (match PCR[0])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">1, 7</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[0]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Firmware configuration (match PCR[1,7])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">2~6</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[1]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM1</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Firmware loaded component, such as OS loader (match PCR[4,5])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">8~15</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[2]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM2</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">OS component, such as OS kernel, initrd, and application (match PCR[8~15]), the usage is OS dependent</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">16-...</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[3]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM3</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Reserved for special usage only</td> </tr> </tbody> </table> </div> <h2 id="attestation">Attestation</h2> <p>在 Delegated Attestation 模型下，证明报告的生成职责由多个参与方协同承担。如图 2 所示，virtCCA Token 封装了两个 Sub-Token（CVM Token 与 Platform Token）。其中，Platform Token 由 InSE 的 HSM 生成，出于性能考虑会缓存到 TMM 的安全内存；TMM 则在生成 CVM Token 基础上组合两者生成 virtCCA Token。</p> <h3 id="authenticity">Authenticity</h3> <p>为了确保信任链的可追溯性，每个 Sub-Token 均由对应生成实体使用专用密钥进行签名。这些密钥遵循链式派生逻辑：链中的每个密钥都由其前序 Sub-Token 的生成实体派生。为防止 Sub-Token 替换攻击，各个 Sub-Token 通过“哈希锚定”机制实现密码学绑定，在前序 Sub-Token 的 Challenge 中嵌入后序 Sub-Token 的公钥哈希。</p> <p>具体到 virtCCA 的密钥体系，CPAK 是 Platform Token 的签名密钥，由 HSM 基于根密钥及平台参数派生，其公钥证书由 Huawei Root CA 及 Sub CA 签发背书，提供了硬件级身份证明；RAK 是 CVM Token 的签名密钥，派生于 TMM 初始化阶段，由 HSM 结合根密钥和 virtCCA 平台度量值等参数生成。</p> <p>🍵 鉴于 virtCCA TMM 属于闭源组件，关于其与 HSM 交互以获取 RAK 和 Platform Token 的流程细节，可参阅 <a href="https://trustedfirmware-m.readthedocs.io/projects/tf-m-extras/en/latest/partitions/delegated_attestation/delegated_attest_integration_guide.html">Delegated Attestation Service Integration Guide</a>。</p> <h3 id="attestation-flow">Attestation Flow</h3> <p>virtCCA 提供了用户态工具包 <a href="https://gitcode.com/openeuler/virtCCA_sdk">virtCCA SDK</a>，用于简化远程证明的使能和集成，如图 6 所示。其配套的 <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/samples">Attestation Demo</a> 展示了端到端的证明逻辑：Server 端在 CVM 运行时环境里，利用 TSI 接口向上透传设备证书（CPAK 证书）与证明报告（virtCCA Token）。Client 端则充当 Local Verifier 角色，解析收到的证书与报告，进行证书链校验和安全策略评估。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 6. Component Details in virtCCA SDK </div> <p>🍵 其他特性，例如 Full Disk Encryption 可参阅 <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/full-disk-encryption">attestation/full-disk-encryption</a>；virtCCA 对 <a href="https://github.com/inclavare-containers/rats-tls">RATS-TLS</a> 的适配可参阅 <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/rats-tls">attestation/rats-tls</a>。</p> <p>如下展示了 Attestation Demo 的具体流程，实际部署与使用请参阅 <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_16_0036.html">使能远程证明</a> 的特性指南。</p> <div id="mermaid-target"></div> <script src="https://cdn.jsdelivr.net/npm/mermaid@11.12.2/dist/mermaid.min.js"></script> <script>document.addEventListener("DOMContentLoaded",function(){mermaid.initialize({startOnLoad:!1});const e="\nsequenceDiagram\n    autonumber\n    \n    participant C as Verifier (Client)\n    participant S as Attester (Server)\n    participant SDK as TSI SDK / Driver\n\n    Note over S, SDK: [ TEE / Confidential VM Boundary ]\n\n    rect rgb(240, 244, 255)\n        Note over C, SDK: Phase 1: Evidence & Identity Collection\n        C->>S: Request Identity Certificate (CPAK)\n        S->>SDK: get_dev_cert()\n        SDK-->>S: Return CPAK Certificate\n        S-->>C: Deliver CPAK Certificate\n\n        C->>C: Generate Fresh Nonce (Challenge)\n\n        C->>S: Request Attestation Evidence (virtCCA Token)\n        S->>SDK: get_attestation_token(nonce)\n        SDK-->>S: Return Signed Evidence\n        S-->>C: Deliver Signed Evidence\n\n        alt UEFI Boot\n            C->>S: Request Event Log (CCEL)\n            S-->>C: Send CCEL Data\n        end\n    end\n\n    rect rgb(255, 251, 235)\n        Note over C: Phase 2: Appraisal & Verification\n        Note right of C: Input: Reference Values, Endorsements, Appraisal Policy\n\n        C->>C: Verify Token Signature based on Endorsements\n        C->>C: Verify Token Claims based on Reference Values\n\n        alt Direct Kernel Boot\n            Note right of C: Match RIM against Reference Values\n        else UEFI Boot\n            Note right of C: Parse & Replay CCEL<br/>Compare Token REMs with Replayed REMs<br/>Extract & Verify Firmware States\n        end\n        \n        C->>C: Apply Appraisal Policy for Final Attestation Result\n        C->>S: Attestation Result (Standardized Claims regarding Trustworthiness)\n    end\n    ",n=document.getElementById("mermaid-target");mermaid.render("graph-id-1",e).then(({svg:e})=>{n.innerHTML=e})["catch"](e=>{console.error("Mermaid \u6e32\u67d3\u5931\u8d25:",e),n.innerHTML="<p style='color:red;'>Mermaid \u56fe\u8868\u6e32\u67d3\u5931\u8d25\uff0c\u8bf7\u68c0\u67e5\u63a7\u5236\u53f0\u62a5\u9519\u3002</p>"})});</script> <h2 id="summary">Summary</h2> <p>度量启动和远程证明这玩意儿，大家一直聊得挺热闹，谁都想讲好“看得见的信任”这个故事。但对技术人来说，确实是全方位的折腾：从最底下的芯片、固件、OS 一路磨到应用和基建，中间还得兼顾标准和生态。等真的忙活完一通，有时也说不清价值到底体现在哪，说到底技术本身也没多炫酷，🥲😅。</p> <p>总之，virtCCA 目前在这块做了一些工作，还有很多不完善的地方，大家 CCA 加油吧。</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[An exploration of the measurement and attestation framework within Huawei virtCCA.]]></summary></entry><entry><title type="html">Key Management with a TPM</title><link href="https://mahaocheng.me/blog/2025/tpm-based-security/" rel="alternate" type="text/html" title="Key Management with a TPM"/><published>2025-07-24T00:00:00+00:00</published><updated>2025-07-24T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tpm-based-security</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tpm-based-security/"><![CDATA[<p>A Trusted Platform Module (TPM) does not need to keep every key permanently loaded. TPM 2.0 organizes keys into hierarchies, each rooted in a secret seed from which the TPM can deterministically recreate primary keys. This post focuses on two of those hierarchies: Endorsement and Storage.</p> <table> <thead> <tr> <th>Seed type</th> <th>Key hierarchy</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td>Endorsement Seed</td> <td>Endorsement Key (EK)</td> <td>Identifies the TPM</td> </tr> <tr> <td>Storage Seed</td> <td>Storage Root Key (SRK)</td> <td>Protects keys for local apps</td> </tr> </tbody> </table> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/key_management.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/key_management.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. The Endorsement and Storage hierarchies </div> <p>The <code class="language-plaintext highlighter-rouge">restricted</code> attribute further limits what a TPM key may do:</p> <ul> <li><strong>Restricted keys</strong> operate only on data that has the structure or provenance expected by the TPM. An Attestation Key, for example, signs TPM-generated attestation data, while a restricted decryption key can protect child objects. EKs, SRKs, and Attestation Keys are normally created as restricted keys.</li> <li><strong>Unrestricted keys</strong> can perform general-purpose operations. A TLS client key or a document-signing key would usually belong here.</li> </ul> <h2 id="endorsement-key">Endorsement Key</h2> <p>The Endorsement Key (EK) is a cryptographic identity for the TPM. Its certificate lets another party associate the EK public key with a genuine TPM.</p> <ul> <li><code class="language-plaintext highlighter-rouge">TPM2_CreatePrimary()</code> can reproduce the EK key pair from the Endorsement Primary Seed (EPS) and a known public template. The TPM performs the underlying key derivation internally.</li> <li>An EK certificate is usually issued during manufacturing by the TPM or platform manufacturer and stored in TPM non-volatile memory.</li> <li>The certificate contains the EK public key and may also identify the manufacturer, model, version, and key ID.</li> </ul> <p>For the public-area format, see the TCG’s <a href="https://trustedcomputinggroup.org/wp-content/uploads/TCG_IWG_EKCredentialProfile_v2p3_r2_pub.pdf#page=37">default RSA 2048 EK template</a>.</p> <p>The EK is held in a TPM Shielded Location. Software can read its public key, but the private part must never be exposed outside the TPM.</p> <p>Two related terms describe how a TPM protects data:</p> <ul> <li><strong>Isolated Locations</strong> are protected by access controls at the TPM boundary.</li> <li><strong>Shielded Locations</strong> also protect secret values from exposure or modification while the TPM operates on them.</li> </ul> <h2 id="storage-root-key">Storage Root Key</h2> <p>The Storage hierarchy organizes keys as a tree. Its root is the Storage Root Key (SRK), and keys such as Attestation Keys and application keys can be created beneath it.</p> <p>The entire tree does not have to remain in TPM memory. A child key can be stored outside the TPM as a protected blob; its parent key protects the confidential part and verifies its integrity when the object is loaded again. Following the parent links eventually leads back to the SRK.</p> <p>This storage relationship is not itself a certificate chain. When a remote service needs to connect a new Attestation Key to the TPM identified by an EK, it can use credential activation, described below.</p> <h2 id="attestation-key">Attestation Key</h2> <p>An Attestation Key (AK) signs evidence produced by the TPM. A typical AK has three important properties:</p> <ul> <li><strong>Bound to one TPM</strong>: attributes such as <code class="language-plaintext highlighter-rouge">fixedTPM</code> and <code class="language-plaintext highlighter-rouge">fixedParent</code> prevent the key from being moved to another TPM or parent. When the TPM also generates the sensitive material, the private key is never exposed in plaintext.</li> <li><strong>Restricted to attestation</strong>: rather than signing arbitrary application data, the key signs TPM-generated structures, including reports containing Platform Configuration Register (PCR) values.</li> <li><strong>Usable as a device credential</strong>: a manufacturer or another trusted party can certify the AK public key and use it as part of a device identity (DevID).</li> </ul> <p>IEEE 802.1AR defines a secure device identifier (DevID) as an identifier cryptographically bound to a device.</p> <p>An identity installed during manufacturing is an Initial Device Identifier (IDevID), with the corresponding key referred to here as an IAK. It normally lasts for the lifetime of the device. An identity installed later by an owner or operator is a Locally Significant Device Identifier (LDevID), with a corresponding LAK; it can be replaced or removed when the device is reset or retired.</p> <p>In this context, a credential consists of the private key together with the certificate that binds its public key to the device identity.</p> <p>Attestation Identity Key (AIK) is the TPM 1.2 name for this kind of key. An AIK supports two signing operations:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">TPM_Quote</code></strong> signs a report containing selected PCR values. A remote verifier can use the report as evidence of the device’s measured state.</li> <li><strong><code class="language-plaintext highlighter-rouge">TPM_CertifyKey</code></strong> signs a statement about another key in the TPM hierarchy, including whether that key is non-migratable.</li> </ul> <h2 id="credential-activation">Credential Activation</h2> <p>Suppose a server already trusts an EK certificate and now needs to enroll a newly created IAK. Credential activation checks two facts before the server accepts that key:</p> <ul> <li> <p>The IAK has the attributes required by the server’s policy. For example:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">FlagFixedTPM</span>             <span class="c1">// The key cannot move to another TPM.</span>
<span class="n">FlagSensitiveDataOrigin</span>  <span class="c1">// The TPM generated the key's sensitive material.</span>
</code></pre></div> </div> </li> <li> <p>The IAK resides in the same TPM as the EK.</p> </li> </ul> <p>In the terminology used in Figure 2, the IAK is the activation key and the EK is the anchor key.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. Credential activation flow </div> <ul> <li>The client sends the server the EK public key and the IAK’s public area. The public area contains both the key and its attributes; hashing it produces the IAK Name.</li> <li> <p>The server checks those attributes against its policy, generates a secret, and creates a credential for the IAK. A simplified view is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>encrypted_seed = Encrypt(EK_public, seed)
symmetric_key  = KDF(seed, IAK_Name)
credential     = EncryptAndAuthenticate(symmetric_key, secret, IAK_Name)
challenge      = credential || encrypted_seed
</code></pre></div> </div> <p>Only the TPM holding the EK private key can recover the seed. The IAK Name is part of the key derivation and integrity check, so changing the IAK public key or its attributes produces a different result.</p> </li> <li>The client passes the challenge and handles for both keys to <code class="language-plaintext highlighter-rouge">TPM2_ActivateCredential</code>.</li> <li>The TPM uses the EK to recover the seed, recomputes the keys from the seed and the loaded IAK’s Name, verifies the credential, and returns the secret. Success tells the server that this exact IAK, with the attributes it inspected, is loaded in the same TPM as the EK.</li> </ul> <h2 id="reference">Reference</h2> <ol> <li><a href="https://ericchiang.github.io/post/tpm-keys/">A Practical Guide to TPM 2.0</a></li> <li><a href="https://tpm2-software.github.io/tpm2-tss/getting-started/2019/12/18/Remote-Attestation.html">Remote Attestation with tpm2-tss</a></li> <li><a href="https://github.com/google/go-attestation/blob/master/docs/credential-activation.md">Credential Activation in go-attestation</a></li> <li><a href="https://github.com/tpm2dev/tpm.dev.tutorials">tpm.dev Tutorials</a></li> </ol>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[TPMs are ridiculously complex.]]></summary></entry><entry xml:lang="zh"><title type="html">Key Management in TPM based Security</title><link href="https://mahaocheng.me/blog/2025/tpm-based-security/zh/" rel="alternate" type="text/html" title="Key Management in TPM based Security"/><published>2025-07-24T00:00:00+00:00</published><updated>2025-07-24T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tpm-based-security/tpm-based-security.zh</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tpm-based-security/zh/"><![CDATA[<p>TPM（可信平台模块）本质上并不会直接存储完整的密钥，而是持久化存储称为 Seed（种子）的秘密值，并通过这些 Seed 确定性地派生出各种密钥。两类常见的 Seed 和对应的密钥体系是 Endorsement 和 Stroage，如下所示：</p> <table> <thead> <tr> <th>Seed 类型</th> <th>密钥体系</th> <th>用途简述</th> </tr> </thead> <tbody> <tr> <td>Endorsement Seed</td> <td>Endorsement Key（EK）</td> <td>识别 TPM 的密钥</td> </tr> <tr> <td>Storage Seed</td> <td>Storage Root Key（SRK）</td> <td>本地应用使用的密钥</td> </tr> </tbody> </table> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/key_management.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/key_management.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 1. Endorcement and Storage types in TPM Key Management </div> <p>上述密钥按照用途分为两类：Restricted 和 Non-Restricted。</p> <ul> <li>Restricted：用于签名或者解密 TPM 的状态和数据，例如，签名启动度量值、证明某个密钥位于同一个 TPM，EK、SRK、AIK 都在此类；</li> <li>Non-Restricted：用于一般用途，例如，TLS 客户端的密钥、签名普通文档的密钥，应用密钥（Application Key）属于此类。</li> </ul> <h2 id="endorsement-key">Endorsement Key</h2> <p>Endorsement Key（EK）是 TPM 的身份标识，由 TPM 制造商配置。</p> <ul> <li>EK 公私钥对由已知模板（EK Template）、Endorsement Primary Seed（EPS）和选定的密钥派生函数（KDF）、通过 <code class="language-plaintext highlighter-rouge">TPM2_CreatePrimary()</code> 确定性生成；</li> <li>EK 证书由TPM制造商的证书颁发机构（CA）签发并将其存储到 TPM NVRAM；</li> <li>EK 证书包含EK公钥以及其他字段，例如，TPM 制造商名称、组件型号、组件版本、密钥 ID 等。</li> </ul> <p>EK Template 的具体格式可查阅：<a href="https://trustedcomputinggroup.org/wp-content/uploads/TCG_IWG_EKCredentialProfile_v2p3_r2_pub.pdf#page=37">Default EK Template (TPMT_PUBLIC): RSA 2048 (Storage)</a></p> <p>EK 被存储在 TPM Shielded Location，私钥永远不能暴露，公钥可从 TPM 内读取。</p> <p>关于 TPM 的安全存储有两个概念：</p> <ul> <li>Isolated Locations：安全系统的非易失性存储，具有访问控制能力，不允许来自系统边界外的访问；</li> <li>Shielded Locations：具有防篡改能力的 Shielded Location，通常用于存储秘密值；</li> </ul> <h2 id="storage-root-key">Storage Root Key</h2> <p>在 TPM 的 Storage 体系结构中，密钥的创建遵循树状层级结构：</p> <ul> <li>比如 Attestation Key（AK）或应用密钥，都是在 TPM 的存储体系下生成的；</li> <li>SRK（Storage Root Key）是这棵树的根密钥，用于保护子密钥；</li> <li>子密钥不必一直保存在 TPM 内部。它可以作为受保护的 Blob 存放在外部，重新加载时由父密钥解密并验证完整性。沿着父子关系向上追溯，最终会回到 SRK。</li> </ul> <p>这种父子关系描述的是密钥的存储和保护方式，本身并不是一条证书签名链。如果远端服务需要确认某个新的 AK 与 EK 位于同一个 TPM 中，可以使用下文介绍的 Credential Activation。</p> <h2 id="attestation-key">Attestation Key</h2> <p>Attestation Key（AK）是一种用于对 TPM 内部数据进行签名的密钥，具有以下关键特性：</p> <ul> <li>non-duplicable（不可复制）：AK 是由 TPM 内部直接生成的，并且密钥材料不会暴露给 TPM 外部，这确保了其私钥无法被导出或复制，增强了安全性。</li> <li>Restricted（受限用途）：AK 只能用于对 TPM 生成的数据进行签名或解密，例如 PCR（平台配置寄存器）值。它不能用于任意数据的签名或加密，以防止其被滥用为通用密钥。</li> <li>签名密钥，用于设备身份（DevID）：AK 的公钥证书可以作为设备的身份凭据，由制造商或受信任方签发。</li> </ul> <p><em>“The 802.1AR standard defines a secure device identifier (DevID) as “an identifier that is cryptographically bound to a device”.”</em></p> <p>设备制造商在制造阶段创建的设备身份称为 IDevID/IAK，IDevID “Credential” 在设备整个生命周期内使用。用户在部署/应用阶段创建的设备身份称为 LDevID/LAK，LDevID “Credential” 会在设备复位或寿命结束时被移除。</p> <p><em>“Credential is a combination of a private key and a certificate.”</em></p> <p>Attestation Identity Key（AIK）是 TPM 1.2 中的一种密钥类型，AIK 仅被允许执行两种基于签名的操作：</p> <ul> <li>TPM_Quote：使用 AIK 对 PCR 寄存器值进行签名，生成代表设备身份和当前状态的硬件报告，常用于远程证明场景，也就是向远端证明设备当前的可信状态。</li> <li>TPM_CertifyKey：生成一个签名声明，用以证明另一个密钥（不是 AIK 本身）位于 TPM 的密钥存储体系中，且不可迁移（non-migratable）。显然，所认证的那个密钥必须实际具备这些属性。</li> </ul> <h2 id="credential-activation">Credential Activation</h2> <p>Credential activation 能够远程地建立对新密钥（例如，IAK）的信任，该密钥随后可以代表设备进行加密或证明，需要证明如下两点：</p> <ul> <li> <p>IAK（Identity Attestation Key）具备预期的密钥属性，例如：</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">FlagFixedTPM</span>             <span class="c1">// 密钥不能被导出，必须留在 TPM 中；</span>
<span class="n">FlagSensitiveDataOrigin</span>  <span class="c1">// 密钥必须由 TPM 内部生成，而不是导入；</span>
</code></pre></div> </div> </li> <li> <p>IAK 确实与 EK 存在于相同的 TPM 中。</p> </li> </ul> <p><em>Credential activation allows a remote party to verify one key is on the same TPM as another key, and that the key has a specific set of properties.</em></p> <p>大概的流程如下：Activation key 代指 IAK，Anchor key 代指 EK。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 2. The Flow of Credential Activation </div> <ul> <li>Client 将 EK 公钥和 IAK 的 Public Area 发送给 Server。Public Area 同时包含公钥和密钥属性；对其计算哈希可以得到 IAK Name；</li> <li> <p>Server 验证 IAK 的属性是否满足安全策略，生成秘密值和随机 Seed，然后构造 Challenge 并返回给 Client。可以将其简化为：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>encrypted_seed = Encrypt(EK_public, seed)
symmetric_key  = KDF(seed, IAK_Name)
credential     = EncryptAndAuthenticate(symmetric_key, secret, IAK_Name)
challenge      = credential || encrypted_seed
</code></pre></div> </div> <p>只有持有 EK 私钥的 TPM 才能恢复 Seed。IAK Name 同时参与密钥派生和完整性校验，因此，只要 IAK 公钥或密钥属性发生变化，TPM 的计算结果就无法匹配 Challenge。</p> </li> <li>Client 将 Challenge 以及 EK、IAK 的句柄发送给 TPM，调用 <code class="language-plaintext highlighter-rouge">TPM2_ActivateCredential</code> 命令；</li> <li>TPM 使用 EK 恢复 Seed，再根据 Seed 和当前加载的 IAK Name 重新派生密钥，验证 Credential 并返回秘密值。成功返回意味着：这个具有指定公钥和属性的 IAK 与 EK 位于同一个 TPM 中。</li> </ul> <h2 id="reference">Reference</h2> <ol> <li><a href="https://ericchiang.github.io/post/tpm-keys/">A Practical Guide to TPM 2.0</a></li> <li><a href="https://tpm2-software.github.io/tpm2-tss/getting-started/2019/12/18/Remote-Attestation.html">Remote Attestation with tpm2-tss</a></li> <li><a href="https://github.com/google/go-attestation/blob/master/docs/credential-activation.md">Credential Activation in go-attestation</a></li> <li><a href="https://github.com/tpm2dev/tpm.dev.tutorials">tpm.dev Tutorials</a></li> </ol>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[TPMs are ridiculously complex.]]></summary></entry><entry><title type="html">Intel TDX: Measured Boot and Remote Attestation with GRUB</title><link href="https://mahaocheng.me/blog/2025/tdx-measure-boot/" rel="alternate" type="text/html" title="Intel TDX: Measured Boot and Remote Attestation with GRUB"/><published>2025-01-12T00:00:00+00:00</published><updated>2025-01-12T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tdx-measure-boot</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tdx-measure-boot/"><![CDATA[<p>Intel Trust Domain Extensions (TDX) places a virtual machine in a hardware-isolated Trust Domain (TD). The TD’s memory and execution state are not directly exposed to the virtual machine manager (VMM), hypervisor, or other non-TD software on the host. The cloud service provider still launches and schedules the VM, but control of the host software stack no longer puts that software in the TD’s trusted computing base (TCB) by default.</p> <p>Isolation answers whether the host can directly inspect or modify a TD. It does not answer what the TD booted. Measured boot and remote attestation address the second question together:</p> <ol> <li>Firmware, boot configuration, GRUB, the kernel, and the initrd are hashed as they are executed or loaded.</li> <li>Each digest is extended, in order, into the Trust Domain Measurement Register (MRTD) or a Runtime Measurement Register (RTMR), and a corresponding event is logged.</li> <li>A TDX Quote provides cryptographic evidence for TD state that includes these measurements.</li> <li>A verifier validates the Quote, replays the event log, and decides whether the observed boot is acceptable under its reference values and policy.</li> </ol> <p>The UEFI environment inside a TD is commonly provided by TDX Virtual Firmware (TDVF), an EDK2/OVMF-based firmware that establishes the TD firmware environment and loads later components. This article is a mechanism explainer for the GRUB boot path: first, how TDVF hands the measurement chain to GRUB; then, how a verifier connects the Quote, event log, and reference values. The TCB ultimately depends on verification policy. The CPU and TDX Module/SEAM firmware form the platform foundation, while trust in guest components such as TDVF, GRUB, and the kernel depends on the reference values accepted by the verifier. The two TDVF configurations and their different threat models are described in the <a href="#jumpA">appendix</a>.</p> <h2 id="td-boot-types">TD Boot Types</h2> <p>Start with the two common boot paths shown below.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. TD Guest Boot Process </div> <p><strong>Direct boot</strong> has no intermediate bootloader such as GRUB. Firmware obtains the kernel, initrd, and command line through QEMU’s <code class="language-plaintext highlighter-rouge">fw_cfg</code> interface and starts the kernel directly.</p> <p><strong>GRUB boot</strong> has firmware load a UEFI bootloader from a disk image. GRUB then reads its configuration, selects a kernel, and loads the initrd. This path introduces another software and configuration layer, so it requires a longer measurement chain.</p> <p>Figure 2 expands both paths. This article covers only the GRUB path on the right.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. Detailed Flow for Different TD Boot </div> <p>The chain has three handoff points. TDVF measures its configuration and external inputs. The UEFI image-loading path measures PE/COFF images such as shim and GRUB. GRUB then measures its configuration and commands, the kernel, and the initrd. When UEFI Secure Boot is enabled, shim can also extend the distribution’s authorization chain to GRUB and the kernel. Secure Boot and measured boot serve different purposes: Secure Boot blocks unauthorized code, while measured boot records what was actually loaded.</p> <p>The event types and PCR semantics come from the TCG measured-boot model. TDX maps those events to MRTD and the RTMRs and includes hardware and TDX Module state in the platform evidence protected by the Quote. The TDVF configuration still determines whether the VMM is actually outside the TCB; using TDX alone is not enough to make that claim.</p> <h2 id="uefi-boot-sequence">UEFI Boot Sequence</h2> <p>GRUB runs as a UEFI application on this path. Figure 3 shows the UEFI boot sequence and the boundary between firmware and the loader.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 3. UEFI Booting Sequence </div> <p>Firmware uses Boot Services (BS) to load and start the selected OS loader. GRUB and other loaders run as UEFI applications and call those services. Once the kernel is ready, the loader calls <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> to terminate BS and release their resources; only UEFI Runtime Services (RT) remain available to the OS. <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> is therefore both an execution boundary and a key event-log transition between the pre-boot and post-boot environments.</p> <p>A detailed introduction to UEFI boot phases refers to <a href="#jumpB">Appendix: UEFI Boot Phase</a> from <a href="https://secret.club/2020/05/26/introduction-to-uefi-part-1.html#uefi-boot-phases">UEFI boot phases</a>.</p> <h2 id="measured-boot-component">Measured Boot Component</h2> <p>As Figure 4 shows, the pre-boot environment consists of the TDVF/OVMF phase and the bootloader phase, which may include shim and GRUB. UEFI components use <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> to log events and extend their digests into Runtime Measurement Registers (RTMRs). MRTD records the initial construction measurement of the TD.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 4. TD Measurement Process </div> <p><code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> follows the TCG event-log model. The Confidential Computing Event Log (CCEL) ACPI table describes the memory region containing the events, and each event digest is extended as <code class="language-plaintext highlighter-rouge">MR_new = Hash(MR_old || event_digest)</code>. The event log is not itself a root of trust. A verifier must replay it and compare the result with the RTMR values protected by the Quote to detect missing or modified events.</p> <p>The Measured Boot Component in EDK2 is as follows.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 5. Measured Boot Component in EDK2 </div> <p><a href="https://github.com/tianocore/edk2/tree/master/OvmfPkg/IntelTdx/TdxHelperLib"><code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code></a> provides measurement functions during SEC. <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Protocol/CcMeasurement.h"><code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code></a> abstracts confidential-computing measurement operations for UEFI guests. <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/Tcg/TdTcg2Dxe/TdTcg2Dxe.c">TdTcg2Dxe.c</a> implements the DXE driver responsible for measurements during DXE. <a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeTpm2MeasureBootLib"><code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootLib</code></a> handles PE image and GPT measurements. <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">UefiTcgPlatform.h</code></a> defines the event types used below.</p> <p>The following interface is defined by the <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html#">Confidential Computing chapter of the UEFI Specification</a>.</p> <p>CC-capable virtual firmware that supports measurement exposes <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> under <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL_GUID</code>. The protocol reports the event log and provides hashing and extension operations.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define EFI_CC_MEASUREMENT_PROTOCOL_GUID  \
  { 0x96751a3d, 0x72f4, 0x41a6, { 0xa7, 0x94, 0xed, 0x5d, 0x0e, 0x67, 0xae, 0x6b }}
</span><span class="k">extern</span> <span class="n">EFI_GUID</span>  <span class="n">gEfiCcMeasurementProtocolGuid</span><span class="p">;</span>

<span class="c1">// Protocol Interface Structure</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">_EFI_CC_MEASUREMENT_PROTOCOL</span> <span class="p">{</span>
  <span class="n">EFI_CC_GET_CAPABILITY</span>          <span class="n">GetCapability</span><span class="p">;</span>
  <span class="n">EFI_CC_GET_EVENT_LOG</span>           <span class="n">GetEventLog</span><span class="p">;</span>
  <span class="n">EFI_CC_HASH_LOG_EXTEND_EVENT</span>   <span class="n">HashLogExtendEvent</span><span class="p">;</span>
  <span class="n">EFI_CC_MAP_PCR_TO_MR_INDEX</span>     <span class="n">MapPcrToMrIndex</span><span class="p">;</span>
<span class="p">}</span> <span class="n">EFI_CC_MEASUREMENT_PROTOCOL</span><span class="p">;</span>
</code></pre></div></div> <p>The interface has four operations:</p> <ul> <li><code class="language-plaintext highlighter-rouge">GetCapability</code> returns protocol capability and state information.</li> <li><code class="language-plaintext highlighter-rouge">GetEventLog</code> returns the location of an event log and its last entry.</li> <li><code class="language-plaintext highlighter-rouge">HashLogExtendEvent</code> extends a measurement and optionally logs the event without exposing a technology-specific CC command.</li> <li><code class="language-plaintext highlighter-rouge">MapPcrToMrIndex</code> maps a TPM PCR index to the corresponding CC measurement-register index.</li> </ul> <h3 id="intel-tdx-register-mapping">Intel TDX Register Mapping</h3> <p>TDX has fewer measurement registers than the TPM PCR model whose event semantics it reuses. The typical mapping is:</p> <table> <thead> <tr> <th>TPM PCR Index</th> <th>Typical Usage of Measurement Registers</th> <th>TDX MR Index</th> </tr> </thead> <tbody> <tr> <td>0</td> <td>FirmwareCode (BFV, including init page table)</td> <td>MRTD</td> </tr> <tr> <td>1</td> <td>FirmwareData (CFV, TD Hob, ACPI Table, Boot Variable)</td> <td>RTMR[0]</td> </tr> <tr> <td>2</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>3</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>4</td> <td>OS loader code</td> <td>RTMR[1]</td> </tr> <tr> <td>5</td> <td>GUID partition table (GPT)</td> <td>RTMR[1]</td> </tr> <tr> <td>6</td> <td>N/A</td> <td>N/A</td> </tr> <tr> <td>7</td> <td>Secure Boot Configuration</td> <td>RTMR[0]</td> </tr> <tr> <td>8~15</td> <td>TD OS measurement</td> <td>RTMR[2]</td> </tr> </tbody> </table> <p><em>Note: RTMR[3] is reserved for specialized uses such as a virtual TPM. It remains available to the guest when no such use claims it.</em></p> <p>The registers are normally divided as follows; see <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">8.1 Measurement Register Usage in TD</a> for details.</p> <ul> <li>MRTD measures TDVF code, corresponding to PCR[0].</li> <li>RTMR[0] measures TDVF configuration, corresponding to PCR[1] and PCR[7], following the TCG Platform Firmware Profile (PFP).</li> <li>RTMR[1] measures components loaded by TDVF, such as the OS loader, corresponding to PCR[4] and PCR[5], also following the TCG PFP.</li> <li>RTMR[2] measures OS components such as the kernel, initrd, and applications, corresponding to PCR[8] through PCR[15]. Its use is defined by the OS.</li> <li>RTMR[3] is reserved for specialized uses.</li> </ul> <h2 id="measured-boot-flow">Measured Boot Flow</h2> <p>The following sections walk through the same path as Figure 4 in event order. TDVF first handles VMM-supplied inputs, UEFI then measures boot policy and the OS loader, and GRUB extends the chain to the kernel and initrd. Steps 0 through 18 correspond to the event nodes in the figure.</p> <h3 id="measuring-the-td-hob-and-configuration-fv">Measuring the TD HOB and Configuration FV</h3> <p><a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">4.2 TD Hand-Off Block (HOB)</a> and <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">3.2 Configuration Firmware Volume (CFV)</a> are supplied by the host VMM, so the TD guest cannot trust them by default. TDVF validates their data structures, measures them, and extends the results into RTMRs. Measurement makes later modification or disagreement visible; it does not turn untrusted input into trusted input. TDVF also creates two <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT_HOB</code> structures carrying the TD HOB list and CFV digests, which the DXE phase uses to create <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT</code> records.</p> <p>The Configuration Firmware Volume contains provisioned, read-only data. It may carry UEFI Secure Boot variables such as PK, KEK, db, and dbx.</p> <p>The VMM uses the TD HOB list to pass information to TDVF. It must contain a PHIT HOB and at least one Resource Descriptor HOB; other HOBs are optional.</p> <ul> <li>The TD HOB must include PHIT HOB as the first HOB. EfiMemoryTop, EfiMemoryBottom, EfiFreeMemoryTop, and EfiFreeMemoryBottom shall be zero.</li> <li>The TD HOB must include at least one Resource Description HOB to declare the physical memory resource.</li> </ul> <p><strong><em>0.</em></strong> <code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code> is the SEC instance of <code class="language-plaintext highlighter-rouge">TdxHelperLib</code>. It provides three TDX measurement functions during SEC:</p> <ul> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureTdHob</code> measures and extends the TD HOB, then stores the result in the work area.</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureCfvImage</code> measures and extends the Configuration FV, then stores the result in the work area.</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperBuildGuidHobForTdxMeasurement</code> builds the GUID HOBs used to carry TDX measurements.</li> </ul> <h3 id="measuring-secure-boot-policy-into-pcr7rtmr0">Measuring Secure Boot Policy into PCR[7]/RTMR[0]</h3> <p><em>1.</em> UEFI Debug Mode.</p> <p>If a platform provides a firmware debugger mode, it measures the string “UEFI Debug Mode” as <code class="language-plaintext highlighter-rouge">EV_EFI_ACTION</code>. <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code> in <code class="language-plaintext highlighter-rouge">TdTcg2Dxe.c</code> implements this rule based on <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/SecurityPkg.dec"><code class="language-plaintext highlighter-rouge">PcdFirmwareDebuggerInitialized</code></a>.</p> <p><em>2.</em> The contents of the <code class="language-plaintext highlighter-rouge">SecureBoot</code> variable.</p> <p><em>3.</em> The contents of the <code class="language-plaintext highlighter-rouge">PK</code> variable.</p> <p><em>4.</em> The contents of the <code class="language-plaintext highlighter-rouge">KEK</code> variable.</p> <p><em>5.</em> The contents of the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> variable (the DB).</p> <p><em>6.</em> The contents of the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE1</code> variable (the DBX).</p> <p><em>7.</em> The contents of the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE2</code> (the DBT) variable, if present and not empty.</p> <p><code class="language-plaintext highlighter-rouge">ReadAndMeasureSecureVariable()</code> in <code class="language-plaintext highlighter-rouge">TdTcg2Dxe.c</code> always measures <code class="language-plaintext highlighter-rouge">SecureBoot</code>, <code class="language-plaintext highlighter-rouge">PK</code>, <code class="language-plaintext highlighter-rouge">KEK</code>, <code class="language-plaintext highlighter-rouge">db</code>, and <code class="language-plaintext highlighter-rouge">dbx</code> as <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code></a>. If a variable is absent, it records a zero-length UEFI variable entry. <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code> measures <code class="language-plaintext highlighter-rouge">dbt</code> and <code class="language-plaintext highlighter-rouge">dbr</code> only when they are present.</p> <p><em>8.</em> Separator.</p> <p>When UEFI variables are ready, <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code> records the PCR[7] <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_SEPARATOR</code></a> after <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code> and before the <code class="language-plaintext highlighter-rouge">ReadyToBoot</code> signal. This places the separator between SecureBootPolicy (Configuration) and ImageVerification (Authority).</p> <h3 id="measuring-boot-variables-into-pcr1rtmr0">Measuring Boot Variables into PCR[1]/RTMR[0]</h3> <p><em>9.</em> The UEFI BootOrder Variable and the Boot#### variables (just device paths).</p> <p><code class="language-plaintext highlighter-rouge">ReadAndMeasureBootVariable()</code> measures UEFI boot variables such as <code class="language-plaintext highlighter-rouge">BootOrder</code> and <code class="language-plaintext highlighter-rouge">Boot####</code> as <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_BOOT</code></a>. <code class="language-plaintext highlighter-rouge">MeasureAllBootVariables()</code> records them when present.</p> <h3 id="after-selecting-a-boot-device">After Selecting a Boot Device</h3> <p><em>10.</em> The action “Calling EFI Application from Boot Option” records that Boot Manager is about to execute code from a boot option (<strong>PCR[4]/RTMR[1]</strong>).</p> <p><code class="language-plaintext highlighter-rouge">OnReadyToBoot()</code> in <code class="language-plaintext highlighter-rouge">TdTcg2Dxe.c</code> records the action “Calling EFI Application from Boot Option” before invoking a boot option. If that option returns, the next boot attempt records “Returning from EFI Application from Boot Option” before trying another option.</p> <p><em>11.</em> A separator marks the transition from the pre-boot environment to the post-boot environment. In the TCG model, separator events are written to PCR[0] through PCR[6]. Here the RTMR[0] separator was already extended after Secure Boot Policy measurement, so <code class="language-plaintext highlighter-rouge">OnReadyToBoot()</code> extends the remaining separator into <strong>RTMR[1]</strong>.</p> <p><em>12.</em> <strong>[Optional] If UEFI Secure Boot is enabled,</strong> measure the entry in the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> that was used to validate the UEFI image (<strong>PCR[7]/RTMR[0]</strong>).</p> <p>When UEFI Secure Boot is enabled, <a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeImageVerificationLib"><code class="language-plaintext highlighter-rouge">DxeImageVerificationLib</code></a> verifies the PE image signature against an <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code></a> entry in an <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_LIST</code></a>. If an entry authorizes the image, <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> in <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeImageVerificationLib/Measurement.c">Measurement.c</a> measures that entry as <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_AUTHORITY</code></a>.</p> <p><em>13.</em> The GUID Partition Table (GPT) disk geometry (<strong>PCR[5]/RTMR[1]</strong>).</p> <p>When the selected boot option is on a GPT partition, <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code> calls <code class="language-plaintext highlighter-rouge">Tcg2MeasureGptTable()</code> in <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeTpm2MeasureBootLib/DxeTpm2MeasureBootLib.c">DxeTpm2MeasureBootLib.c</a> to measure the disk layout.</p> <p><em>14.</em> The selected UEFI application’s PE/COFF image—that is, the OS loader (<strong>PCR[4]/RTMR[1]</strong>).</p> <p><code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code> uses <code class="language-plaintext highlighter-rouge">Tcg2MeasurePeImage()</code> to measure third-party UEFI applications such as shell utilities, standard OS loaders, and OEM boot options as <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_BOOT_SERVICES_APPLICATION</code></a>. A UEFI application dispatched from an FV during DXE is measured into PCR[4] regardless of whether the containing FV was measured separately.</p> <h3 id="grub-extends-the-measurement-chain-into-the-os">GRUB Extends the Measurement Chain into the OS</h3> <p><a href="https://github.com/rhboot/grub2">GRUB 2</a> then extends the measurement chain from virtual firmware into the OS.</p> <p><em>Note: <a href="https://github.com/rhboot/shim">shim</a> primarily extends the UEFI Secure Boot authorization chain into the Linux ecosystem. This article does not explain shim’s verification logic, but when Secure Boot is enabled, shim itself and its related authority events remain part of the measured boot chain.</em></p> <p><em>15.</em> GRUB 2 measures its configuration file (for example, <code class="language-plaintext highlighter-rouge">grub.cfg</code>), GRUB commands, the kernel binary and command line, and the initrd binary (<strong>PCR[8, 9]/RTMR[2]</strong>). PCR[8] records command strings, while PCR[9] records file contents, as shown below.</p> <p>To support measurements on confidential computing platforms, two patches have been upstreamed, including:</p> <ul> <li><a href="https://github.com/rhboot/grub2/commit/4c76565b6cb885b7e144dc27f3612066844e2d19">efi/tpm: Add EFI_CC_MEASUREMENT_PROTOCOL support</a></li> <li><a href="https://github.com/rhboot/grub2/commit/86df79275d065d87f4de5c97e456973e8b4a649c">commands/efi/tpm: Re-enable measurements on confidential computing platforms</a></li> </ul> <table> <thead> <tr> <th>PCR Index</th> <th>PCR Usage</th> </tr> </thead> <tbody> <tr> <td>8</td> <td>Grub command line: All executed commands (including those from configuration files) will be logged and measured as entered with a prefix of “grub cmd:”</td> </tr> <tr> <td> </td> <td>Kernel command line: Any command line passed to a kernel will be logged and measured as entered with a prefix of “kernel cmdline:”</td> </tr> <tr> <td> </td> <td>Module command line: Any command line passed to a kernel module will be logged and measured as entered with a prefix of “module cmdline:”</td> </tr> <tr> <td>9</td> <td>Files: Any file read by GRUB will be logged and measured with a descriptive text corresponding to the filename.</td> </tr> </tbody> </table> <p><a href="https://github.com/rhboot/grub2/blob/master/grub-core/commands/tpm.c">tpm.c</a> registers <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> and <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> as a <code class="language-plaintext highlighter-rouge">grub_file_verifier</code>. They are called by <code class="language-plaintext highlighter-rouge">grub_verify_string()</code> and <code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> in <a href="https://github.com/rhboot/grub2/blob/master/grub-core/kern/verifiers.c">verifiers.c</a>.</p> <p>When GRUB 2 handles <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_MODULE_CMDLINE</code>, <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_KERNEL_CMDLINE</code>, or <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_COMMAND</code>, or calls <code class="language-plaintext highlighter-rouge">grub_create_loader_cmdline()</code> in <a href="https://github.com/rhboot/grub2/blob/master/grub-core/lib/cmdline.c">cmdline.c</a>, it invokes <code class="language-plaintext highlighter-rouge">grub_verify_string()</code>. <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> then calls <code class="language-plaintext highlighter-rouge">grub_tpm_measure()</code>, and <code class="language-plaintext highlighter-rouge">grub_cc_log_event()</code> measures the string into <strong>PCR[8]/RTMR[2]</strong>.</p> <p><code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> is registered as one of the <code class="language-plaintext highlighter-rouge">grub_file_filter</code> entries in <a href="https://github.com/rhboot/grub2/blob/master/include/grub/file.h"><code class="language-plaintext highlighter-rouge">file.h</code></a>. Whenever GRUB calls <code class="language-plaintext highlighter-rouge">grub_file_open()</code> in <a href="https://github.com/rhboot/grub2/blob/master/grub-core/kern/file.c">file.c</a>, the filter runs. <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> then calls <code class="language-plaintext highlighter-rouge">grub_tpm_measure()</code>, and <code class="language-plaintext highlighter-rouge">grub_cc_log_event()</code> measures the file contents into <strong>PCR[9]/RTMR[2]</strong>.</p> <p><em>16.</em> “Exit Boot Services Invocation” records that Boot Manager has called UEFI to end Boot Services (<strong>PCR[5]/RTMR[1]</strong>).</p> <p><em>17.</em> “Exit Boot Services Returned with Success” records that UEFI successfully exited Boot Services and terminated the pre-OS environment (<strong>PCR[5]/RTMR[1]</strong>).</p> <p><code class="language-plaintext highlighter-rouge">TdTcg2Dxe.c</code> records the <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> transition through <code class="language-plaintext highlighter-rouge">OnExitBootServices()</code> on success or <code class="language-plaintext highlighter-rouge">OnExitBootServicesFailed()</code> on failure.</p> <h3 id="if-secure-boot-policy-changes-during-boot">If Secure Boot Policy Changes During Boot</h3> <p><em>18.</em> The platform MAY be restarted OR the variables MUST be remeasured into (<strong>PCR[7]/RTMR[0]</strong>). Additionally the normal update process for setting any of the defined Secure Boot variables SHOULD occur before the initial measurement in PCR[7] or after the call to <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> has completed.</p> <p>UEFI Secure Boot variable updates are measured by <a href="https://github.com/tianocore/edk2/tree/master/MdeModulePkg/Universal/Variable/RuntimeDxe"><code class="language-plaintext highlighter-rouge">RuntimeDxe</code></a>. When one of the variables above changes, <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> in <a href="https://github.com/tianocore/edk2/blob/master/MdeModulePkg/Universal/Variable/RuntimeDxe/Measurement.c">Measurement.c</a> records the new data as <code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code>.</p> <h2 id="attestation">Attestation</h2> <p>From a verifier’s perspective, remote attestation is not a single check that asks whether the Quote signature is valid. It is a three-stage pipeline: establish the Quote’s provenance and integrity, use its MRTD and RTMR values to validate the event log, then interpret the verified events as firmware, Secure Boot, GRUB, and kernel state and compare that state with policy and reference values. Quote generation and transport are outside this article’s scope.</p> <h3 id="quote-verification">Quote Verification</h3> <p>A TDX Quote carries the contents of TDREPORT together with signatures and certification material needed for verification. A verifier checks the signature, certificate chain, revocation and TCB status, and freshness needed to prevent replay. It must also confirm that the reported platform and TD attributes satisfy policy. Successfully parsing a Quote does not by itself establish that the workload is trustworthy. The attestation key and its certification material form part of the endorsements in the IETF RATS architecture. See <a href="https://github.com/intel/SGXDataCenterAttestationPrimitives">Intel SGX Data Center Attestation Primitives (DCAP)</a> for the concrete formats and verification flow.</p> <h3 id="event-log-replay">Event Log Replay</h3> <p>Event log replay involves deserializing a raw event log and using the events to recalculate all of the measurement registers. Each event contains a digest and a measurement register index. The verifier will create simulated measurement registers and, for each event, extend the event digest into its corresponding simulated register. At the end, the verifier compares the simulated register values against the actual quoted measurement register values from the first step.</p> <p>Canonical’s TDX test repository contains the Python tool <a href="https://github.com/canonical/tdx/tree/main/tests/lib/tdx-tools/src/tdxtools">tdx-tools</a>, which compares event-log replay results with the RTMR values in TDREPORT from inside a TD guest. Its <code class="language-plaintext highlighter-rouge">VerifyActor</code> illustrates three steps:</p> <ul> <li>Get TD event log from CCEL ACPI table.</li> <li>Get TDREPORT via Linux attestation driver.</li> <li>Compare RTMR value from TDREPORT and RTMR value replayed via event log.</li> </ul> <p>An exact match establishes that the event log and the RTMR values in TDREPORT are consistent. It does not establish that the measured components satisfy the verifier’s policy; that decision requires event parsing and reference values. The following excerpt shows the current implementation’s core path:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">VerifyActor</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Actor to verify the RTMR
    </span><span class="sh">"""</span>

    <span class="k">def</span> <span class="nf">_verify_single_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">rtmr_value_1</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">,</span>
        <span class="n">rtmr_value_2</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>

        <span class="k">if</span> <span class="n">rtmr_value_1</span> <span class="o">==</span> <span class="n">rtmr_value_2</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] passed the verification.</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">error</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] did not pass the verification</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">verify_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Get TD report and RTMR replayed by event log to do verification.
        </span><span class="sh">"""</span>
        <span class="c1"># 1. Read CCEL from ACPI table at /sys/firmware/acpi/tables/CCEL
</span>        <span class="n">ccelobj</span> <span class="o">=</span> <span class="n">CCEL</span><span class="p">.</span><span class="nf">create_from_acpi_file</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">ccelobj</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">return</span>

        <span class="c1"># 2. Get the start address and length for event log area
</span>        <span class="n">td_event_log_actor</span> <span class="o">=</span> <span class="nc">TDEventLogActor</span><span class="p">(</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_start_address</span><span class="p">,</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_minimum_length</span><span class="p">)</span>

        <span class="c1"># 3. Collect event log and replay the RTMR value according to event log
</span>        <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>

        <span class="c1"># 4. Read TD REPORT via TDCALL.GET_TDREPORT
</span>        <span class="n">td_report</span> <span class="o">=</span> <span class="n">TdReport</span><span class="p">.</span><span class="nf">get_td_report</span><span class="p">()</span>

        <span class="c1"># 5. Verify individual RTMR value from TDREPORT and recalculated from
</span>        <span class="c1">#    event log
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">0</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_0</span><span class="sh">'</span><span class="p">])))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">1</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_1</span><span class="sh">'</span><span class="p">])))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">2</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">2</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_2</span><span class="sh">'</span><span class="p">])))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">3</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">3</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_3</span><span class="sh">'</span><span class="p">])))</span>
</code></pre></div></div> <h3 id="event-parsing">Event Parsing</h3> <p>Event parsing is the process of pulling information from the events. The verifier uses the output to make verification decisions against the appraisal policy, endorsements, and reference values.</p> <p><a href="https://github.com/google/go-eventlog/blob/main/extract/extract.go">go-eventlog</a>, for example, can parse, replay, and extract state from several measured-boot log formats, including firmware, Secure Boot configuration, GRUB, and Linux kernel state. The library explicitly does not verify Quotes or provide reference measurements. Integrating code must verify the Quote first and then supply trusted register values to the replay and extraction logic. The <code class="language-plaintext highlighter-rouge">FirmwareLogState</code> excerpt below shows this extraction step; the exact API may continue to change while the library remains pre-release.</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// FirmwareLogState extracts event info from a verified TCG PC Client event</span>
<span class="c">// log into a FirmwareLogState.</span>
<span class="c">//</span>
<span class="c">// It is the caller's responsibility to ensure that the passed events have</span>
<span class="c">// been replayed (e.g., using `tcg.ParseAndReplay`) against a verified measurement</span>
<span class="c">// register bank.</span>
<span class="k">func</span> <span class="n">FirmwareLogState</span><span class="p">(</span><span class="n">events</span> <span class="p">[]</span><span class="n">tcg</span><span class="o">.</span><span class="n">Event</span><span class="p">,</span> <span class="n">hash</span> <span class="n">crypto</span><span class="o">.</span><span class="n">Hash</span><span class="p">,</span> <span class="n">registerCfg</span> <span class="n">registerConfig</span><span class="p">,</span> <span class="n">opts</span> <span class="n">Opts</span><span class="p">)</span> <span class="p">(</span><span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">var</span> <span class="n">joined</span> <span class="kt">error</span>
	<span class="n">tcgHash</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">tpm2</span><span class="o">.</span><span class="n">HashToAlgorithm</span><span class="p">(</span><span class="n">hash</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
	<span class="p">}</span>

	<span class="n">platform</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">PlatformExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">sbState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">SecureBootState</span><span class="p">(</span><span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">,</span> <span class="n">opts</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">efiState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">EfiState</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">,</span> <span class="n">opts</span><span class="p">)</span>

	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>

	<span class="k">var</span> <span class="n">grub</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">GrubState</span>
	<span class="k">var</span> <span class="n">kernel</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">LinuxKernelState</span>
	<span class="k">if</span> <span class="n">opts</span><span class="o">.</span><span class="n">Loader</span> <span class="o">==</span> <span class="n">GRUB</span> <span class="p">{</span>
		<span class="n">grub</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">GRUBExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>

		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
		<span class="n">kernel</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">LinuxKernelStateFromGRUB</span><span class="p">(</span><span class="n">grub</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="o">&amp;</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">{</span>
		<span class="n">Platform</span><span class="o">:</span>    <span class="n">platform</span><span class="p">,</span>
		<span class="n">SecureBoot</span><span class="o">:</span>  <span class="n">sbState</span><span class="p">,</span>
		<span class="n">Efi</span><span class="o">:</span>         <span class="n">efiState</span><span class="p">,</span>
		<span class="n">RawEvents</span><span class="o">:</span>   <span class="n">tcg</span><span class="o">.</span><span class="n">ConvertToPbEvents</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">),</span>
		<span class="n">Hash</span><span class="o">:</span>        <span class="n">pb</span><span class="o">.</span><span class="n">HashAlgo</span><span class="p">(</span><span class="n">tcgHash</span><span class="p">),</span>
		<span class="n">Grub</span><span class="o">:</span>        <span class="n">grub</span><span class="p">,</span>
		<span class="n">LinuxKernel</span><span class="o">:</span> <span class="n">kernel</span><span class="p">,</span>
		<span class="n">LogType</span><span class="o">:</span>     <span class="n">registerCfg</span><span class="o">.</span><span class="n">LogType</span><span class="p">,</span>
	<span class="p">},</span> <span class="n">joined</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="reference-measurements">Reference Measurements</h3> <p>After establishing log consistency, the verifier must still ask whether the components are the expected versions. That requires reference measurements or a higher-level Reference Integrity Manifest (RIM). The final register values depend not only on file contents, but also on loaded representations, dynamic platform data, and event order. Hashing a few files independently with SHA-384 is therefore not a complete reference-value calculation.</p> <p>Canonical issue <a href="https://github.com/canonical/tdx/issues/263">#263</a> documents these difficulties in offline MRTD and RTMR calculation. It was closed in July 2026 when the original Tech Preview was archived, not because it produced a general algorithm for arbitrary GRUB boot configurations. The following is an author proposal, not a capability guaranteed by an existing tool: inspired by the plugin architecture of <a href="https://github.com/cc-api/cvm-image-rewriter">cvm-image-rewriter</a>, an image build could add a <code class="language-plaintext highlighter-rouge">reference-measurement-calculator</code> that extracts GRUB, the kernel, the initrd, and configuration and emits reference material bound to that build.</p> <p><code class="language-plaintext highlighter-rouge">cvm-image-rewriter</code> uses plugins to customize a confidential VM’s guest image, configuration, and OVMF firmware. The repository includes the following plugins:</p> <table> <thead> <tr> <th>Name</th> <th>Descriptions</th> </tr> </thead> <tbody> <tr> <td>01-resize-image</td> <td>Resize the input qcow2 image</td> </tr> <tr> <td>02-motd-welcome</td> <td>Customize the login welcome message</td> </tr> <tr> <td>03-netplan</td> <td>Customize the netplan.yaml</td> </tr> <tr> <td>04-user-authkey</td> <td>Add auth key for user login instead of password</td> </tr> <tr> <td>05-readonly-data</td> <td>Fix some file permissions to read-only</td> </tr> <tr> <td>06-install-tdx-guest-kernel</td> <td>Install MVP TDX guest kernel</td> </tr> <tr> <td>07-device-permission</td> <td>Fix the permission for device node</td> </tr> <tr> <td>08-ccnp-uds-directory-permission</td> <td>Fix the permission for CCNP UDS directory</td> </tr> <tr> <td>60-initrd-update</td> <td>Update the initrd image</td> </tr> <tr> <td>97-sample</td> <td>plugin customization example</td> </tr> <tr> <td>98-ima-enable-simple</td> <td>Enable IMA (Integrity Measurement Architecture) feature</td> </tr> </tbody> </table> <p>Such a plugin would need to record component digests, event types, destination registers, and extension order. If the measured object is an in-memory representation rather than the original file, the plugin must also reproduce the relevant loading logic. Its output should be treated as reference material for a specific build and boot configuration, not as an RTMR value that applies to every platform.</p> <p>On the firmware side, TDVF Binary Layout can be used to locate the BFV and CFV in a firmware image. Contrast’s <a href="https://github.com/edgelesssys/contrast/blob/main/tools/tdx-measure/tdvf/tdvf.go">tdx-measure</a> demonstrates how to precompute selected measurements for explicit inputs, but its assumptions must match the actual TDVF build, QEMU parameters, and boot path.</p> <p>The tool also exposes an RTMR precomputation command for a given firmware, kernel, initrd, and command line through <code class="language-plaintext highlighter-rouge">newRtMrCmd()</code>. When inputs and event order are completely fixed, a verifier can compare a precomputed value directly with the reported value. Dynamic ACPI data, boot-menu choices, or additional events can invalidate that comparison. The event log therefore remains essential evidence for explaining which object affected a register and in what order.</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">newRtMrCmd</span><span class="p">()</span> <span class="o">*</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span> <span class="p">{</span>
	<span class="n">cmd</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span><span class="p">{</span>
		<span class="n">Use</span><span class="o">:</span>   <span class="s">"rtmr -f OVMF.fd -k bzImage [0|1|2|3]"</span><span class="p">,</span>
		<span class="n">Short</span><span class="o">:</span> <span class="s">"calculate the RTMR for a firmware and kernel file"</span><span class="p">,</span>
		<span class="n">Long</span><span class="o">:</span> <span class="s">`Calculate the RTMR for a firmware and kernel file.

		This will parse the firmware according to the TDX Virtual Firmware Design Guide
		and/or hash the kernel and pre-calculate a given RTMR.`</span><span class="p">,</span>
		<span class="n">Args</span><span class="o">:</span>      <span class="n">cobra</span><span class="o">.</span><span class="n">MatchAll</span><span class="p">(</span><span class="n">cobra</span><span class="o">.</span><span class="n">ExactArgs</span><span class="p">(</span><span class="m">1</span><span class="p">),</span> <span class="n">cobra</span><span class="o">.</span><span class="n">OnlyValidArgs</span><span class="p">),</span>
		<span class="n">ValidArgs</span><span class="o">:</span> <span class="p">[]</span><span class="kt">string</span><span class="p">{</span><span class="s">"0"</span><span class="p">,</span> <span class="s">"1"</span><span class="p">,</span> <span class="s">"2"</span><span class="p">,</span> <span class="s">"3"</span><span class="p">},</span>
		<span class="n">RunE</span><span class="o">:</span>      <span class="n">runRtMr</span><span class="p">,</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"f"</span><span class="p">,</span> <span class="s">"OVMF.fd"</span><span class="p">,</span> <span class="s">"path to firmware file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"fd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">,</span> <span class="s">"k"</span><span class="p">,</span> <span class="s">"bzImage"</span><span class="p">,</span> <span class="s">"path to kernel file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">,</span> <span class="s">"i"</span><span class="p">,</span> <span class="s">"initrd.zst"</span><span class="p">,</span> <span class="s">"path to initrd file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"cmdline"</span><span class="p">,</span> <span class="s">"c"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"kernel command line"</span><span class="p">)</span>
	<span class="k">return</span> <span class="n">cmd</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="appendix">Appendix</h2> <p><span id="jumpA"></span></p> <h3 id="tdvf-configurations">TDVF Configurations</h3> <p><strong>Config-A</strong>:</p> <ul> <li>Merge the <em>basic</em> TDVF feature into the existing <code class="language-plaintext highlighter-rouge">OvmfPkgX64.dsc</code>. (Align with existing SEV.)</li> <li>Threat model: the VMM remains in the TCB.</li> <li><code class="language-plaintext highlighter-rouge">OvmfPkgX64.dsc</code> provides basic boot support for SEV, TDX, and normal OVMF. The resulting binary can run in all three environments.</li> <li>The existing OvmfPkgX64 image layout is unchanged.</li> <li>Existing features do not have to be removed.</li> <li>PEI is not skipped for either TD or non-TD guests.</li> <li>RTMR-based measurement is optional and can be enabled.</li> <li>Host VMM inputs such as the TD HOB and CFV are measured.</li> <li>Other external inputs, including <code class="language-plaintext highlighter-rouge">fw_cfg</code> data, the OS loader, and the initrd, are measured.</li> </ul> <p><strong>Build TDVF Config-A:</strong></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">.</span> ./edksetup.sh
make <span class="nt">-C</span> BaseTools
build <span class="nt">-p</span> OvmfPkg/OvmfPkgX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC <span class="se">\</span>
  <span class="nt">-D</span> <span class="nv">CC_MEASUREMENT_ENABLE</span><span class="o">=</span>TRUE <span class="nt">-b</span> RELEASE
</code></pre></div></div> <p><strong>Config-B</strong>:</p> <ul> <li>Add a standalone <code class="language-plaintext highlighter-rouge">IntelTdxX64.dsc</code> under the TDX-specific directory for a full-featured TDVF. (Align with existing SEV.)</li> <li>Threat model: the VMM is outside the TCB, so the firmware must defend against VMM-controlled inputs.</li> <li><code class="language-plaintext highlighter-rouge">IntelTdxX64.dsc</code> provides basic boot support for TDX and normal OVMF. The resulting binary can run in both environments.</li> <li>It may eventually merge with <code class="language-plaintext highlighter-rouge">AmdSev.dsc</code>, but the projects have not committed to a schedule.</li> <li>RTMR-based measurement is mandatory.</li> <li>Host VMM inputs such as the TD HOB and CFV are measured.</li> <li>Other external inputs, including <code class="language-plaintext highlighter-rouge">fw_cfg</code> data, the OS loader, and the initrd, are measured.</li> <li>Skip PEI and split the DXE FV so a TD guest loads only the required drivers, reducing the attack surface.</li> </ul> <p><strong>Build TDVF Config-B:</strong></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">.</span> ./edksetup.sh
make <span class="nt">-C</span> BaseTools
build <span class="nt">-p</span> OvmfPkg/IntelTdx/IntelTdxX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC <span class="nt">-b</span> RELEASE
</code></pre></div></div> <p>These configuration notes and commands follow the EDK2 <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/IntelTdx/README.md">TDVF README</a>. Always use the README from the EDK2 revision being built as the final authority.</p> <p><span id="jumpB"></span></p> <h3 id="uefi-boot-phase">UEFI Boot Phase</h3> <p>This article uses the common six-phase view of UEFI platform initialization (PI): SEC, PEI, DXE, BDS, TSL, and Runtime.</p> <p><em>1.</em> Security (SEC)</p> <p>This is the entry point to PI. It establishes the initial execution environment and temporary memory, then passes platform boot information to PEI. SEC may host a platform root of trust, but that is not the same as UEFI Secure Boot, whose signature policy is primarily enforced later when images are loaded.</p> <p><em>2.</em> Pre-EFI Initialization (PEI)</p> <p>PEI uses the CPU resources available at that point to dispatch Pre-EFI Initialization Modules (PEIMs). These modules perform boot-critical work such as memory initialization, then pass control to the Driver Execution Environment (DXE).</p> <p><em>3.</em> Driver Execution Environment (DXE)</p> <p>DXE performs most system initialization. After PEI has prepared memory and transferred control, the DXE Dispatcher loads hardware drivers, Runtime Services, and the Boot Services needed to start the OS.</p> <p><em>4.</em> Boot Device Selection (BDS)</p> <p>After the DXE Dispatcher has run the required drivers, Boot Device Selection initializes consoles and any remaining required devices, selects a boot entry, and loads its OS loader in preparation for TSL.</p> <p><em>5.</em> Transient System Load (TSL)</p> <p>TSL spans the interval between boot selection and the final OS handoff. A UEFI application such as the shell may run here, but the usual path executes a bootloader that prepares the OS and calls <code class="language-plaintext highlighter-rouge">ExitBootServices()</code>. An OS can also make that call directly, as a Linux kernel built with <code class="language-plaintext highlighter-rouge">CONFIG_EFI_STUB</code> does.</p> <p><em>6.</em> Runtime (RT)</p> <p>The OS takes control during Runtime. UEFI Runtime Services remain available, including services for reading and writing variables in NVRAM.</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[How a GRUB-booted TD is measured from firmware to kernel, and how a verifier evaluates that boot chain.]]></summary></entry><entry xml:lang="zh"><title type="html">Intel TDX: Measured Boot and Remote Attestation with GRUB</title><link href="https://mahaocheng.me/blog/2025/tdx-measure-boot/zh/" rel="alternate" type="text/html" title="Intel TDX: Measured Boot and Remote Attestation with GRUB"/><published>2025-01-12T00:00:00+00:00</published><updated>2025-01-12T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tdx-measure-boot/tdx-measure-boot.zh</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tdx-measure-boot/zh/"><![CDATA[<p>Intel Trust Domain Extensions（TDX）把虚拟机放入由硬件隔离的 Trust Domain（TD）中，使 TD 的内存和执行状态不直接暴露给虚拟机管理器（VMM）、hypervisor 及宿主机上的其他非 TD 软件。云服务提供商仍负责启动和调度虚拟机，但不再因掌控宿主软件栈而自动进入 TD 的可信计算基（Trusted Computing Base，TCB）。</p> <p>隔离只能回答“宿主是否能直接读取或修改 TD”，不能回答“TD 启动了什么”。后一个问题由度量启动（measured boot）和远程证明（remote attestation）共同回答：</p> <ol> <li>固件、启动配置、GRUB、内核和 initrd 等对象在执行或加载时被哈希；</li> <li>每个摘要按顺序扩展（extend）进 Trust Domain Measurement Register（MRTD）或 Runtime Measurement Register（RTMR），同时写入事件日志；</li> <li>TDX Quote 对包括这些度量值在内的 TD 状态提供可验证的密码学证据；</li> <li>验证方先校验 Quote，再回放事件日志，并按自己的参考值和策略判断这次启动是否可接受。</li> </ol> <p>TD 内的 UEFI 固件通常由 TDX Virtual Firmware（TDVF）提供；它基于 EDK2/OVMF，负责建立 TD 的固件执行环境并加载后续组件。本文是一篇机制说明，聚焦采用 GRUB 的 TD 启动路径：先说明 TDVF 如何把度量链交给 GRUB，再说明验证方如何把 Quote、事件日志和参考值连接起来。文中的 TCB 取决于验证策略；CPU 和 TDX Module/SEAM 固件属于平台侧基础，TDVF、GRUB、内核等 guest 组件是否被信任，则取决于验证方接受的参考值。TDVF 的两种配置及其不同威胁模型见<a href="#jumpA">附录</a>。</p> <h2 id="td-启动类型">TD 启动类型</h2> <p>先从两条常见启动路径看本文的范围。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. TD Guest Boot Process </div> <p><strong>Direct boot</strong> 不经过 GRUB 等中间引导加载器。固件通过 QEMU 提供的 <code class="language-plaintext highlighter-rouge">fw_cfg</code> 接口取得 kernel、initrd 和命令行，然后直接启动内核。</p> <p><strong>GRUB boot</strong> 则由固件从磁盘镜像加载 UEFI bootloader，再由 GRUB 读取配置、选择内核并加载 initrd。这条路径多了一层软件和配置，也因此需要更长的度量链。</p> <p>图 2 展开了两条路径。本文只讨论右侧的 GRUB boot。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. Detailed Flow for Different TD Boot </div> <p>从机制上看，这条链有三个交接点：TDVF 度量自身配置和外部输入；UEFI 镜像加载路径度量 shim/GRUB 等 PE/COFF 镜像；GRUB 再度量配置、命令、kernel 和 initrd。若启用 UEFI Secure Boot，shim 还可以把发行版信任链延伸到 GRUB 和内核；但 Secure Boot 负责阻止未获授权的代码执行，度量启动负责记录实际加载了什么，两者不能互相替代。</p> <p>这套事件类型和 PCR 语义沿用了 TCG 可信启动模型，TDX 则把事件映射到 MRTD/RTMR，并把硬件与 TDX Module 纳入 Quote 所证明的平台状态。TDVF 配置仍然决定 VMM 是否真正排除在 TCB 之外，不能仅凭“使用了 TDX”作出判断。</p> <h2 id="uefi-启动时序">UEFI 启动时序</h2> <p>GRUB 在这条路径中以 UEFI 应用的身份运行。理解它与固件的边界，需要先看图 3 所示的 UEFI 启动时序。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 3. UEFI Booting Sequence </div> <p>固件通过 Boot Services（BS）加载并启动所选的 OS loader；GRUB 等 loader 也以 UEFI 应用的身份调用这些服务。内核准备就绪后，loader 调用 <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 结束 BS 并释放相关资源，此后 OS 只能继续使用 UEFI Runtime Services（RT）。因此，<code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 既是执行边界，也是事件日志中区分 pre-boot 与 post-boot 的关键节点。</p> <p>UEFI 启动阶段的详细介绍见 <a href="https://secret.club/2020/05/26/introduction-to-uefi-part-1.html#uefi-boot-phases">UEFI boot phases</a> 中的 <a href="#jumpB">Appendix: UEFI Boot Phase</a>。</p> <h2 id="度量启动组件">度量启动组件</h2> <p>如图 4，内核之前的 pre-boot 环境包括 TDVF/OVMF 和 bootloader（可选的 shim 以及 GRUB）两个阶段。UEFI 组件通过 <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> 记录事件，并把摘要扩展进 Runtime Measurement Register（RTMR）；MRTD 则记录 TD 初始构建阶段的度量。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 4. TD Measurement Process </div> <p><code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> 沿用 TCG event log 的基本模型：事件日志位于 Confidential Computing Event Log（CCEL）描述的内存区域，事件摘要按 <code class="language-plaintext highlighter-rouge">MR_new = Hash(MR_old || event_digest)</code> 扩展到对应 RTMR。日志本身不是可信根；验证方必须回放日志，并把结果与已由 Quote 保护的 RTMR 值比较，才能发现日志是否缺失或被修改。</p> <p>EDK2 中的度量启动组件如下。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 5. Measured Boot Component in EDK2 </div> <p><a href="https://github.com/tianocore/edk2/tree/master/OvmfPkg/IntelTdx/TdxHelperLib"><code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code></a> 在 SEC 阶段提供度量函数。 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Protocol/CcMeasurement.h"><code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code></a> 协议抽象了 UEFI guest 环境中的机密计算（CC）度量操作。 <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/Tcg/TdTcg2Dxe/TdTcg2Dxe.c">TdTcg2Dxe.c</a> DXE 驱动处理 DXE 阶段的度量。 <a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeTpm2MeasureBootLib"><code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootLib</code></a> 处理 PE 镜像度量和 GPT 度量。所有事件类型的定义见 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">UefiTcgPlatform.h</code></a>。</p> <p>下面根据 <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html#">Confidential Computing in UEFI Specification</a> 简述 <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code>。</p> <p>如果具备 CC 能力的虚拟固件支持度量，则它应提供使用新 GUID <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL_GUID</code> 的 <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code>，用以上报事件日志并提供哈希能力。</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define EFI_CC_MEASUREMENT_PROTOCOL_GUID  \
  { 0x96751a3d, 0x72f4, 0x41a6, { 0xa7, 0x94, 0xed, 0x5d, 0x0e, 0x67, 0xae, 0x6b }}
</span><span class="k">extern</span> <span class="n">EFI_GUID</span>  <span class="n">gEfiCcMeasurementProtocolGuid</span><span class="p">;</span>

<span class="c1">// Protocol Interface Structure</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">_EFI_CC_MEASUREMENT_PROTOCOL</span> <span class="p">{</span>
  <span class="n">EFI_CC_GET_CAPABILITY</span>          <span class="n">GetCapability</span><span class="p">;</span>
  <span class="n">EFI_CC_GET_EVENT_LOG</span>           <span class="n">GetEventLog</span><span class="p">;</span>
  <span class="n">EFI_CC_HASH_LOG_EXTEND_EVENT</span>   <span class="n">HashLogExtendEvent</span><span class="p">;</span>
  <span class="n">EFI_CC_MAP_PCR_TO_MR_INDEX</span>     <span class="n">MapPcrToMrIndex</span><span class="p">;</span>
<span class="p">}</span> <span class="n">EFI_CC_MEASUREMENT_PROTOCOL</span><span class="p">;</span>
</code></pre></div></div> <p>该协议接口包含四个成员：</p> <ul> <li>GetCapability：提供协议能力信息与状态信息。</li> <li>GetEventLog：允许调用方获取指定事件日志的地址及其末尾条目。</li> <li>HashLogExtendEvent：允许调用方扩展并可选地记录事件，无需了解实际的 CC 命令。</li> <li>MapPcrToMrIndex：向调用方提供 TPM PCR 到 CC MR 的映射信息。</li> </ul> <h3 id="intel-tdx-的寄存器映射">Intel TDX 的寄存器映射</h3> <p>下表展示了 TPM Platform Configuration Register（PCR）索引映射以及 Intel TDX 下 CC event log MR 索引的含义。MRTD 指 Trust Domain Measurement Register，RTMR 指 Runtime Measurement Register。TD MR 数量少于 TPM PCR，典型映射如下：</p> <table> <thead> <tr> <th>TPM PCR Index</th> <th>Typical Usage of Measurement Registers</th> <th>TDX MR Index</th> </tr> </thead> <tbody> <tr> <td>0</td> <td>FirmwareCode (BFV, including init page table)</td> <td>MRTD</td> </tr> <tr> <td>1</td> <td>FirmwareData (CFV, TD Hob, ACPI Table, Boot Variable)</td> <td>RTMR[0]</td> </tr> <tr> <td>2</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>3</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>4</td> <td>OS loader code</td> <td>RTMR[1]</td> </tr> <tr> <td>5</td> <td>GUID partition table (GPT)</td> <td>RTMR[1]</td> </tr> <tr> <td>6</td> <td>N/A</td> <td>N/A</td> </tr> <tr> <td>7</td> <td>Secure Boot Configuration</td> <td>RTMR[0]</td> </tr> <tr> <td>8~15</td> <td>TD OS measurement</td> <td>RTMR[2]</td> </tr> </tbody> </table> <p><em>注：RTMR[3] 保留用于特殊用途，例如 virtual TPM。如果这些专用场景不需要 RTMR[3]，用户可以灵活使用。</em></p> <p>MRTD 与 RTMR 的典型用途如下，更多细节见 <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">8.1 Measurement Register Usage in TD</a>：</p> <ul> <li>MRTD 用于 TDVF 代码（对应 PCR[0]）。</li> <li>RTMR[0] 用于 TDVF 配置（对应 PCR[1,7]），应遵循 TCG Platform Firmware Profile（PFP）规范。</li> <li>RTMR[1] 用于 TDVF 加载的组件，例如 OS loader（对应 PCR[4,5]），应遵循 TCG PFP 规范。</li> <li>RTMR[2] 用于 OS 组件，例如 OS kernel、initrd 与应用程序（对应 PCR[8~15]），使用方式由 OS 决定。</li> <li>RTMR[3] 仅保留给特殊用途。</li> </ul> <h2 id="度量启动流程">度量启动流程</h2> <p>下面沿用图 4 的同一条启动路径，按事件发生顺序展开：TDVF 先处理来自 VMM 的输入，UEFI 再度量启动策略和 OS loader，GRUB 最后把链延伸到 kernel 与 initrd。编号 0–18 对应图中的事件节点。</p> <h3 id="度量-td-hob-与-configuration-fv">度量 TD HOB 与 Configuration FV</h3> <p><a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">4.2 TD Hand-Off Block（HOB）</a>和 <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">3.2 Configuration Firmware Volume（CFV）</a>由 Host VMM 提供，TD guest 不能默认信任。TDVF 先检查其数据结构，再度量并扩展到 RTMR；度量只能让后续篡改或差异可见，不会把不可信输入变成可信输入。与此同时，TDVF 创建两个 <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT_HOB</code>，分别携带 TD HOB list 和 CFV 的摘要；DXE 阶段再据此创建 <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT</code>。</p> <p>Configuration Firmware Volume 包含所有预置数据，该区域只读。一种可能用途是存放 UEFI Secure Boot 变量内容，例如 PK、KEK、db、dbx。</p> <p>TD HOB list 用于从 VMM 向 TDVF 传递信息。TD HOB 必须包含 PHIT HOB 和 Resource Descriptor HOB，其他 HOB 可选。</p> <ul> <li>TD HOB 的第一个 HOB 必须是 PHIT HOB。EfiMemoryTop、EfiMemoryBottom、EfiFreeMemoryTop、EfiFreeMemoryBottom 都应为 0。</li> <li>TD HOB 必须至少包含一个 Resource Description HOB，用以声明物理内存资源。</li> </ul> <p><strong><em>0.</em></strong> <code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code> 是 TdxHelperLib 的 SEC 实例。它在 SEC 阶段为 TDX 提供以下功能：</p> <ul> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureTdHob</code> 度量/扩展 TdHob，并把度量值存入 workarea。</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureCfvImage</code> 度量/扩展 Configuration FV 镜像，并把度量值存入 workarea。</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperBuildGuidHobForTdxMeasurement</code> 为 TDX 度量构建 GuidHob。</li> </ul> <h3 id="度量-secure-boot-policy-到-pcr7rtmr0">度量 Secure Boot Policy 到 PCR[7]/RTMR[0]</h3> <p><em>1.</em> UEFI Debug Mode。</p> <p>如果平台提供固件调试模式，则必须以 <code class="language-plaintext highlighter-rouge">EV_EFI_ACTION</code> 度量字符串 “UEFI Debug Mode”。该逻辑在 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code> 中实现，依据 <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/SecurityPkg.dec"><code class="language-plaintext highlighter-rouge">PcdFirmwareDebuggerInitialized</code></a> 判定。</p> <p><em>2.</em> <code class="language-plaintext highlighter-rouge">SecureBoot</code> 变量的内容。</p> <p><em>3.</em> <code class="language-plaintext highlighter-rouge">PK</code> 变量的内容。</p> <p><em>4.</em> <code class="language-plaintext highlighter-rouge">KEK</code> 变量的内容。</p> <p><em>5.</em> <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> 变量（DB）的内容。</p> <p><em>6.</em> <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE1</code> 变量（DBX）的内容。</p> <p><em>7.</em> 如存在且非空，则度量 <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE2</code>（DBT）变量的内容。</p> <p>UEFI Secure Boot 相关变量——”SecureBoot”、”PK”、”KEK”、”db”、”dbx”——由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">ReadAndMeasureSecureVariable()</code> 无条件度量，事件类型为 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code></a>。若变量不存在，则度量一条零长度的 UEFI 变量条目。”dbt” 与 “dbr” 变量在存在时才被 <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code> 度量。</p> <p><em>8.</em> Separator。</p> <p>PCR7 的 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_SEPARATOR</code></a> 在 UEFI 变量就绪后由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code> 处理，位于 <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code> 之后、<code class="language-plaintext highlighter-rouge">ReadyToBoot</code> 事件信号之前。原因是 PCR7 的 <code class="language-plaintext highlighter-rouge">EV_SEPARATOR</code> 必须位于 SecureBootPolicy（Configure）与 ImageVerification（Authority）之间。</p> <h3 id="度量-boot-variable-到-pcr1rtmr0">度量 Boot Variable 到 PCR[1]/RTMR[0]</h3> <p><em>9.</em> UEFI BootOrder 变量和 Boot#### 变量（仅 device path）。</p> <p>UEFI 启动相关变量（例如 “BootOrder” 和 “Boot####”）由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">ReadAndMeasureBootVariable()</code> 度量，事件类型为 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_BOOT</code></a>。<code class="language-plaintext highlighter-rouge">MeasureAllBootVariables()</code> 在变量存在时将其度量。</p> <h3 id="选择启动设备之后">选择启动设备之后</h3> <p><em>10.</em> 启动动作 “Calling EFI Application from Boot Option”，表示 Boot Manager 正在尝试从某个启动选项执行代码（<strong>PCR[4]/RTMR[1]</strong>）。</p> <p>该启动动作由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">OnReadyToBoot()</code> 度量。调用启动选项之前，度量 “Calling EFI Application from Boot Option”；启动选项返回之后，度量 “Returning from EFI Application from Boot Option”。</p> <p><em>11.</em> Separator，用以标记离开 pre-boot 环境、进入 post-boot 环境。TCG 模型会在 PCR[0~6] 中写入分隔事件；在这里，RTMR[0] 的分隔事件已于 Secure Boot Policy 度量后写入，因此 <code class="language-plaintext highlighter-rouge">OnReadyToBoot()</code> 再把分隔事件扩展到 <strong>RTMR[1]</strong>。</p> <p><em>12.</em> <strong>[可选] 若启用 UEFI Secure Boot</strong>，度量 <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> 中用于校验 UEFI 镜像的那一条条目（<strong>PCR[7]/RTMR[0]</strong>）。</p> <p>启用 UEFI Secure Boot 时，<a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeImageVerificationLib"><code class="language-plaintext highlighter-rouge">DxeImageVerificationLib</code></a> 依据镜像签名数据库中 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_LIST</code></a> 里的 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code></a> 来校验 PE 镜像签名。若用某个 <code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code> 校验了镜像，则 <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeImageVerificationLib/Measurement.c">Measurement.c</a> 的 <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> 会以 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_AUTHORITY</code></a> 度量该 <code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code>。</p> <p><em>13.</em> GUID Partition Table（GPT）磁盘布局（<strong>PCR[5]/RTMR[1]</strong>）。</p> <p>当系统从磁盘中某个 GUID 命名分区启动时，需要度量 GPT 磁盘布局。由 <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeTpm2MeasureBootLib/DxeTpm2MeasureBootLib.c">DxeTpm2MeasureBootLib.c</a> 的 <code class="language-plaintext highlighter-rouge">Tcg2MeasureGptTable()</code> 在 <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code> 中完成。</p> <p><em>14.</em> 选中的 UEFI 应用代码 PE/COFF 镜像，即 OS loader（<strong>PCR[4]/RTMR[1]</strong>）。</p> <p>第三方 UEFI 应用（例如 UEFI shell 工具、标准 OS loader 或 OEM 启动选项）由 DxeTpm2MeasureBootLib.c 的 <code class="language-plaintext highlighter-rouge">Tcg2MeasurePeImage()</code> 在 <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code> 中度量，事件类型为 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_BOOT_SERVICES_APPLICATION</code></a>。若某 UEFI 应用是 DXE 阶段分派的 FV，则无论该 FV 本身是否被度量，都会度量到 PCR4。</p> <h3 id="grub-把度量链延伸到-os">GRUB 把度量链延伸到 OS</h3> <p><a href="https://www.gnu.org/software/grub/">GRUB 2</a> 接手后，把可信启动链从虚拟固件延伸到 OS。</p> <p><em>注：<a href="https://github.com/rhboot/shim">shim</a> 的主要职责是把 UEFI Secure Boot 的信任链延伸到 Linux 生态。本文不展开其验证机制，但启用 Secure Boot 时，shim 本身及相关授权事件仍属于启动链的一部分。</em></p> <p><em>15.</em> GRUB 2 度量配置文件（例如 <code class="language-plaintext highlighter-rouge">grub.cfg</code>）、GRUB 命令、内核二进制、内核命令行和 initrd 二进制（<strong>PCR[8, 9]/RTMR[2]</strong>）。PCR[8] 对应命令行字符串，PCR[9] 对应文件二进制，如下表所示。</p> <p>为支持机密计算平台上的度量，两个补丁已合入上游：</p> <ul> <li><a href="https://github.com/rhboot/grub2/commit/4c76565b6cb885b7e144dc27f3612066844e2d19">efi/tpm: Add EFI_CC_MEASUREMENT_PROTOCOL support</a></li> <li><a href="https://github.com/rhboot/grub2/commit/86df79275d065d87f4de5c97e456973e8b4a649c">commands/efi/tpm: Re-enable measurements on confidential computing platforms</a></li> </ul> <table> <thead> <tr> <th>PCR Index</th> <th>PCR Usage</th> </tr> </thead> <tbody> <tr> <td>8</td> <td>Grub command line: All executed commands (including those from configuration files) will be logged and measured as entered with a prefix of “grub cmd:”</td> </tr> <tr> <td> </td> <td>Kernel command line: Any command line passed to a kernel will be logged and measured as entered with a prefix of “kernel cmdline:”</td> </tr> <tr> <td> </td> <td>Module command line: Any command line passed to a kernel module will be logged and measured as entered with a prefix of “module cmdline:”</td> </tr> <tr> <td>9</td> <td>Files: Any file read by GRUB will be logged and measured with a descriptive text corresponding to the filename.</td> </tr> </tbody> </table> <p><a href="https://github.com/rhboot/grub2/blob/master/grub-core/commands/tpm.c">tpm.c</a> 将 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> 和 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> 注册为 <code class="language-plaintext highlighter-rouge">grub_file_verifier</code>。它们由 <a href="https://github.com/rhboot/grub2/blob/master/grub-core/kern/verifiers.c">verifiers.c</a> 中的 <code class="language-plaintext highlighter-rouge">grub_verify_string()</code> 和 <code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> 调用。</p> <p>当 GRUB 2 处理 <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_MODULE_CMDLINE</code>、<code class="language-plaintext highlighter-rouge">GRUB_VERIFY_KERNEL_CMDLINE</code>、<code class="language-plaintext highlighter-rouge">GRUB_VERIFY_COMMAND</code> 等命令类型，或执行 <a href="https://github.com/rhboot/grub2/blob/master/grub-core/lib/cmdline.c">cmdline.c</a> 中的 <code class="language-plaintext highlighter-rouge">grub_create_loader_cmdline()</code> 时，会调用 <code class="language-plaintext highlighter-rouge">grub_verify_string()</code>。随后 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> 调用 <code class="language-plaintext highlighter-rouge">grub_tpm_measure()</code>，再由 <code class="language-plaintext highlighter-rouge">grub_cc_log_event()</code> 将字符串度量到 <strong>PCR[8]/RTMR[2]</strong>。</p> <p><code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> 注册为 <a href="https://github.com/rhboot/grub2/blob/master/include/grub/file.h"><code class="language-plaintext highlighter-rouge">file.h</code></a> 中的 <code class="language-plaintext highlighter-rouge">grub_file_filter</code> 之一。每当 GRUB 调用 <a href="https://github.com/rhboot/grub2/blob/master/grub-core/kern/file.c">file.c</a> 中的 <code class="language-plaintext highlighter-rouge">grub_file_open()</code>，都会触发该过滤器。随后 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> 调用 <code class="language-plaintext highlighter-rouge">grub_tpm_measure()</code>，再由 <code class="language-plaintext highlighter-rouge">grub_cc_log_event()</code> 将文件二进制度量到 <strong>PCR[9]/RTMR[2]</strong>。</p> <p><em>16.</em> 启动动作 “Exit Boot Services Invocation”，表示 Boot Manager 已向 UEFI 发出结束 Boot Services 的调用（<strong>PCR[5]/RTMR[1]</strong>）。</p> <p><em>17.</em> 启动动作 “Exit Boot Services Returned with Success”，表示 UEFI 成功退出 Boot Services，pre-OS 环境已终止（<strong>PCR[5]/RTMR[1]</strong>）。</p> <p>ExitBootServices 动作由 TdTcg2Dxe.c 度量。成功时调用 <code class="language-plaintext highlighter-rouge">OnExitBootServices()</code>，失败时调用 <code class="language-plaintext highlighter-rouge">OnExitBootServicesFailed()</code>。</p> <h3 id="secure-boot-policy-在启动途中变更">Secure Boot Policy 在启动途中变更</h3> <p><em>18.</em> 平台必须重启，或把这些变量重新度量到 <strong>PCR[7]/RTMR[0]</strong>。另外，对任何 Secure Boot 变量的常规更新都应在 PCR[7] 的首次度量之前，或 <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 调用完成之后进行。</p> <p>UEFI Secure Boot 变量的更新在 Variable <a href="https://github.com/tianocore/edk2/tree/master/MdeModulePkg/Universal/Variable/RuntimeDxe"><code class="language-plaintext highlighter-rouge">RuntimeDxe</code></a> 中度量。如果上述任一 Secure Boot 相关变量被更新，<a href="https://github.com/tianocore/edk2/blob/master/MdeModulePkg/Universal/Variable/RuntimeDxe/Measurement.c">Measurement.c</a> 的 <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> 会以 <code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code> 度量新的数据。</p> <h2 id="远程证明">远程证明</h2> <p>从验证方视角看，远程证明不是“Quote 签名有效”这一项检查，而是一条三阶段流水线：先确认 Quote 的来源与完整性，再用其中的 MRTD/RTMR 校验事件日志，最后把已验证的事件解释为固件、Secure Boot、GRUB 和内核状态，并与策略及参考值比较。本文不展开 Quote 的生成和传输协议。</p> <h3 id="quote-校验">Quote 校验</h3> <p>TDX Quote 携带 TDREPORT 的内容以及支持验证的签名和认证材料。验证方需要检查签名、证书链、吊销状态、TCB 状态和防重放所需的新鲜性（freshness），并确认报告中的平台与 TD 属性满足策略；仅仅成功解析 Quote 并不代表工作负载可信。证明密钥及其认证材料在 IETF RATS 架构中属于背书材料（endorsements）的一部分。具体格式和校验流程见 <a href="https://github.com/intel/SGXDataCenterAttestationPrimitives">Intel SGX Data Center Attestation Primitives（DCAP）</a>。</p> <h3 id="事件日志回放">事件日志回放</h3> <p>事件日志回放是把原始事件日志反序列化后，利用其中的事件重新计算所有度量寄存器的值。每个事件包含一个摘要和度量寄存器索引。验证方创建一组模拟度量寄存器，对每个事件将其摘要扩展到对应的模拟寄存器中。最后将模拟寄存器的值与第一步中 quote 上报的真实度量寄存器值比对。</p> <p>Canonical 的 TDX 测试仓库提供了 Python 工具 <a href="https://github.com/canonical/tdx/tree/main/tests/lib/tdx-tools/src/tdxtools">tdx-tools</a>，可在 TD guest 中比较事件日志回放值和 TDREPORT 中的 RTMR。其 <code class="language-plaintext highlighter-rouge">VerifyActor</code> 展示了三步流程：</p> <ul> <li>从 CCEL ACPI 表获取 TD event log。</li> <li>通过 Linux attestation driver 获取 TDREPORT。</li> <li>对比 TDREPORT 中的 RTMR 值与事件日志回放得到的 RTMR 值。</li> </ul> <p>两者完全一致，只能证明事件日志与 TDREPORT 中的 RTMR 自洽；它不证明这些事件代表的组件符合验证方策略。后一个判断属于事件解析和参考值比较。下面摘出当前实现的核心路径：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">VerifyActor</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Actor to verify the RTMR
    </span><span class="sh">"""</span>

    <span class="k">def</span> <span class="nf">_verify_single_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">rtmr_value_1</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">,</span>
        <span class="n">rtmr_value_2</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>

        <span class="k">if</span> <span class="n">rtmr_value_1</span> <span class="o">==</span> <span class="n">rtmr_value_2</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] passed the verification.</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">error</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] did not pass the verification</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">verify_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Get TD report and RTMR replayed by event log to do verification.
        </span><span class="sh">"""</span>
        <span class="c1"># 1. Read CCEL from ACPI table at /sys/firmware/acpi/tables/CCEL
</span>        <span class="n">ccelobj</span> <span class="o">=</span> <span class="n">CCEL</span><span class="p">.</span><span class="nf">create_from_acpi_file</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">ccelobj</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">return</span>

        <span class="c1"># 2. Get the start address and length for event log area
</span>        <span class="n">td_event_log_actor</span> <span class="o">=</span> <span class="nc">TDEventLogActor</span><span class="p">(</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_start_address</span><span class="p">,</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_minimum_length</span><span class="p">)</span>

        <span class="c1"># 3. Collect event log and replay the RTMR value according to event log
</span>        <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>

        <span class="c1"># 4. Read TD REPORT via TDCALL.GET_TDREPORT
</span>        <span class="n">td_report</span> <span class="o">=</span> <span class="n">TdReport</span><span class="p">.</span><span class="nf">get_td_report</span><span class="p">()</span>

        <span class="c1"># 5. Verify individual RTMR value from TDREPORT and recalculated from
</span>        <span class="c1">#    event log
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">0</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_0</span><span class="sh">'</span><span class="p">])))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">1</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_1</span><span class="sh">'</span><span class="p">])))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">2</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">2</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_2</span><span class="sh">'</span><span class="p">])))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">3</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">3</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nb">bytearray</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">td_report</span><span class="p">[</span><span class="sh">'</span><span class="s">td_info</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">rtmr_3</span><span class="sh">'</span><span class="p">])))</span>
</code></pre></div></div> <h3 id="事件解析">事件解析</h3> <p>事件解析是从事件中提取信息的过程。验证方据此对照评估策略（appraisal policy）、背书材料和参考值（reference values）做出判断。</p> <p>以 <a href="https://github.com/google/go-eventlog/blob/main/extract/extract.go">go-eventlog</a> 为例，它可以解析、回放并从多种度量启动日志中提取信息，包括固件、Secure Boot 配置、GRUB 和 Linux kernel 状态。该库明确不负责 Quote 校验，也不提供参考度量值；集成方必须先验证 Quote，再把可信的寄存器值交给日志回放与解析逻辑。下面的 <code class="language-plaintext highlighter-rouge">FirmwareLogState</code> 代码展示了这类状态提取过程；具体 API 仍可能随该预发布库演进。</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// FirmwareLogState extracts event info from a verified TCG PC Client event</span>
<span class="c">// log into a FirmwareLogState.</span>
<span class="c">//</span>
<span class="c">// It is the caller's responsibility to ensure that the passed events have</span>
<span class="c">// been replayed (e.g., using `tcg.ParseAndReplay`) against a verified measurement</span>
<span class="c">// register bank.</span>
<span class="k">func</span> <span class="n">FirmwareLogState</span><span class="p">(</span><span class="n">events</span> <span class="p">[]</span><span class="n">tcg</span><span class="o">.</span><span class="n">Event</span><span class="p">,</span> <span class="n">hash</span> <span class="n">crypto</span><span class="o">.</span><span class="n">Hash</span><span class="p">,</span> <span class="n">registerCfg</span> <span class="n">registerConfig</span><span class="p">,</span> <span class="n">opts</span> <span class="n">Opts</span><span class="p">)</span> <span class="p">(</span><span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">var</span> <span class="n">joined</span> <span class="kt">error</span>
	<span class="n">tcgHash</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">tpm2</span><span class="o">.</span><span class="n">HashToAlgorithm</span><span class="p">(</span><span class="n">hash</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
	<span class="p">}</span>

	<span class="n">platform</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">PlatformExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">sbState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">SecureBootState</span><span class="p">(</span><span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">,</span> <span class="n">opts</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">efiState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">EfiState</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">,</span> <span class="n">opts</span><span class="p">)</span>

	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>

	<span class="k">var</span> <span class="n">grub</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">GrubState</span>
	<span class="k">var</span> <span class="n">kernel</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">LinuxKernelState</span>
	<span class="k">if</span> <span class="n">opts</span><span class="o">.</span><span class="n">Loader</span> <span class="o">==</span> <span class="n">GRUB</span> <span class="p">{</span>
		<span class="n">grub</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">GRUBExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>

		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
		<span class="n">kernel</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">LinuxKernelStateFromGRUB</span><span class="p">(</span><span class="n">grub</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="o">&amp;</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">{</span>
		<span class="n">Platform</span><span class="o">:</span>    <span class="n">platform</span><span class="p">,</span>
		<span class="n">SecureBoot</span><span class="o">:</span>  <span class="n">sbState</span><span class="p">,</span>
		<span class="n">Efi</span><span class="o">:</span>         <span class="n">efiState</span><span class="p">,</span>
		<span class="n">RawEvents</span><span class="o">:</span>   <span class="n">tcg</span><span class="o">.</span><span class="n">ConvertToPbEvents</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">),</span>
		<span class="n">Hash</span><span class="o">:</span>        <span class="n">pb</span><span class="o">.</span><span class="n">HashAlgo</span><span class="p">(</span><span class="n">tcgHash</span><span class="p">),</span>
		<span class="n">Grub</span><span class="o">:</span>        <span class="n">grub</span><span class="p">,</span>
		<span class="n">LinuxKernel</span><span class="o">:</span> <span class="n">kernel</span><span class="p">,</span>
		<span class="n">LogType</span><span class="o">:</span>     <span class="n">registerCfg</span><span class="o">.</span><span class="n">LogType</span><span class="p">,</span>
	<span class="p">},</span> <span class="n">joined</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="参考度量值">参考度量值</h3> <p>日志自洽之后，验证方仍需要回答“这些组件是不是预期版本”。这需要参考度量值或更高层的 Reference Integrity Manifest（RIM）。难点在于，最终寄存器值不仅取决于文件内容，还取决于加载后的表示、动态平台数据和事件顺序；不能把几个文件各自做一次 SHA-384 就当作完整参考值。</p> <p>Canonical 的 <a href="https://github.com/canonical/tdx/issues/263">#263</a> 记录了离线计算 MRTD/RTMR 时遇到的这些问题。该 issue 于 2026 年 7 月随原 Tech Preview 归档而关闭，并未给出适用于任意 GRUB 启动配置的通用算法。下面是一种作者设想，而不是现有工具已经保证的能力：受 cc-api 仓库中 <a href="https://github.com/cc-api/cvm-image-rewriter">cvm-image-rewriter</a> 插件架构启发，可以在镜像构建阶段增加 <code class="language-plaintext highlighter-rouge">reference-measurement-calculator</code>，提取 GRUB、kernel、initrd 和配置，并生成与该构建产物绑定的参考材料。</p> <p>cvm-image-rewriter 基于插件架构，用于定制机密计算 VM guest，包括 guest 镜像、配置、OVMF 固件等。现有插件如下表所示。</p> <table> <thead> <tr> <th>Name</th> <th>Descriptions</th> </tr> </thead> <tbody> <tr> <td>01-resize-image</td> <td>Resize the input qcow2 image</td> </tr> <tr> <td>02-motd-welcome</td> <td>Customize the login welcome message</td> </tr> <tr> <td>03-netplan</td> <td>Customize the netplan.yaml</td> </tr> <tr> <td>04-user-authkey</td> <td>Add auth key for user login instead of password</td> </tr> <tr> <td>05-readonly-data</td> <td>Fix some file permission to ready-only</td> </tr> <tr> <td>06-install-tdx-guest-kernel</td> <td>Install MVP TDX guest kernel</td> </tr> <tr> <td>07-device-permission</td> <td>Fix the permission for device node</td> </tr> <tr> <td>08-ccnp-uds-directory-permission</td> <td>Fix the permission for CCNP UDS directory</td> </tr> <tr> <td>60-initrd-update</td> <td>Update the initrd image</td> </tr> <tr> <td>97-sample</td> <td>plugin customization example</td> </tr> <tr> <td>98-ima-enable-simple</td> <td>Enable IMA (Integrity Measurement Architecture) feature</td> </tr> </tbody> </table> <p>该插件至少需要记录组件摘要、事件类型、目标寄存器和扩展顺序；若实际度量对象是加载后的内存表示，还必须复现对应加载逻辑。它生成的结果应视为特定构建和启动配置的参考材料，而不是天然适用于所有平台的 RTMR 终值。</p> <p>固件侧可以依据 TDVF Binary Layout 解析镜像并定位 BFV、CFV，具体实现可参考 Contrast 的 <a href="https://github.com/edgelesssys/contrast/blob/main/tools/tdx-measure/tdvf/tdvf.go">tdx-measure</a>。它展示了如何针对一组明确输入预计算部分度量，但其假设必须与实际 TDVF、QEMU 参数及启动路径一致。</p> <p>该工具还提供针对给定 firmware、kernel、initrd 和命令行的 RTMR 预计算入口（见 <code class="language-plaintext highlighter-rouge">newRtMrCmd()</code>）。在输入和事件顺序完全固定时，验证方可以直接比较预计算值与报告值；一旦存在动态 ACPI 数据、启动菜单选择或额外事件，这种比较就容易失效。事件日志因此仍是解释“哪个对象以什么顺序影响了寄存器”的关键证据。</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">newRtMrCmd</span><span class="p">()</span> <span class="o">*</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span> <span class="p">{</span>
	<span class="n">cmd</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span><span class="p">{</span>
		<span class="n">Use</span><span class="o">:</span>   <span class="s">"rtmr -f OVMF.fd -k bzImage [0|1|2|3]"</span><span class="p">,</span>
		<span class="n">Short</span><span class="o">:</span> <span class="s">"calculate the RTMR for a firmware and kernel file"</span><span class="p">,</span>
		<span class="n">Long</span><span class="o">:</span> <span class="s">`Calculate the RTMR for a firmware and kernel file.

		This will parse the firmware according to the TDX Virtual Firmware Design Guide
		and/or hash the kernel and pre-calculate a given RTMR.`</span><span class="p">,</span>
		<span class="n">Args</span><span class="o">:</span>      <span class="n">cobra</span><span class="o">.</span><span class="n">MatchAll</span><span class="p">(</span><span class="n">cobra</span><span class="o">.</span><span class="n">ExactArgs</span><span class="p">(</span><span class="m">1</span><span class="p">),</span> <span class="n">cobra</span><span class="o">.</span><span class="n">OnlyValidArgs</span><span class="p">),</span>
		<span class="n">ValidArgs</span><span class="o">:</span> <span class="p">[]</span><span class="kt">string</span><span class="p">{</span><span class="s">"0"</span><span class="p">,</span> <span class="s">"1"</span><span class="p">,</span> <span class="s">"2"</span><span class="p">,</span> <span class="s">"3"</span><span class="p">},</span>
		<span class="n">RunE</span><span class="o">:</span>      <span class="n">runRtMr</span><span class="p">,</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"f"</span><span class="p">,</span> <span class="s">"OVMF.fd"</span><span class="p">,</span> <span class="s">"path to firmware file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"fd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">,</span> <span class="s">"k"</span><span class="p">,</span> <span class="s">"bzImage"</span><span class="p">,</span> <span class="s">"path to kernel file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">,</span> <span class="s">"i"</span><span class="p">,</span> <span class="s">"initrd.zst"</span><span class="p">,</span> <span class="s">"path to initrd file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"cmdline"</span><span class="p">,</span> <span class="s">"c"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"kernel command line"</span><span class="p">)</span>
	<span class="k">return</span> <span class="n">cmd</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="附录">附录</h2> <p><span id="jumpA"></span></p> <h3 id="tdvf-configurations">TDVF Configurations</h3> <p><strong>Config-A</strong>：</p> <ul> <li>将<em>基础</em> TDVF 特性合入现有的 <code class="language-plaintext highlighter-rouge">OvmfPkgX64.dsc</code>。（与既有 SEV 对齐）</li> <li>威胁模型：VMM 不排除在 TCB 之外。（不改变现状）</li> <li><code class="language-plaintext highlighter-rouge">OvmfPkgX64.dsc</code> 包含 SEV/TDX/normal OVMF 的基本启动能力，最终二进制可在 SEV/TDX/normal OVMF 上运行。</li> <li>不修改 OvmfPkgX64 现有镜像布局。</li> <li>无需移除现有特性。</li> <li>TD 与 Non-TD 下均不跳过 PEI 阶段。</li> <li>支持基于 RTMR 的度量。</li> <li>度量来自 Host VMM 的外部输入，例如 TdHob、CFV。</li> <li>度量其他外部输入，例如 FW_CFG 数据、os loader、initrd 等。</li> </ul> <p><strong>构建 TDVF（Config-A）：</strong></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">.</span> ./edksetup.sh
make <span class="nt">-C</span> BaseTools
build <span class="nt">-p</span> OvmfPkg/OvmfPkgX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC <span class="se">\</span>
  <span class="nt">-D</span> <span class="nv">CC_MEASUREMENT_ENABLE</span><span class="o">=</span>TRUE <span class="nt">-b</span> RELEASE
</code></pre></div></div> <p><strong>Config-B</strong>：</p> <ul> <li>在 TDX 专用目录中新增独立的 <code class="language-plaintext highlighter-rouge">IntelTdxX64.dsc</code>，提供<em>完整</em>特性的 TDVF。（与既有 SEV 对齐）</li> <li>威胁模型：VMM 排除在 TCB 之外。（需要必要改动以防御来自 VMM 的攻击）</li> <li><code class="language-plaintext highlighter-rouge">IntelTdxX64.dsc</code> 包含 TDX/normal OVMF 的基本启动能力，最终二进制可在 TDX/normal OVMF 上运行。</li> <li>将来可能与 AmdSev.dsc 合并，但目前不合并；具体时间未知。只有 Intel 与 AMD 双方都认为各自方案足够成熟时，才会在社区中协调合并。</li> <li>必须新增必要的安全特性作为强制要求，例如基于 RTMR 的可信启动支持。</li> <li>必须度量来自 Host VMM 的外部输入，例如 TdHob、CFV。</li> <li>必须度量其他外部输入，例如 FW_CFG 数据、os loader、initrd 等。</li> <li>跳过 PEI，并拆分 DXE FV，使 TD guest 只加载需要的驱动，以缩小攻击面。</li> </ul> <p><strong>构建 TDVF（Config-B）：</strong></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">.</span> ./edksetup.sh
make <span class="nt">-C</span> BaseTools
build <span class="nt">-p</span> OvmfPkg/IntelTdx/IntelTdxX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC <span class="nt">-b</span> RELEASE
</code></pre></div></div> <p>以上配置说明和命令依据 EDK2 的 <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/IntelTdx/README.md">TDVF README</a>；构建前应以目标 EDK2 版本中的同一文件为准。</p> <p><span id="jumpB"></span></p> <h3 id="uefi-boot-phase">UEFI Boot Phase</h3> <p>UEFI 有六个主要启动阶段，每一阶段都是平台初始化过程中的关键环节。这些阶段合称 Platform Initialization（PI）。</p> <p><em>1.</em> Security（SEC）</p> <p>PI 的入口阶段，建立最初的执行环境和临时内存，并把平台启动信息交给 PEI。SEC 可以承载平台信任根，但这不等于 UEFI Secure Boot；后者主要在后续镜像加载时执行签名策略。</p> <p><em>2.</em> Pre-EFI Initialization（PEI）</p> <p>启动过程的第二阶段，仅使用 CPU 当前可用的资源调度 Pre-EFI Initialization Modules（PEIM）。PEIM 用于完成启动关键操作的初始化（例如内存初始化），并将控制权交给 Driver Execution Environment（DXE）。</p> <p><em>3.</em> Driver Execution Environment（DXE）</p> <p>DXE 阶段完成大部分系统初始化。DXE 运行所需的内存在 PEI 阶段分配并初始化；控制权交给 DXE 后，DXE Dispatcher 被触发，负责加载并执行硬件驱动、运行时服务以及 OS 启动所需的各类启动服务。</p> <p><em>4.</em> Boot Device Selection（BDS）</p> <p>DXE Dispatcher 执行完所有 DXE 驱动后，控制权交给 BDS。BDS 负责初始化控制台设备及其他尚未初始化的设备；随后加载并执行所选启动项（OS loader），为 Transient System Load（TSL）做准备。</p> <p><em>5.</em> Transient System Load（TSL）</p> <p>此阶段中，PI 流程位于启动项选择与交付给主操作系统之间。可以调用如 UEFI shell 之类的应用，或（更常见地）运行 bootloader 以准备最终 OS 环境。通常由 bootloader 通过 <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 终止 UEFI Boot Services；但 OS 本身也可以做此动作，例如启用 <code class="language-plaintext highlighter-rouge">CONFIG_EFI_STUB</code> 的 Linux kernel。</p> <p><em>6.</em> Runtime（RT）</p> <p>最后一个阶段，控制权正式交给 OS。兼容 UEFI 的 OS 接管系统。UEFI Runtime Services 仍对 OS 可用，例如用于从 NVRAM 查询和写入变量。</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[GRUB 启动的 TD 如何从固件一路度量到内核，以及验证方如何校验这条启动链。]]></summary></entry></feed>