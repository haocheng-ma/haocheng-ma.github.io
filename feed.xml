<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://mahaocheng.me/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mahaocheng.me/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-08T09:26:34+00:00</updated><id>https://mahaocheng.me/feed.xml</id><title type="html">blank</title><subtitle>Haocheng&apos;s academic webpage. </subtitle><entry><title type="html">virtCCA: Measured Boot and Attestation Demystified</title><link href="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/" rel="alternate" type="text/html" title="virtCCA: Measured Boot and Attestation Demystified"/><published>2026-01-21T00:00:00+00:00</published><updated>2026-01-21T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation</id><content type="html" xml:base="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/"><![CDATA[<p>💭 A quick note: virtCCA’s measured-boot and remote-attestation features landed in stages as the product was commercialized, scattering the code across components and fragmenting the documentation. This article consolidates the framework at the architectural level, so readers don’t have to dig through scattered older sources.</p> <h2 id="introduction">Introduction</h2> <p>Huawei virtCCA is a confidential-computing solution built on the TrustZone and Secure EL2 features of the Kunpeng processor. It brings the Arm CCA architectural concepts to existing hardware, giving applications and data a hardware-rooted, verifiable Trusted Execution Environment (TEE) that isolates them from unauthorized access or tampering at runtime.</p> <p>virtCCA bridges toward native CCA hardware while migrating business applications without modification. Until CCA hardware is widespread, it gives enterprises a practical option with high performance, strong security, and forward compatibility. See <a href="https://arxiv.org/abs/2306.11011">virtCCA: Virtualized Arm Confidential Compute Architecture with TrustZone</a> and the <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_19_0003.html">Confidential Computing TEE Suite Technical White Paper</a> for details.</p> <p>As Figure 1 shows, hardware isolation draws the trust boundary, and Measured Boot and Attestation make the state inside it cryptographically verifiable:</p> <ul> <li> <p>Measured Boot: following “measure before execute”, virtCCA hashes the TCB state during platform and CVM (Confidential VM) boot, then stores the results in tamper-resistant registers.</p> </li> <li> <p>Attestation: a hardware-protected Attestation Key signs the measurements to produce an Attestation Report, proving the CVM and its platform are genuine and untampered.</p> </li> </ul> <p>Together they build a chain of trust from the hardware Root of Trust (RoT) up to the CVM. A Relying Party can then verify remotely that a workload runs inside a CVM on a genuine virtCCA platform and meets the expected security baseline.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. Measured Boot and Attestation </div> <p>💡 TCB (Trusted Computing Base): the set of hardware, firmware, and software components that must be trusted to keep the system secure. It includes hardware (CPU, RoT), firmware (TF-A, TMM), and critical Guest OS code (UEFI, the GRUB bootloader, the OS kernel). If any TCB component is exploited or modified, the system’s confidentiality and integrity no longer hold.</p> <h2 id="measured-boot">Measured Boot</h2> <p>The virtCCA architecture consists of the CVM and the underlying virtCCA platform (hardware plus firmware) that hosts it — mirroring the Realm / CCA Platform split in the Arm CCA specification (see <a href="https://developer.arm.com/documentation/DEN0096/latest">Arm CCA Security Model 1.0</a>). Given this separation, end-to-end Measured Boot comes in two interlocking phases — Platform Measurement and CVM Measurement — shown in Figure 2. Only when the Platform Measurement verifies does the CVM Measurement it carries mean anything.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. Platform Measurement and CVM Measurement </div> <h3 id="platform-measurement">Platform Measurement</h3> <p>Platform Measurement follows a chained trust-transfer model. During hardware initialization BSBC acts as the Core Root of Trust for Measurement (CRTM); the HSM running on InSE acts as the Root of Trust for Storage (RTS) and the Root of Trust for Reporting (RTR), storing results in InSE’s protected internal SRAM (Figure 2). By boot order, virtCCA follows the Arm CCA firmware-boot specification (Trusted Subsystems → Application PE → Monitor → RMM). In practice, the functional-component boundaries differ from Arm CCA because of constraints in the existing Kunpeng hardware, as the table below shows.</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Measured Components</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Descriptions</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>BSBC</strong> (BootROM Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Secure boot code embedded in the on-chip ROM</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ESBC</strong> (External Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Secure boot code stored in off-chip non-volatile memory</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>InSE</strong> (Integrated Secure Element)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">An on-chip secure subsystem with physical-security protection</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>HSM</strong> (Hardware Security Module)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">High-security firmware running on InSE; provides key management, secure storage, and Platform-token generation</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMU</strong> (Intelligence Management Unit)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Intelligent management unit responsible for hardware management, monitoring, and secure boot</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IPU</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ATF</strong> (Arm Trusted Firmware)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Low-level secure firmware running at the highest exception level (EL3)</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>TMM</strong> (TrustZone Management Monitor)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">A lightweight secure-monitoring component responsible for the CVM lifecycle, memory isolation, and interface dispatch</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>UEFI</strong> (Unified Extensible Firmware Interface)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Platform boot firmware that initializes hardware, performs security checks, and boots the system</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMF AP</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> </tbody> </table> </div> <p>💡 RoTs are components that must be trusted because their misbehavior cannot be detected: (1) the Root of Trust for Measurement (RTM) measures software and forwards the results to the RTS; (2) the Root of Trust for Storage is access-controlled, tamper-resistant storage; (3) the Root of Trust for Reporting reports the RTS’s contents — measurement values — as an attestation report; (4) the CRTM is the first code executed after reset and anchors the chain of trust.</p> <h3 id="cvm-measurement">CVM Measurement</h3> <p>virtCCA supports two CVM boot methods (Figure 3): Direct Kernel Boot and Virtual Firmware Boot.</p> <ul> <li>Direct Kernel Boot: the Hypervisor (QEMU/KVM) loads the OS kernel and the initial RAM filesystem (initramfs) directly, bypassing the virtual firmware and bootloader.</li> <li>Virtual Firmware Boot: virtual firmware (UEFI) initializes hardware and loads a bootloader such as GRUB, which then loads the kernel and initramfs. We call this UEFI Boot in the rest of the article.</li> </ul> <p>Direct Kernel Boot starts fast with a small resource footprint, a good fit for lightweight container environments (e.g., Kata Container). UEFI Boot works with existing OS images, handles complex boot configurations such as multi-kernel and multi-boot management, and preserves the cloud-management experience of a regular VM — a good fit for general-purpose VMs (e.g., IaaS cloud instances).</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 3. Direct Kernel Boot and Firmware Boot </div> <p>Both paths follow the same security primitive: Host-provided assets are untrusted. Any input from the Hypervisor (code, data, configuration) must be measured by the TMM — or by an RTM it delegates to — before it is used. The measurements are then extended into specific TMM-protected registers. Per the <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a> (A7.1 Realm measurements), these are the RIM (Realm Initial Measurement) and the REM (Realm Event Measurement). The TMM computes the RIM directly, capturing the initial register and memory state; subsequent RTMs compute the REM — for example, UEFI measuring the GRUB image it loads.</p> <h4 id="direct-kernel-boot">Direct Kernel Boot</h4> <p>When a CVM boots via Direct Kernel Boot, virtCCA’s measurement logic follows the Arm CCA RMM specification. Since virtCCA’s TMM is closed-source, this section uses RMM terminology. The table below summarizes the three phases; for details, see B4.3.9.4 RMI_REALM_CREATE initialization of RIM, B4.3.12.4 RMI_REC_CREATE extension of RIM, and B4.3.1.4 RMI_DATA_CREATE extension of RIM in the <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a>.</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px; width: 20%;">Phase</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Description</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a Realm</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>CCA Realm &lt;-&gt; virtCCA CVM</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REALM_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_realm_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>flags</strong>: <code>lpa2</code> (LPA2 enabled), <code>sve</code> (SVE enabled), <code>pmu</code> (PMU enabled)</li> <li><strong>s2sz</strong>: requested IPA width</li> <li><strong>sve_vl</strong>: requested SVE vector length</li> <li><strong>num_bps</strong>: number of breakpoints</li> <li><strong>num_wps</strong>: number of watchpoints</li> <li><strong>pmu_num_ctrs</strong>: requested number of PMU counters</li> <li><strong>hash_algo</strong>: algorithm used to measure the initial state of the Realm</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a REC</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>REC is a execution context which is associated with Realm vCPU</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REC_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_rec_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>gprs</strong> (general-purpose registers): host provides the initial vaule of registers 0-7, others are zeros</li> <li><strong>pc</strong> (program counter): the pc of primary REC is set to <code>LOADER_START_ADDR</code> of Realm, others are zeros</li> <li><strong>flags</strong>: <code>runnable</code>, whether REC is eligible for execution, primary REC is set to eligible</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a Data Granule</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>Copies contents (e.g., kernel, initramfs) from a Non-secure Granule provided by the caller</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_DATA_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_data_granule_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>ipa</strong>: IPA at which the DATA Granule is mapped in the Realm</li> <li><strong>flags</strong>: whether to measure DATA Granule contents</li> <li><strong>content</strong>: hash of contents of DATA Granule, or zero if flags indicate DATA Granule contents are unmeasured</li> </ul> </td> </tr> </tbody> </table> </div> <h4 id="virtual-firmware-boot">Virtual Firmware Boot</h4> <p>For UEFI-firmware-booted CVMs, the Arm CCA measurement specification did not yet exist at the time, so virtCCA developed its own solution ahead of the standard. Unlike the fixed flow of Direct Kernel Boot, the UEFI boot chain uses diverse boot media and dynamic configuration, making its path highly non-deterministic. Drawing on the trusted-boot mechanisms of TCG and TDX, virtCCA built a deterministic, measurable, and auditable boot pipeline.</p> <p>🍵 For technical details on Intel TDX’s Trusted Boot, see this companion article: <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a>.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 4. Measurement in UEFI Boot </div> <p>As Figure 4 shows, although the UEFI boot combinations are many, the factors that affect the system’s security state are relatively fixed. virtCCA measures every security-sensitive element — boot variables, image files, and boot order — uniformly, and builds a reproducible, verifiable Event Log that covers any UEFI boot combination.</p> <p>Concretely, in the pre-kernel-load environment (spanning the UEFI and bootloader stages), all critical state is measured through EFI_CC_MEASUREMENT_PROTOCOL and extended into REM. The protocol also records each measurement into the CCEL (Confidential Computing Event Log) within the ACPI tables. A CVM can then verify REM by replaying the log, auditing the entire chain of trust end-to-end.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 5. Measured Boot Components in EDK2 </div> <p>Figure 5 shows virtCCA’s measurement components in UEFI firmware. EFI_CC_MEASUREMENT_PROTOCOL wraps the low-level measurement logic for confidential-computing environments. Per the <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html">UEFI Specification 2.10</a>, it provides hash computation and event-log APIs, and defines the semantic mapping from traditional TPM PCRs to virtCCA REMs (table below). The CcaTcg2Dxe.c driver handles measurement during the DXE phase; the DxeTpm2MeasureBootLib library handles integrity measurement of PE images and the GPT partition table. Source: <a href="https://atomgit.com/src-openeuler/edk2/blob/openEuler-24.03-LTS-SP2/edk2.spec">src-openeuler/edk2</a>, patches 97–106 (“Support measurement when UEFI Boot CVM”). For the flow, see the Measured Boot Flow section of <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a>.</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TPM PCR</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TDX</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">virtCCA/CCA</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Typical Usage of Measurement Registers</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">MRTD</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RIM</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Virtual firmware code (match PCR[0])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">1, 7</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[0]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Firmware configuration (match PCR[1,7])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">2~6</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[1]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM1</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Firmware loaded component, such as OS loader (match PCR[4,5])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">8~15</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[2]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM2</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">OS component, such as OS kernel, initrd, and application (match PCR[8~15]), the usage is OS dependent</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">16-...</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[3]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM3</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Reserved for special usage only</td> </tr> </tbody> </table> </div> <h2 id="attestation">Attestation</h2> <p>Under the Delegated Attestation model, several parties share the work of producing the attestation report. As Figure 2 shows, the virtCCA Token wraps two Sub-Tokens: the CVM Token and the Platform Token. The HSM in InSE generates the Platform Token; the TMM caches it in secure memory for performance. The TMM then generates the CVM Token and combines both into the virtCCA Token.</p> <h3 id="authenticity">Authenticity</h3> <p>Each generating entity signs its Sub-Token with a dedicated key, making the chain of trust traceable. The keys derive in a chain: the entity that produced the predecessor Sub-Token derives the next key. To block Sub-Token substitution attacks, Sub-Tokens are cryptographically bound through a “hash anchoring” mechanism — the predecessor Sub-Token’s Challenge embeds the hash of the successor Sub-Token’s public key.</p> <p>In virtCCA’s key hierarchy: CPAK signs the Platform Token, derived by the HSM from a root key and platform parameters; its public-key certificate is issued by the Huawei Root CA and Sub CA, giving a hardware-rooted identity proof. RAK signs the CVM Token, derived during TMM initialization by the HSM from the root key, the virtCCA platform measurement, and other parameters.</p> <p>🍵 Since the virtCCA TMM is a closed-source component, the details of how it interacts with the HSM to obtain the RAK and the Platform Token can be found in the <a href="https://trustedfirmware-m.readthedocs.io/projects/tf-m-extras/en/latest/partitions/delegated_attestation/delegated_attest_integration_guide.html">Delegated Attestation Service Integration Guide</a>.</p> <h3 id="attestation-flow">Attestation Flow</h3> <p>The <a href="https://gitcode.com/openeuler/virtCCA_sdk">virtCCA SDK</a> is a user-space toolkit for enabling and integrating remote attestation (Figure 6). Its companion <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/samples">Attestation Demo</a> shows the end-to-end flow: the Server runs inside the CVM and exposes the device certificate (CPAK) and the attestation report (virtCCA Token) through the TSI interface. The Client acts as a Local Verifier, parsing the certificate and report, validating the certificate chain, and evaluating the security policy.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 6. Component Details in virtCCA SDK </div> <p>🍵 For other features — for example Full Disk Encryption, see <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/full-disk-encryption">attestation/full-disk-encryption</a>; for virtCCA’s adaptation of <a href="https://github.com/inclavare-containers/rats-tls">RATS-TLS</a>, see <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/rats-tls">attestation/rats-tls</a>.</p> <p>The diagram below shows the Attestation Demo flow. For real-world deployment and usage, see the feature guide <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_16_0036.html">Enabling Remote Attestation</a>.</p> <div id="mermaid-target"></div> <script src="https://cdn.jsdelivr.net/npm/mermaid@11.12.2/dist/mermaid.min.js"></script> <script>document.addEventListener("DOMContentLoaded",function(){mermaid.initialize({startOnLoad:!1});const e="\nsequenceDiagram\n    autonumber\n    \n    participant C as Verifier (Client)\n    participant S as Attester (Server)\n    participant SDK as TSI SDK / Driver\n\n    Note over S, SDK: [ TEE / Confidential VM Boundary ]\n\n    rect rgb(240, 244, 255)\n        Note over C, SDK: Phase 1: Evidence & Identity Collection\n        C->>S: Request Identity Certificate (CPAK)\n        S->>SDK: get_dev_cert()\n        SDK-->>S: Return CPAK Certificate\n        S-->>C: Deliver CPAK Certificate\n\n        C->>C: Generate Fresh Nonce (Challenge)\n\n        C->>S: Request Attestation Evidence (virtCCA Token)\n        S->>SDK: get_attestation_token(nonce)\n        SDK-->>S: Return Signed Evidence\n        S-->>C: Deliver Signed Evidence\n\n        alt UEFI Boot\n            C->>S: Request Event Log (CCEL)\n            S-->>C: Send CCEL Data\n        end\n    end\n\n    rect rgb(255, 251, 235)\n        Note over C: Phase 2: Appraisal & Verification\n        Note right of C: Input: Reference Values, Endorsements, Appraisal Policy\n\n        C->>C: Verify Token Signature based on Endorsements\n        C->>C: Verify Token Claims based on Reference Values\n\n        alt Direct Kernel Boot\n            Note right of C: Match RIM against Reference Values\n        else UEFI Boot\n            Note right of C: Parse & Replay CCEL<br/>Compare Token REMs with Replayed REMs<br/>Extract & Verify Firmware States\n        end\n        \n        C->>C: Apply Appraisal Policy for Final Attestation Result\n        C->>S: Attestation Result (Standardized Claims regarding Trustworthiness)\n    end\n    ",n=document.getElementById("mermaid-target");mermaid.render("graph-id-1",e).then(({svg:e})=>{n.innerHTML=e})["catch"](e=>{console.error("Mermaid rendering failed:",e),n.innerHTML="<p style='color:red;'>Mermaid diagram failed to render \u2014 please check the console.</p>"})});</script> <h2 id="summary">Summary</h2> <p>Measured boot and remote attestation make for a great “visible trust” story, and everyone loves telling it. For engineers, though, the work really does span the stack — grinding from chip, firmware, and OS up through applications and infrastructure, with one eye on the standards and ecosystem the whole time. After all that, it can be hard to point at exactly where the value lands, and honestly the tech itself isn’t all that flashy, 🥲😅.</p> <p>Anyway, virtCCA has done some of this work, with plenty still to improve. Keep at it, CCA folks.</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[An exploration of the measurement and attestation framework within Huawei virtCCA.]]></summary></entry><entry xml:lang="zh"><title type="html">virtCCA: Measured Boot and Attestation Demystified</title><link href="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/zh/" rel="alternate" type="text/html" title="virtCCA: Measured Boot and Attestation Demystified"/><published>2026-01-21T00:00:00+00:00</published><updated>2026-01-21T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/virtcca-measured-boot-attestation.zh</id><content type="html" xml:base="https://mahaocheng.me/blog/2026/virtcca-measured-boot-attestation/zh/"><![CDATA[<p>💭 碎碎念：关于 virtCCA 的度量启动和远程证明，其特性随商业化进程分阶段落地，导致相关代码散落在各个组件中，文档也比较碎片化。趁此机会，本文将从架构层面进行统一的梳理与分析，希望能帮大家理清脉络，省去翻阅零散旧资料的周折。</p> <h2 id="introduction">Introduction</h2> <p>Huawei virtCCA 是基于鲲鹏处理器 TrustZone 与 Secure EL2 特性打造的机密计算方案。它在现有硬件上先行实践了 ARM CCA 架构理念，通过构建“基于硬件且可验证”的可行执行环境（TEE），为运行中的应用和数据提供硬件隔离，防止任何未经授权的访问或篡改。</p> <p>作为向原生 CCA 硬件平滑演进的桥梁，virtCCA 实现了业务应用的零改造迁移。它在 CCA 硬件大规模普及前，为企业提供了兼具高性能、高安全性与前瞻兼容性的实践方案。详见 <a href="https://arxiv.org/abs/2306.11011">virtCCA: Virtualized Arm Confidential Compute Architecture with TrustZone</a> 与 <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_19_0003.html">机密计算TEE套件技术白皮书</a>。</p> <p>如图 1 所示，硬件隔离机制界定了信任边界，度量启动（Measured Boot）与远程证明（Attestation）通过密码学方式赋予该边界可验证的状态可信：</p> <ul> <li> <p>度量启动：遵循“度量先于执行”的原则，在 virtCCA 平台和机密虚拟机（CVM） 的启动过程中，通过哈希度量记录 TCB 的完整性状态，并将度量结果存储到不可篡改寄存器中。</p> </li> <li> <p>远程证明：利用硬件保护的认证密钥（Attestation Key）对度量结果进行数字签名，生成具有证据效力的证明报告（Attestation Report），证明 CVM 及底层平台是真实、安全且未被篡改的。</p> </li> </ul> <p>两者协同配合，构建了从底层硬件信任根（RoT）到上层 CVM 的完整信任链，这使得依赖方（Relying Party）能够远程确信：目标负载运行在真实 virtCCA 平台的 CVM 内，且符合预期安全基准，从而满足机密计算的信任验证需求。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measurement-and-attestation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 1. Measured Boot and Attestation </div> <p>💡 TCB (Trusted Computing Base) ：为了维持系统安全性而必须被信任的所有硬件、固件及软件组件的集合。这既包含 CPU、RoT 等硬件和 TF-A、TMM 等固件，也涵盖 Guest OS 侧的关键代码，如 UEFI、Bootloader（Grub）以及操作系统内核（Kernel）。如果 TCB 中的任何一个组件出现了漏洞或被恶意篡改，整个系统的安全性（机密性、完整性）都将无法得到保证。</p> <h2 id="measured-boot">Measured Boot</h2> <p>virtCCA 架构由 CVM 及其底层支撑环境 virtCCA 平台（硬件与固件集合）共同构成，这在逻辑上映射了 Arm CCA 规范中 Realm 与 CCA Platform 的实体关系（参考 <a href="https://developer.arm.com/documentation/DEN0096/latest">Arm CCA Security Model 1.0</a> ）。基于这种实体解耦，完整的度量启动由相互衔接的平台度量和 CVM 度量组成，如图 2 所示，也只有当平台度量通过验证，其承载的 CVM 度量才具备实质性的安全意义。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/platfom-cvm-measurement.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 2. Platform Measurement and CVM Measurement </div> <h3 id="platform-measurement">Platform Measurement</h3> <p>平台度量遵循“链式信任传递”原则，在硬件初始化阶段，BSBC 充当了核心度量信任根 (CRTM) ，运行 HSM 的 InSE 则作为存储信任根（RTS）和报告信任根（RTR），确保度量结果存储于 InSE 内部受保护的 SRAM 中，如图 2 所示。在流程设计上，virtCCA 对标 Arm CCA 的固件引导规范（即 Trusted Subsystems → Application PE → Monitor → RMM）。但在具体的工程实现上，受限于现有鲲鹏硬件的实现架构，相关功能组件的划分与 Arm CCA 存在差异，如下表所示。</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Measured Components</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Descriptions</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>BSBC</strong> (BootROM Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">内置于芯片内 ROM 的安全启动代码</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ESBC</strong> (External Secure Boot Code)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">存储在芯片外非易失性存储的安全启动代码</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>InSE</strong> (Integrated Secure Element)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">芯片内置的安全子系统，具备物理安全防护能力</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>HSM</strong> (Hardware Security Module)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">运行在 InSE 上的高安固件，提供密钥管理、安全存储和 Platform token 生成等功能</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMU</strong> (Intelligence Management Unit)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">智能管理单元，用于硬件管理、监控和安全启动</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IPU</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>ATF</strong> (Arm Trusted Firmware)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">运行在最高特权级 (EL3) 的底层安全固件</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>TMM</strong> (TrustZone Management Monitor)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">轻量安全监控组件，负责 CVM 的生命周期、内存隔离和接口调用</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>UEFI</strong> (Unified Extensible Firmware Interface)</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">平台启动固件，负责初始化硬件、执行安全校验并引导系统</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"><strong>IMF AP</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; color: #888;"><em>TBA</em></td> </tr> </tbody> </table> </div> <p>💡 RoTs 是必须被信任的系统组件，因为无法检测它的非预期行为：（1）度量信任根（RTM）计算软件的度量值，并将度量值发送给 RTS；（2）存储信任根是具备访问控制且防篡改的安全存储；（3）报告信任根上报 RTS 的记录内容，e.g., 度量值，呈现形式是证明报告；（4）CRTM 是系统复位后执行的初始代码，构成信任链的起点。</p> <h3 id="cvm-measurement">CVM Measurement</h3> <p>针对不同的业务场景，virtCCA 为 CVM 提供了两类启动方式：直接内核启动（Direct Kernel Boot）和虚拟固件启动（Virtual Firmware Boot），如图 3 所示。</p> <ul> <li>直接内核启动：由 Hypervisor（QEMU/KVM）直接加载操作系统内核（kernel）和初始内存文件系统（initramfs），跳过虚拟固件和启动引导程序。</li> <li>虚拟固件启动：通过虚拟固件（UEFI）初始化硬件，加载启动引导程序（bootloader）如 GRUB，再由 bootloader 加载 kernel 和 initramfs，后文统称为 UEFI 启动。</li> </ul> <p>直接内核启动的优势是启动速度快、资源占用小，适用于轻量化容器环境（如 Kata Container）。UEFI 启动的优势是兼容现有 OS 镜像，支持多内核、多引导管理等复杂启动项，能保持与普通虚拟机一致的云管服务，适用于通用型虚拟机（如 IaaS 云实例）。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/direct-kernel-and-firmware-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 3. Direct Kernel Boot and Firmware Boot </div> <p>尽管引导路径各异，其底层均遵循统一的安全原语：Host-provided assets are untrusted。任何源自 Hypervisor 侧的输入（包括代码、数据及配置）在被执行或使用前，必须由 TMM 或其委托的 RTM 完成强制性度量。这些度量结果最终被扩展至受 TMM 保护的特定寄存器中。参考 <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a>（A7.1 Realm measurements）的定义，上述寄存器包括 RIM（Realm Initial Measurement）和 REM（Realm Event Measurement），其中 RIM 由 TMM 直接计算，记录初始的寄存器和内存状态，而 REM 则由后续 RTM 计算，例如 UEFI 度量加载的 GRUB。</p> <h4 id="direct-kernel-boot">Direct Kernel Boot</h4> <p>采用直接内核启动拉起 CVM 时，virtCCA 的度量逻辑遵循 Arm CCA 的 RMM 规范，由于 virtCCA 的 TMM 是闭源代码，本节将沿用 RMM 语义描述度量流程，下表给出了度量流程的三个阶段，细节可以参考 <a href="https://developer.arm.com/documentation/den0137/latest">Realm Management Monitor specification</a> 的 B4.3.9.4 RMI_REALM_CREATE initialization of RIM、B4.3.12.4 RMI_REC_CREATE extension of RIM 和 B4.3.1.4 RMI_DATA_CREATE extension of RIM。</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px; width: 20%;">Phase</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Description</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a Realm</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>CCA Realm &lt;-&gt; virtCCA CVM</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REALM_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_realm_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>flags</strong>: <code>lpa2</code> (LPA2 enabled), <code>sve</code> (SVE enabled), <code>pmu</code> (PMU enabled)</li> <li><strong>s2sz</strong>: requested IPA width</li> <li><strong>sve_vl</strong>: requested SVE vector length</li> <li><strong>num_bps</strong>: number of breakpoints</li> <li><strong>num_wps</strong>: number of watchpoints</li> <li><strong>pmu_num_ctrs</strong>: requested number of PMU counters</li> <li><strong>hash_algo</strong>: algorithm used to measure the initial state of the Realm</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a REC</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>REC is a execution context which is associated with Realm vCPU</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_REC_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_rec_params_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>gprs</strong> (general-purpose registers): host provides the initial vaule of registers 0-7, others are zeros</li> <li><strong>pc</strong> (program counter): the pc of primary REC is set to <code>LOADER_START_ADDR</code> of Realm, others are zeros</li> <li><strong>flags</strong>: <code>runnable</code>, whether REC is eligible for execution, primary REC is set to eligible</li> </ul> </td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px; vertical-align: top;"><strong>Creates a Data Granule</strong></td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;"> <div style="margin-bottom: 8px;"><em>Copies contents (e.g., kernel, initramfs) from a Non-secure Granule provided by the caller</em></div> <div style="margin-bottom: 8px;"><strong>Command:</strong> <code>RMI_DATA_CREATE</code></div> <div style="margin-bottom: 8px;"><strong>Function:</strong> <code>measurement_data_granule_measure()</code></div> <strong>Components:</strong> <ul style="margin: 4px 0 0 0; padding-left: 18px; line-height: 1.6;"> <li><strong>ipa</strong>: IPA at which the DATA Granule is mapped in the Realm</li> <li><strong>flags</strong>: whether to measure DATA Granule contents</li> <li><strong>content</strong>: hash of contents of DATA Granule, or zero if flags indicate DATA Granule contents are unmeasured</li> </ul> </td> </tr> </tbody> </table> </div> <h4 id="virtual-firmware-boot">Virtual Firmware Boot</h4> <p>针对基于 UEFI 固件启动的 CVM 场景，鉴于彼时 Arm CCA 的度量规范仍尚未定义，virtCCA 提前进行了技术预研与方案落地。不同于流程相对固定的直接内核启动，UEFI 启动链条涉及多样的启动介质与动态配置，其路径表现出较强的非确定性。virtCCA 参考了 TCG 与 TDX 的可信启动机制，构建了一套可确定、可度量、可审计的引导链路。</p> <p>🍵 关于 Intel TDX 的 Trusted Boot 技术细节，可参阅另外一篇文章 <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a>。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/uefi-measured-boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 4. Measurement in UEFI Boot </div> <p>如图 4 所示，尽管 UEFI 的启动组合路径繁杂，但影响系统安全状态的关键因素相对固定。virtCCA 将启动变量、镜像文件以及启动顺序等所有安全敏感状态进行统一度量，通过构建可复现、可验证的事件日志（Event Log），来覆盖任意的 UEFI 启动组合。</p> <p>具体而言，在内核载入前的预启动环境中（跨越 UEFI 与 bootloader 阶段），所有关键状态均通过 EFI_CC_MEASUREMENT_PROTOCOL 进行度量，并扩展到 REM 中。同时，该协议将每次度量操作记录至 ACPI 表中的 CCEL（Confidential Computing Event Log）。这种设计支持 CVM 通过日志重放机制验证 REM 的真实性，从而确保了整条信任链的端到端可审计。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/measured-boot-components-edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="80%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 5. Measured Boot Components in EDK2 </div> <p>virtCCA 在 UEFI 固件中的度量组件如图 5 所示。其中，EFI_CC_MEASUREMENT_PROTOCOL 封装了机密计算环境下的底层度量逻辑。遵循 <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html">UEFI Specification 2.10</a> 规范，该实例提供哈希计算和事件日志的能力，并确立了从传统的 TPM PCR 到 virtCCA REM 的语义映射（见下表）。CcaTcg2Dxe.c 驱动负责处理 DXE 阶段的度量事务，DxeTpm2MeasureBootLib 库则负责 PE 镜像及 GPT 分区表的完整性度量，代码可参阅 <a href="https://atomgit.com/src-openeuler/edk2/blob/openEuler-24.03-LTS-SP2/edk2.spec">src-openeuler/edk2</a> 的 patch97-patch106（Support measurement when UEFI Boot CVM），流程可参考 <a href="https://mahaocheng.me/blog/2025/tdx-measure-boot/">Intel TDX: Measured Boot and Attestation in Grub Boot</a> 的 Measured Boot Flow 章节。</p> <div style="overflow-x: auto;"> <table style="width: 100%; border-collapse: collapse; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 24px 0; border: 1px solid #e1e4e8; color: #24292e;"> <thead> <tr style="background-color: #f6f8fa; border-bottom: 2px solid #d1d5da;"> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TPM PCR</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">TDX</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">virtCCA/CCA</th> <th style="padding: 12px 16px; border: 1px solid #e1e4e8; text-align: left; font-size: 14px;">Typical Usage of Measurement Registers</th> </tr> </thead> <tbody> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">MRTD</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RIM</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Virtual firmware code (match PCR[0])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">1, 7</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[0]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM0</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Firmware configuration (match PCR[1,7])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">2~6</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[1]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM1</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Firmware loaded component, such as OS loader (match PCR[4,5])</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">8~15</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[2]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM2</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">OS component, such as OS kernel, initrd, and application (match PCR[8~15]), the usage is OS dependent</td> </tr> <tr> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">16-...</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">RTMR[3]</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">REM3</td> <td style="padding: 12px 16px; border: 1px solid #e1e4e8; font-size: 14px;">Reserved for special usage only</td> </tr> </tbody> </table> </div> <h2 id="attestation">Attestation</h2> <p>在 Delegated Attestation 模型下，证明报告的生成职责由多个参与方协同承担。如图 2 所示，virtCCA Token 封装了两个 Sub-Token（CVM Token 与 Platform Token）。其中，Platform Token 由 InSE 的 HSM 生成，出于性能考虑会缓存到 TMM 的安全内存；TMM 则在生成 CVM Token 基础上组合两者生成 virtCCA Token。</p> <h3 id="authenticity">Authenticity</h3> <p>为了确保信任链的可追溯性，每个 Sub-Token 均由对应生成实体使用专用密钥进行签名。这些密钥遵循链式派生逻辑：链中的每个密钥都由其前序 Sub-Token 的生成实体派生。为防止 Sub-Token 替换攻击，各个 Sub-Token 通过“哈希锚定”机制实现密码学绑定，在前序 Sub-Token 的 Challenge 中嵌入后序 Sub-Token 的公钥哈希。</p> <p>具体到 virtCCA 的密钥体系，CPAK 是 Platform Token 的签名密钥，由 HSM 基于根密钥及平台参数派生，其公钥证书由 Huawei Root CA 及 Sub CA 签发背书，提供了硬件级身份证明；RAK 是 CVM Token 的签名密钥，派生于 TMM 初始化阶段，由 HSM 结合根密钥和 virtCCA 平台度量值等参数生成。</p> <p>🍵 鉴于 virtCCA TMM 属于闭源组件，关于其与 HSM 交互以获取 RAK 和 Platform Token 的流程细节，可参阅 <a href="https://trustedfirmware-m.readthedocs.io/projects/tf-m-extras/en/latest/partitions/delegated_attestation/delegated_attest_integration_guide.html">Delegated Attestation Service Integration Guide</a>。</p> <h3 id="attestation-flow">Attestation Flow</h3> <p>virtCCA 提供了用户态工具包 <a href="https://gitcode.com/openeuler/virtCCA_sdk">virtCCA SDK</a>，用于简化远程证明的使能和集成，如图 6 所示。其配套的 <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/samples">Attestation Demo</a> 展示了端到端的证明逻辑：Server 端在 CVM 运行时环境里，利用 TSI 接口向上透传设备证书（CPAK 证书）与证明报告（virtCCA Token）。Client 端则充当 Local Verifier 角色，解析收到的证书与报告，进行证书链校验和安全策略评估。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" sizes="95vw"/> <img src="/assets/img/2026-01-21-virtcca-measured-boot-attestation/virtcca-sdk-components.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 6. Component Details in virtCCA SDK </div> <p>🍵 其他特性，例如 Full Disk Encryption 可参阅 <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/full-disk-encryption">attestation/full-disk-encryption</a>；virtCCA 对 <a href="https://github.com/inclavare-containers/rats-tls">RATS-TLS</a> 的适配可参阅 <a href="https://gitcode.com/openeuler/virtCCA_sdk/tree/master/attestation/rats-tls">attestation/rats-tls</a>。</p> <p>如下展示了 Attestation Demo 的具体流程，实际部署与使用请参阅 <a href="https://www.hikunpeng.com/document/detail/zh/kunpengcctrustzone/tee/cVMcont/kunpengtee_16_0036.html">使能远程证明</a> 的特性指南。</p> <div id="mermaid-target"></div> <script src="https://cdn.jsdelivr.net/npm/mermaid@11.12.2/dist/mermaid.min.js"></script> <script>document.addEventListener("DOMContentLoaded",function(){mermaid.initialize({startOnLoad:!1});const e="\nsequenceDiagram\n    autonumber\n    \n    participant C as Verifier (Client)\n    participant S as Attester (Server)\n    participant SDK as TSI SDK / Driver\n\n    Note over S, SDK: [ TEE / Confidential VM Boundary ]\n\n    rect rgb(240, 244, 255)\n        Note over C, SDK: Phase 1: Evidence & Identity Collection\n        C->>S: Request Identity Certificate (CPAK)\n        S->>SDK: get_dev_cert()\n        SDK-->>S: Return CPAK Certificate\n        S-->>C: Deliver CPAK Certificate\n\n        C->>C: Generate Fresh Nonce (Challenge)\n\n        C->>S: Request Attestation Evidence (virtCCA Token)\n        S->>SDK: get_attestation_token(nonce)\n        SDK-->>S: Return Signed Evidence\n        S-->>C: Deliver Signed Evidence\n\n        alt UEFI Boot\n            C->>S: Request Event Log (CCEL)\n            S-->>C: Send CCEL Data\n        end\n    end\n\n    rect rgb(255, 251, 235)\n        Note over C: Phase 2: Appraisal & Verification\n        Note right of C: Input: Reference Values, Endorsements, Appraisal Policy\n\n        C->>C: Verify Token Signature based on Endorsements\n        C->>C: Verify Token Claims based on Reference Values\n\n        alt Direct Kernel Boot\n            Note right of C: Match RIM against Reference Values\n        else UEFI Boot\n            Note right of C: Parse & Replay CCEL<br/>Compare Token REMs with Replayed REMs<br/>Extract & Verify Firmware States\n        end\n        \n        C->>C: Apply Appraisal Policy for Final Attestation Result\n        C->>S: Attestation Result (Standardized Claims regarding Trustworthiness)\n    end\n    ",n=document.getElementById("mermaid-target");mermaid.render("graph-id-1",e).then(({svg:e})=>{n.innerHTML=e})["catch"](e=>{console.error("Mermaid \u6e32\u67d3\u5931\u8d25:",e),n.innerHTML="<p style='color:red;'>Mermaid \u56fe\u8868\u6e32\u67d3\u5931\u8d25\uff0c\u8bf7\u68c0\u67e5\u63a7\u5236\u53f0\u62a5\u9519\u3002</p>"})});</script> <h2 id="summary">Summary</h2> <p>度量启动和远程证明这玩意儿，大家一直聊得挺热闹，谁都想讲好“看得见的信任”这个故事。但对技术人来说，确实是全方位的折腾：从最底下的芯片、固件、OS 一路磨到应用和基建，中间还得兼顾标准和生态。等真的忙活完一通，有时也说不清价值到底体现在哪，说到底技术本身也没多炫酷，🥲😅。</p> <p>总之，virtCCA 目前在这块做了一些工作，还有很多不完善的地方，大家 CCA 加油吧。</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[An exploration of the measurement and attestation framework within Huawei virtCCA.]]></summary></entry><entry><title type="html">Key Management in TPM based Security</title><link href="https://mahaocheng.me/blog/2025/tpm-based-security/" rel="alternate" type="text/html" title="Key Management in TPM based Security"/><published>2025-07-24T00:00:00+00:00</published><updated>2025-07-24T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tpm-based-security</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tpm-based-security/"><![CDATA[<p>A TPM (Trusted Platform Module) does not store full keys directly. Instead, it persists secret values called Seeds and derives keys from them deterministically. Two common seed types drive two key hierarchies: Endorsement and Storage.</p> <table> <thead> <tr> <th>Seed type</th> <th>Key hierarchy</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td>Endorsement Seed</td> <td>Endorsement Key (EK)</td> <td>Identifies the TPM</td> </tr> <tr> <td>Storage Seed</td> <td>Storage Root Key (SRK)</td> <td>Protects keys for local apps</td> </tr> </tbody> </table> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/key_management.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/key_management.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. Endorsement and Storage types in TPM key management </div> <p>TPM keys fall into two categories by use:</p> <ul> <li><strong>Restricted</strong>: sign or decrypt TPM-internal state and data. Examples include signing boot measurements and proving that two keys live in the same TPM. EK, SRK, and AIK are all restricted.</li> <li><strong>Non-Restricted</strong>: serve general purposes such as TLS client keys or document signing keys. Application keys fall in this category.</li> </ul> <h2 id="endorsement-key">Endorsement Key</h2> <p>The Endorsement Key (EK) is the TPM’s identity, provisioned by the TPM manufacturer.</p> <ul> <li>The EK key pair is generated deterministically by <code class="language-plaintext highlighter-rouge">TPM2_CreatePrimary()</code> from a known template (the EK Template), the Endorsement Primary Seed (EPS), and a chosen key derivation function (KDF).</li> <li>The EK certificate is issued by the TPM manufacturer’s CA and stored in TPM NVRAM.</li> <li>The EK certificate contains the EK public key along with fields such as the manufacturer name, component model, component version, and key ID.</li> </ul> <p>For the EK Template format, see <a href="https://trustedcomputinggroup.org/wp-content/uploads/TCG_IWG_EKCredentialProfile_v2p3_r2_pub.pdf#page=37">Default EK Template (TPMT_PUBLIC): RSA 2048 (Storage)</a>.</p> <p>The EK lives in a TPM Shielded Location. The private key never leaves the TPM; software can read only the public key.</p> <p>Two related storage concepts apply:</p> <ul> <li><strong>Isolated Locations</strong>: non-volatile storage with access control that rejects access from outside the system boundary.</li> <li><strong>Shielded Locations</strong>: tamper-resistant storage, typically used for secret values.</li> </ul> <h2 id="storage-root-key">Storage Root Key</h2> <p>The TPM’s storage hierarchy organizes keys as a tree:</p> <ul> <li>Keys such as the Attestation Key (AK) and application keys are created under this hierarchy.</li> <li>Their parent is the Storage Root Key (SRK), a root inside the TPM that protects and derives child keys.</li> <li>Key attestation for these keys is performed by the EK.</li> </ul> <p>In other words, key attestation builds a chain of trust by signing: each link carries trust from the EK down to a specific key such as an AK.</p> <h2 id="attestation-key">Attestation Key</h2> <p>The Attestation Key (AK) signs data produced inside the TPM. It has three defining properties:</p> <ul> <li><strong>Non-duplicable</strong>: the TPM generates the AK internally and never exposes key material. The private key cannot be exported or copied.</li> <li><strong>Restricted</strong>: the AK signs or decrypts only TPM-produced data, such as PCR (Platform Configuration Register) values. It cannot sign or encrypt arbitrary data, which prevents misuse as a general-purpose key.</li> <li><strong>Signing key for device identity (DevID)</strong>: the AK public key certificate can serve as a device credential, issued by the manufacturer or another trusted party.</li> </ul> <p><em>“The 802.1AR standard defines a secure device identifier (DevID) as ‘an identifier that is cryptographically bound to a device’.”</em></p> <p>Device identities created during manufacturing are called IDevID/IAK. Their credentials last for the device’s lifetime. Identities created during deployment or use are called LDevID/LAK, and their credentials are removed at device reset or end of life.</p> <p><em>“Credential is a combination of a private key and a certificate.”</em></p> <p>The Attestation Identity Key (AIK) is a TPM 1.2 key type. AIKs perform only two signing operations:</p> <ul> <li><strong>TPM_Quote</strong>: signs PCR values with the AIK to produce a hardware report of device identity and current state. Remote attestation uses this to prove a device’s trustworthy state to a remote verifier.</li> <li><strong>TPM_CertifyKey</strong>: produces a signed statement that another key (not the AIK itself) resides in the TPM’s key hierarchy and is non-migratable. The certified key must actually have these properties.</li> </ul> <h2 id="credential-activation">Credential Activation</h2> <p>Credential activation lets a remote party establish trust in a new key such as the IAK, which can then encrypt or attest on the device’s behalf. It proves two things:</p> <ul> <li> <p>The IAK (Identity Attestation Key) has the expected key attributes:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">FlagFixedTPM</span>             <span class="c1">// Key cannot be exported; it must stay in the TPM.</span>
<span class="n">FlagSensitiveDataOrigin</span>  <span class="c1">// Key was generated inside the TPM, not imported.</span>
</code></pre></div> </div> </li> <li> <p>The IAK resides in the same TPM as the EK.</p> </li> </ul> <p><em>Credential activation allows a remote party to verify one key is on the same TPM as another key, and that the key has a specific set of properties.</em></p> <p>The flow is shown below. The activation key is the IAK; the anchor key is the EK.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. The flow of credential activation </div> <ul> <li>The client sends the IAK public key, the EK public key, and the IAK attributes to the server.</li> <li> <p>The server checks the IAK attributes against policy, generates a secret, and builds a challenge:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Challenge = ENC{secret} | aes_key1
            ENC{key1}  | aes_key2
            ENC{seed}  | EK_pub
</code></pre></div> </div> <p><code class="language-plaintext highlighter-rouge">aes_key1</code> is a randomly generated symmetric key. <code class="language-plaintext highlighter-rouge">aes_key2 = KDF(seed, IAK name)</code>, where KDF is a key derivation function and IAK name is the hash of the IAK public key blob (which includes attributes). All sensitive data is encrypted with the EK public key, so only the target TPM can decrypt it.</p> </li> <li>The client passes the challenge to the TPM via the <code class="language-plaintext highlighter-rouge">ActivateCredential</code> command.</li> <li>The TPM can decrypt and return the secret only when both the matching EK and IAK reside inside it. A successful decryption proves that the IAK exists in this TPM and carries the correct attributes.</li> </ul> <h2 id="reference">Reference</h2> <ol> <li><a href="">https://ericchiang.github.io/post/tpm-keys/</a></li> <li><a href="">https://tpm2-software.github.io/tpm2-tss/getting-started/2019/12/18/Remote-Attestation.html</a></li> <li><a href="">https://github.com/google/go-attestation/blob/master/docs/credential-activation.md</a></li> <li><a href="">https://github.com/tpm2dev/tpm.dev.tutorials</a></li> </ol>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[TPMs are ridiculously complex.]]></summary></entry><entry xml:lang="zh"><title type="html">Key Management in TPM based Security</title><link href="https://mahaocheng.me/blog/2025/tpm-based-security/zh/" rel="alternate" type="text/html" title="Key Management in TPM based Security"/><published>2025-07-24T00:00:00+00:00</published><updated>2025-07-24T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tpm-based-security/tpm-based-security.zh</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tpm-based-security/zh/"><![CDATA[<p>TPM（可信平台模块）本质上并不会直接存储完整的密钥，而是持久化存储称为 Seed（种子）的秘密值，并通过这些 Seed 确定性地派生出各种密钥。两类常见的 Seed 和对应的密钥体系是 Endorsement 和 Stroage，如下所示：</p> <table> <thead> <tr> <th>Seed 类型</th> <th>密钥体系</th> <th>用途简述</th> </tr> </thead> <tbody> <tr> <td>Endorsement Seed</td> <td>Endorsement Key（EK）</td> <td>识别 TPM 的密钥</td> </tr> <tr> <td>Storage Seed</td> <td>Storage Root Key（SRK）</td> <td>本地应用使用的密钥</td> </tr> </tbody> </table> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/key_management.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/key_management.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 1. Endorcement and Storage types in TPM Key Management </div> <p>上述密钥按照用途分为两类：Restricted 和 Non-Restricted。</p> <ul> <li>Restricted：用于签名或者解密 TPM 的状态和数据，例如，签名启动度量值、证明某个密钥位于同一个 TPM，EK、SRK、AIK 都在此类；</li> <li>Non-Restricted：用于一般用途，例如，TLS 客户端的密钥、签名普通文档的密钥，应用密钥（Application Key）属于此类。</li> </ul> <h2 id="endorsement-key">Endorsement Key</h2> <p>Endorsement Key（EK）是 TPM 的身份标识，由 TPM 制造商配置。</p> <ul> <li>EK 公私钥对由已知模板（EK Template）、Endorsement Primary Seed（EPS）和选定的密钥派生函数（KDF）、通过 <code class="language-plaintext highlighter-rouge">TPM2_CreatePrimary()</code> 确定性生成；</li> <li>EK 证书由TPM制造商的证书颁发机构（CA）签发并将其存储到 TPM NVRAM；</li> <li>EK 证书包含EK公钥以及其他字段，例如，TPM 制造商名称、组件型号、组件版本、密钥 ID 等。</li> </ul> <p>EK Template 的具体格式可查阅：<a href="https://trustedcomputinggroup.org/wp-content/uploads/TCG_IWG_EKCredentialProfile_v2p3_r2_pub.pdf#page=37">Default EK Template (TPMT_PUBLIC): RSA 2048 (Storage)</a></p> <p>EK 被存储在 TPM Shielded Location，私钥永远不能暴露，公钥可从 TPM 内读取。</p> <p>关于 TPM 的安全存储有两个概念：</p> <ul> <li>Isolated Locations：安全系统的非易失性存储，具有访问控制能力，不允许来自系统边界外的访问；</li> <li>Shielded Locations：具有防篡改能力的 Shielded Location，通常用于存储秘密值；</li> </ul> <h2 id="storage-root-key">Storage Root Key</h2> <p>在 TPM 的 Storage 体系结构中，密钥的创建遵循树状层级结构：</p> <ul> <li>比如 Attestation Key（AK）或应用密钥，都是在 TPM 的存储体系下生成的；</li> <li>这些密钥的父密钥是 SRK（Storage Root Key），SRK 是 TPM 中的一个根密钥，用于保护和派生其他密钥；</li> <li>而这些密钥的 key attestation（密钥认证） 则由 EK（Endorsement Key） 来实现。</li> </ul> <p>换句话说，Key attestation 是一种通过签名链建立信任的过程，核心目的是构建一条从 EK 一直到某个具体密钥（如 AK）的信任链。</p> <h2 id="attestation-key">Attestation Key</h2> <p>Attestation Key（AK）是一种用于对 TPM 内部数据进行签名的密钥，具有以下关键特性：</p> <ul> <li>non-duplicable（不可复制）：AK 是由 TPM 内部直接生成的，并且密钥材料不会暴露给 TPM 外部，这确保了其私钥无法被导出或复制，增强了安全性。</li> <li>Restricted（受限用途）：AK 只能用于对 TPM 生成的数据进行签名或解密，例如 PCR（平台配置寄存器）值。它不能用于任意数据的签名或加密，以防止其被滥用为通用密钥。</li> <li>签名密钥，用于设备身份（DevID）：AK 的公钥证书可以作为设备的身份凭据，由制造商或受信任方签发。</li> </ul> <p><em>“The 802.1AR standard defines a secure device identifier (DevID) as “an identifier that is cryptographically bound to a device”.”</em></p> <p>设备制造商在制造阶段创建的设备身份称为 IDevID/IAK，IDevID “Credential” 在设备整个生命周期内使用。用户在部署/应用阶段创建的设备身份称为 LDevID/LAK，LDevID “Credential” 会在设备复位或寿命结束时被移除。</p> <p><em>“Credential is a combination of a private key and a certificate.”</em></p> <p>Attestation Identity Key（AIK）是 TPM 1.2 中的一种密钥类型，AIK 仅被允许执行两种基于签名的操作：</p> <ul> <li>TPM_Quote：使用 AIK 对 PCR 寄存器值进行签名，生成代表设备身份和当前状态的硬件报告，常用于远程证明场景，也就是向远端证明设备当前的可信状态。</li> <li>TPM_CertifyKey：生成一个签名声明，用以证明另一个密钥（不是 AIK 本身）位于 TPM 的密钥存储体系中，且不可迁移（non-migratable）。显然，所认证的那个密钥必须实际具备这些属性。</li> </ul> <h2 id="credential-activation">Credential Activation</h2> <p>Credential activation 能够远程地建立对新密钥（例如，IAK）的信任，该密钥随后可以代表设备进行加密或证明，需要证明如下两点：</p> <ul> <li> <p>IAK（Identity Attestation Key）具备预期的密钥属性，例如：</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">FlagFixedTPM</span>             <span class="c1">// 密钥不能被导出，必须留在 TPM 中；</span>
<span class="n">FlagSensitiveDataOrigin</span>  <span class="c1">// 密钥必须由 TPM 内部生成，而不是导入；</span>
</code></pre></div> </div> </li> <li> <p>IAK 确实与 EK 存在于相同的 TPM 中。</p> </li> </ul> <p><em>Credential activation allows a remote party to verify one key is on the same TPM as another key, and that the key has a specific set of properties.</em></p> <p>大概的流程如下：Activation key 代指 IAK，Anchor key 代指 EK。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" sizes="95vw"/> <img src="/assets/img/2025-07-24-tpm-based-security/credential_activation.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 2. The Flow of Credential Activation </div> <ul> <li>Client 将 IAK 的公钥、EK 的公钥和 IAK 的密钥属性发送给 Server；</li> <li> <p>Server 验证 IAK 的属性是否满足安全策略、生成秘密值并构造 Challenge，形式如下，然后返回给 Client：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Challenge = ENC{secret} | aes_key1
            ENC{key1}  | aes_key2
            ENC{seed}  | EK_pub
</code></pre></div> </div> <p>其中，<code class="language-plaintext highlighter-rouge">aes_key1</code> 是随机生成的对称密钥；<code class="language-plaintext highlighter-rouge">aes_key2 = KDF(seed, IAK name)</code>：KDF 是密钥派生函数，IAK name 是 IAK 公钥 Blob（包含属性）的哈希；所有敏感数据都用 EK 公钥加密，确保只能由目标 TPM 解密。</p> </li> <li>Client 将 Challenge 发送给 TPM，调用 <code class="language-plaintext highlighter-rouge">ActivateCredential</code> 命令；</li> <li>TPM 内部只有在拥有与 Challenge 匹配的 EK 和 IAK 的前提下，才能正确解密并返回秘密值。成功解密即意味着：IAK 存在于该 TPM 中；IAK 拥有正确的密钥属性。</li> </ul> <h2 id="reference">Reference</h2> <ol> <li><a href="">https://ericchiang.github.io/post/tpm-keys/</a></li> <li><a href="">https://tpm2-software.github.io/tpm2-tss/getting-started/2019/12/18/Remote-Attestation.html</a></li> <li><a href="">https://github.com/google/go-attestation/blob/master/docs/credential-activation.md</a></li> <li><a href="">https://github.com/tpm2dev/tpm.dev.tutorials</a></li> </ol>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[TPMs are ridiculously complex.]]></summary></entry><entry><title type="html">Intel TDX: Measured Boot and Attestation in Grub Boot</title><link href="https://mahaocheng.me/blog/2025/tdx-measure-boot/" rel="alternate" type="text/html" title="Intel TDX: Measured Boot and Attestation in Grub Boot"/><published>2025-01-12T00:00:00+00:00</published><updated>2025-01-12T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tdx-measure-boot</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tdx-measure-boot/"><![CDATA[<p>Intel Trust Domain Extension (TDX) allows people to deploy hardware-isolated virtual machines (VMs) called trust domains (TDs). A TD VM is isolated from the virtual machine manager (VMM), hypervisor, and other non-TD software on the host platform. The entire memory contents of a TD is encrypted using a multiple-key encryption method. Intel TDX excludes the host platform from the TD’s trusted computing base (TCB).</p> <p>Intel TDX workloads are primarily VM images that tenants want to run as TDs in a cloud environment. Special Open Virtual Machine Firmware (OVMF) supplied by the CSP is required to run a VM in a TD, such as TDX Virtual Firmware (TDVF). TDVF is an EDK2 based project to enable UEFI support for TDX based Virtual Machines. It provides the capability to launch a TD. TDVF allows two configurations with different features, please see <a href="#jumpA">Appendix: TDVF Configurations</a> from <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/IntelTdx/README.md">Configurations and Features</a>.</p> <p>During TD boot, the hash-chained measurement on TCB will be extended to some secure registers (also known as measured boot). The values from several secure registers construct to a report and are finally signed to be a quote by an attestation key. Before providing data to the workload, a relying party uses attestation to verify that the software is running within a TD on a genuine Intel TDX system and at the specified security level.</p> <p><em>NOTE: Here TCB refers to all of a system’s hardware, firmware, and software components that provide a secure environment, including hardware information such as CPU, SEAM firmware, and guest components such as OVMF, bootloader (shim/grub), and kernel.</em></p> <p><strong>The above is a brief description of Intel TDX. In the upcoming series of articles, we will focus on TD’s measured boot and attestation.</strong></p> <h2 id="td-boot-types">TD Boot Types</h2> <p>The following diagram illustrates the TD boot type and boot process.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 1. TD Guest Boot Process </div> <p><strong>Direct boot</strong> is a boot process where the system boots directly into the OS without an intermediate boot loader. It can also be referred to as direct kernel boot with firmware, in which firmware loads kernel and initrd through the FwCfg device provided by Qemu.</p> <p><strong>Grub boot</strong> involves using the Grub bootloader, which provides advanced boot menu options, allowing you to select different operating systems and customize boot configurations. It can also be referred to as firmware-only boot, in which firmware launches a bootloader from a disk image.</p> <p>The detailed boot flow for different TD boot methods can be found in Figure 2.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 2. Detailed Flow for Different TD Boot </div> <p><strong>Today we will be diving into Measured Boot in Grub Boot.</strong></p> <p>In the virtual firmware, i.e., OVMF, the image handler <code class="language-plaintext highlighter-rouge">DxeTpmMeasureBootHandler</code> will be triggered when loading EFI image via <code class="language-plaintext highlighter-rouge">CoreLoadImageCommon()</code>. The <code class="language-plaintext highlighter-rouge">DxeTpmMeasureBootHandler</code> measures the objects like FV, QEMU CFG, VMM Hob, Variable into measurement registers (MRs). Then in boot loader ShimX64.efi, <code class="language-plaintext highlighter-rouge">TpmMeasureVariable()</code> measures the secure boot’s certificates into MRs. If secure boot is not enabled, ShimX64.efi may be absent from the VM image. Finally, boot loader GrubX64.efi measures kernel binary and cmdline, initrd binary, and grub’s module into MRs.</p> <p>Yes, the process here is draws on the TCG trusted boot chain. The difference is that it does not trust the hypervisor and avoids using mutable non-volatile storage (causing MR change). Futhermore, its trust chain can trace back to the TDX enable hardware.</p> <h2 id="uefi-boot-sequence">UEFI Boot Sequence</h2> <p>UEFI allows the extension of platform firmware by loading UEFI driver and UEFI application images. When UEFI drivers and UEFI applications are loaded they have access to all UEFI-defined runtime and boot services. See the Booting Sequence Figure 3 below.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 3. UEFI Booting Sequence </div> <p>UEFI allows the consolidation of boot menus from the OS loader and platform firmware into a single platform firmware menu. These platform firmware menus will allow the selection of any UEFI OS loader from any partition on any boot medium that is supported by UEFI boot services.</p> <p>The OS loader operates as a UEFI application, utilizing UEFI services via Boot Services (BS) and Runtime Services (RT). It then invokes <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> to terminate BS and release its resources, with only RT remaining available to the OS.</p> <p>A detailed introduction to UEFI boot phases refers to <a href="#jumpB">Appendix: UEFI Boot Phase</a> from <a href="https://secret.club/2020/05/26/introduction-to-uefi-part-1.html#uefi-boot-phases">UEFI boot phases</a>.</p> <h2 id="measured-boot-component">Measured Boot Component</h2> <p>See Figure 4, the pre-boot environment before the kernel includes the TDVF/OVMF phase and the bootloader phase (shim and grub). The whole boot chain will be measured into Runtime Measurement Register (RTMR) via <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code>.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 4. TD Measurement Process </div> <p>Similar to the TCG event log, <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> logs the events into confidential computing event log (CCEL) ACPI table and the measurement hash is extended to the corresponding RTMR register. The event logs in CCEL table can be replayed within a TD guest to verify the RTMR value.</p> <p>The Measured Boot Component in EDK2 is as follows.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">     Figure 5. Measured Boot Component in EDK2 </div> <p>The <a href="https://github.com/tianocore/edk2/tree/master/OvmfPkg/IntelTdx/TdxHelperLib"><code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code></a> library provides measurement functions in SEC phase. The <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Protocol/CcMeasurement.h"><code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code></a> protocol abstracts the confidential computing (CC) measurement operation in UEFI guest environment. The <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/Tcg/TdTcg2Dxe/TdTcg2Dxe.c">TdTcg2Dxe.c</a> DXE driver handles the DXE phase measurement. The <a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeTpm2MeasureBootLib"><code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootLib</code></a> library handles the PE image measurements and GPT measurement. All event type definition can be found at <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">UefiTcgPlatform.h</code></a>.</p> <p>Here we give a description of <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> from <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html#">Confidential Computing in UEFI Specification</a>.</p> <p>If a virtual firmware with CC capability supports measurement, the virtual firmware should produce <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> with new GUID <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL_GUID</code> to report event log and provide hash capability.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define EFI_CC_MEASUREMENT_PROTOCOL_GUID  \
  { 0x96751a3d, 0x72f4, 0x41a6, { 0xa7, 0x94, 0xed, 0x5d, 0x0e, 0x67, 0xae, 0x6b }}
</span><span class="k">extern</span> <span class="n">EFI_GUID</span>  <span class="n">gEfiCcMeasurementProtocolGuid</span><span class="p">;</span>

<span class="cp"># Protocol Interface Structure
</span><span class="k">typedef</span> <span class="k">struct</span> <span class="n">_EFI_CC_MEASUREMENT_PROTOCOL</span> <span class="p">{</span>
  <span class="n">EFI_CC_GET_CAPABILITY</span>          <span class="n">GetCapability</span><span class="p">;</span>
  <span class="n">EFI_CC_GET_EVENT_LOG</span>           <span class="n">GetEventLog</span><span class="p">;</span>
  <span class="n">EFI_CC_HASH_LOG_EXTEND_EVENT</span>   <span class="n">HashLogExtendEvent</span><span class="p">;</span>
  <span class="n">EFI_CC_MAP_PCR_TO_MR_INDEX</span>     <span class="n">MapPcrToMrIndex</span><span class="p">;</span>
<span class="p">}</span> <span class="n">EFI_CC_MEASUREMENT_PROTOCOL</span><span class="p">;</span>
</code></pre></div></div> <p>This protocol defines four parameters in the above interface structure, including:</p> <ul> <li>GetCapability provides protocol capability information and state information.</li> <li>GetEventLog allows a caller to retrieve the address of a given event log and its last entry.</li> <li>HashLogExtendEvent provides callers with an opportunity to extend and optionally log events without requiring knowledge of actual CC command.</li> <li>MapPcrToMrIndex provides callers information on TPM PCR to CC MR mapping.</li> </ul> <p><strong>Mapping for Intel TDX</strong></p> <p>The following table shows the TPM Platform Configuration Register (PCR) index mapping and CC event log MR index interpretation for Intel TDX, where MRTD means Trust Domain Measurement Register and RTMR means Runtime Measurement Register. There certainly are fewer TD MRs than TPM PCRs. They are typically mapped as below:</p> <table> <thead> <tr> <th>TPM PCR Index</th> <th>Typical Usage of Measurement Registers</th> <th>TDX MR Index</th> </tr> </thead> <tbody> <tr> <td>0</td> <td>FirmwareCode (BFV, including init page table)</td> <td>MRTD</td> </tr> <tr> <td>1</td> <td>FirmwareData (CFV, TD Hob, ACPI Table, Boot Variable)</td> <td>RTMR[0]</td> </tr> <tr> <td>2</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>3</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>4</td> <td>OS loader code</td> <td>RTMR[1]</td> </tr> <tr> <td>5</td> <td>GUID partition table (GPT)</td> <td>RTMR[1]</td> </tr> <tr> <td>6</td> <td>N/A</td> <td>N/A</td> </tr> <tr> <td>7</td> <td>Secure Boot Configuration</td> <td>RTMR[0]</td> </tr> <tr> <td>8~15</td> <td>TD OS measurement</td> <td>RTMR[2]</td> </tr> </tbody> </table> <p><em>NOTE: RTMR[3] is reserved for special usage, such as virtual TPM. Users have the flexibility to utilize RTMR[3] if it is not required for these specialized purposes.</em></p> <p>The typical usage of MRTD and RTMR is shown below, more detailes could be found in <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">8.1 Measurement Register Usage in TD</a>.</p> <ul> <li>MRTD is for the TDVF code (match PCR[0]).</li> <li>RTMR[0] is for the TDVF configuration (match PCR[1,7]). The usage should follow TCG Platform Firmware Profile (PFP) specification.</li> <li>RTMR[1] is for the TDVF loaded component, such as OS loader (match PCR[4,5]). The usage should follow TCG Platform Firmware Profile (PFP) specification.</li> <li>RTMR[2] is for the OS component, such as OS kernel, initrd, and application (match PCR[8~15]). The usage is OS dependent.</li> <li>RTMR[3] is reserved for special usage only.</li> </ul> <h2 id="measured-boot-flow">Measured Boot Flow</h2> <p>In general, the transitions (dotted line in Booting Sequence figure) where events are measured. Hence a trusted chain, i.e., virtual firmware -&gt; bootloaders -&gt; OS -&gt; applications will be built.</p> <p><strong>Measure TdHob and Configuration FV (Cfv)</strong></p> <p><a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">4.2 TD Hand-Off Block (HOB)</a> and <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">3.2 Configuration Firmware Volume (CFV)</a> are external data provided by Host VMM. These are not trusted in TD guest. So they should be validated, measured and extended to TD RTMR registers. In the meantime 2 <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT_HOB</code> are created. These 2 GUIDed HOBs carry the hash value of TdHobList and Configuration FV. In DXE phase <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT</code> can be created based on these 2 GUIDed HOBs.</p> <p>Configuration Firmware Volume includes all the provisioned data. This region is read only. One possible usage is to provide UEFI Secure Boot Variable content in this region, such as PK, KEK, db, dbx.</p> <p>The TD HOB list is used to pass the information from VMM to TDVF. The TD HOB must include PHIT HOB, Resource Descriptor HOB. Other HOBs are optional.</p> <ul> <li>The TD HOB must include PHIT HOB as the first HOB. EfiMemoryTop, EfiMemoryBottom, EfiFreeMemoryTop, and EfiFreeMemoryBottom shall be zero.</li> <li>The TD HOB must include at least one Resource Description HOB to declare the physical memory resource.</li> </ul> <p><strong><em>0.</em></strong> <code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code> is the SEC instance of TdxHelperLib. It implements the following functions for tdx in SEC phase:</p> <ul> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureTdHob</code> measure/extend TdHob and store the measurement value in workarea.</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureCfvImage</code> measure/extend the Configuration FV image and store the measurement value in workarea.</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperBuildGuidHobForTdxMeasurement</code> builds GuidHob for TDX measurement.</li> </ul> <p><strong>Measure Secure Boot Policy to PCR[7]/RTMR[0]</strong></p> <p><em>1.</em> UEFI Debug Mode.</p> <p>If a platform provides a firmware debugger mode, then the platform shall measure “UEFI Debug Mode” string with <code class="language-plaintext highlighter-rouge">EV_EFI_ACTION</code>. This logic is done at TdTcg2Dxe.c <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code>, based upon <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/SecurityPkg.dec"><code class="language-plaintext highlighter-rouge">PcdFirmwareDebuggerInitialized</code></a>.</p> <p><em>2.</em> The contents of the <code class="language-plaintext highlighter-rouge">SecureBoot</code> variable.</p> <p><em>3.</em> The contents of the <code class="language-plaintext highlighter-rouge">PK</code> variable.</p> <p><em>4.</em> The contents of the <code class="language-plaintext highlighter-rouge">KEK</code> variable.</p> <p><em>5.</em> The contents of the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> variable (the DB).</p> <p><em>6.</em> The contents of the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE1</code> variable (the DBX).</p> <p><em>7.</em> The contents of the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE2</code> (the DBT) variable, if present and not empty.</p> <p>The UEFI secure boot related variables – “SecureBoot”, “PK”, “KEK”, “db”, and “dbx” are unconditionally measured by TdTcg2Dxe.c <code class="language-plaintext highlighter-rouge">ReadAndMeasureSecureVariable()</code>. The event type is <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code></a>. If they are not present, a zero size UEFI variable entry will be measured. The “dbt” and “dbr” variables are conditionally measured only if they are present by the routine <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code>.</p> <p><em>8.</em> Separator.</p> <p><a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_SEPARATOR</code></a> for PCR7 is handled in TdTcg2Dxe.c <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code> when the UEFI variable is ready. It is just after <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code>. It is earlier than the <code class="language-plaintext highlighter-rouge">ReadyToBoot</code> event signal. The reason is that the PCR7 <code class="language-plaintext highlighter-rouge">EV_SEPARATOR</code> must be between SecureBootPolicy (Configure) and and ImageVerification (Authority).</p> <p><strong>Measure Boot Variable to PCR[1]/RTMR[0]</strong></p> <p><em>9.</em> The UEFI BootOrder Variable and the Boot#### variables (just device paths).</p> <p>The UEFI boot related variables, such as “BootOrder.” and “Boot####” are measured by TdTcg2Dxe.c <code class="language-plaintext highlighter-rouge">ReadAndMeasureBootVariable()</code>. The event type is <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_BOOT</code></a>. These variables are measured if they are present in <code class="language-plaintext highlighter-rouge">MeasureAllBootVariables()</code>.</p> <p><strong>Upon selecting a boot device,</strong></p> <p><em>10.</em> The boot attempt action “Calling EFI Application from Boot Option”, this means Boot Manager attempting to execute code from a Boot Option (<strong>PCR[4]/RTMR[1]</strong>).</p> <p>The boot attempt action is measured by TdTcg2Dxe.c <code class="language-plaintext highlighter-rouge">OnReadyToBoot()</code>. Before invoking a boot option, it measures the action "Calling EFI Application from Boot Option". After the boot option returns, it measures the action "Returning from EFI Application from Boot Option".</p> <p><em>11.</em> Separator, Draw a line between leaving pre-boot env and entering post-boot env (<strong>PCR[0~6]/RTMR[1]</strong>).</p> <p><em>12.</em> <strong>[Optional] If UEFI Secure Boot is enabled,</strong> measure the entry in the <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> that was used to validate the UEFI image (<strong>PCR[7]/RTMR[0]</strong>).</p> <p>When UEFI secure boot is enabled, the <a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeImageVerificationLib"><code class="language-plaintext highlighter-rouge">DxeImageVerificationLib</code></a> verifies the PE image signature based upon the <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code></a> in the <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_LIST</code></a> of an image signature database. If an <code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code> is used to verify the image, then this <code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code> will be measured with <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_AUTHORITY</code></a> in <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> of <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeImageVerificationLib/Measurement.c">Measurement.c</a>.</p> <p><em>13.</em> The GUID Partition Table (GPT) disk geometry (<strong>PCR[5]/RTMR[1]</strong>).</p> <p>When a system boots a boot option in a GUID-named partition of the disk, the GUID partition table (GPT) disk geometry needs to be measured. It is done by <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeTpm2MeasureBootLib/DxeTpm2MeasureBootLib.c">DxeTpm2MeasureBootLib.c</a> <code class="language-plaintext highlighter-rouge">Tcg2MeasureGptTable()</code> in <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code>.</p> <p><em>14.</em> The selected UEFI application code PE/COFF image, i.e., OS loader (<strong>PCR[4]/RTMR[1]</strong>).</p> <p>A third party UEFI application, such as a UEFI shell utility, a standard OS loader or an OEM boot option, is measured by DxeTpm2MeasureBootLib.c <code class="language-plaintext highlighter-rouge">Tcg2MeasurePeImage()</code> in <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code>. The event type is <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_BOOT_SERVICES_APPLICATION</code></a>. If a UEFI application is an FV which is dispatched in the DXE phase, it is also measured to PCR4 irrespective of whether the FV is measured or unmeasured.</p> <p><strong>Then OS loader, i.e., <a href="https://git.savannah.gnu.org/cgit/grub.git/">Grub2</a> extends the trusted boot chain from virtual firmware into the OS.</strong></p> <p><em>NOTE: Here we do not concern <a href="https://github.com/rhboot/shim">Shim</a> component since we only focus on measured boot. Shim is used to extend the UEFI secure boot concept to Linux.</em></p> <p><em>15.</em> Grub2 measures configuration file (e.g., grub.cfg), grub commands, kernel binary, kernel commands and initrd binary (<strong>PCR[8, 9]/RTMR[2]</strong>). PCR[8] is for the command line string and PCR[9] is for a file binary, as shown in the following table.</p> <p>To support measurements on confidential computing platforms, two patches have been upstreamed, including:</p> <ul> <li><a href="https://git.savannah.gnu.org/cgit/grub.git/commit/?id=4c76565b6cb885b7e144dc27f3612066844e2d19">efi/tpm: Add EFI_CC_MEASUREMENT_PROTOCOL support</a></li> <li><a href="https://git.savannah.gnu.org/cgit/grub.git/commit/?id=86df79275d065d87f4de5c97e456973e8b4a649c">commands/efi/tpm: Re-enable measurements on confidential computing platforms</a></li> </ul> <table> <thead> <tr> <th>PCR Index</th> <th>PCR Usage</th> </tr> </thead> <tbody> <tr> <td>8</td> <td>Grub command line: All executed commands (including those from configuration files) will be logged and measured as entered with a prefix of “grub cmd:”</td> </tr> <tr> <td> </td> <td>Kernel command line: Any command line passed to a kernel will be logged and measured as entered with a prefix of “kernel cmdline:”</td> </tr> <tr> <td> </td> <td>Module command line: Any command line passed to a kernel module will be logged and measured as entered with a prefix of “module cmdline:”</td> </tr> <tr> <td>9</td> <td>Files: Any file read by GRUB will be logged and measured with a descriptive text corresponding to the filename.</td> </tr> </tbody> </table> <p><a href="https://github.com/rhboot/grub2/blob/master/grub-core/commands/tpm.c">tpm.c</a> registers <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> and <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> to a grub_file_verifier structure. They will be called by <code class="language-plaintext highlighter-rouge">grub_verify_string()</code> and <code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> in <a href="https://github.com/rhboot/grub2/blob/master/grub-core/commands/verifiers.c">verifiers.c</a>.</p> <p>when grub2 executes a command line such as <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_MODULE_CMDLINE</code>, <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_KERNEL_CMDLINE</code>, <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_COMMAND</code> or <code class="language-plaintext highlighter-rouge">grub_create_loader_cmdline()</code> in <a href="https://github.com/rhboot/grub2/blob/master/grub-core/lib/cmdline.c">cmdline.c</a>, <code class="language-plaintext highlighter-rouge">grub_verify_string()</code> is used. Finally, <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> calls <code class="language-plaintext highlighter-rouge">grub_tpm_measure</code> and then <code class="language-plaintext highlighter-rouge">grub_cc_log_event</code> to measure the string to <strong>PCR[8]/RTMR[2]</strong>.</p> <p><code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> is registered as one of grub_file_filters in <a href="https://github.com/rhboot/grub2/blob/master/include/grub/file.h"><code class="language-plaintext highlighter-rouge">file.h</code></a>. Whenever grub uses <a href="https://github.com/rhboot/grub2/blob/master/grub-core/kern/file.c">file.c</a> <code class="language-plaintext highlighter-rouge">grub_file_open()</code> this filter is invoked. Finally, <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> calls <code class="language-plaintext highlighter-rouge">grub_tpm_measure</code> and then <code class="language-plaintext highlighter-rouge">grub_cc_log_event</code> to measure the file binary to <strong>PCR[9]/RTMR[2]</strong>.</p> <p><em>16.</em> The boot attempt action “Exit Boot Services Invocation”, this means Boot Manager has sent the call to UEFI to end Boot Services (<strong>PCR[5]/RTMR[1]</strong>).</p> <p><em>17.</em> The boot attempt action “Exit Boot Services Returned with Success”, this means UEFI successfully existed Boot Services and pre-OS environment has been terminated (<strong>PCR[5]/RTMR[1]</strong>).</p> <p>The ExitBootServices action is measured by TdTcg2Dxe.c. If ExitBootServices succeeds, then <code class="language-plaintext highlighter-rouge">OnExitBootServices()</code> is invoked. If ExitBootServices fails, then <code class="language-plaintext highlighter-rouge">OnExitBootServicesFailed()</code> is invoked.</p> <p><strong>If Security Boot Policy update after initial measurement and before <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> has completed,</strong></p> <p><em>18.</em> The platform MAY be restarted OR the variables MUST be remeasured into (<strong>PCR[7]/RTMR[0]</strong>). Additionally the normal update process for setting any of the defined Secure Boot variables SHOULD occur before the initial measurement in PCR[7] or after the call to <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> has completed.</p> <p>The UEFI secure boot variable update is measured in Variable <a href="https://github.com/tianocore/edk2/tree/master/MdeModulePkg/Universal/Variable/RuntimeDxe"><code class="language-plaintext highlighter-rouge">RuntimeDxe</code></a>. If any of the above secure boot related variables are updated, then <a href="https://github.com/tianocore/edk2/blob/master/MdeModulePkg/Universal/Variable/RuntimeDxe/Measurement.c">Measurement.c</a> <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> will measure the new data with <code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code>.</p> <h2 id="attestation">Attestation</h2> <p>We will analyse how to verify the TD Quote from the perspective of the verifier, without focusing on the Quote generation or interactions with the attester. At a high level, it can be broken down into Quote Verification, Event Log Replay and Event Parsing.</p> <h3 id="quote-verification">Quote Verification</h3> <p>A measured boot root of trust (RoT) can issue a report of the MRs, often called a quote or attestation report. This quote is typically a digitally-signed digest of MRs.</p> <p>A verifier then checks that the digest of MRs are signed by a trustworthy key, the RoT for Reporting (RTR). This RTR, aka attestation key, is typically a certified key that signs a report of the MRs. This certification is known as an Endorsement in the IETF RATS Architecture. More details about quote verification could be found in <a href="https://github.com/intel/SGXDataCenterAttestationPrimitives">Intel SGX-based Data Center Attestation Primitives (DCAP)</a>.</p> <h3 id="event-log-replay">Event Log Replay</h3> <p>Event log replay involves deserializing a raw event log and using the events to recalculate all of the measurement registers. Each event contains a digest and a measurement register index. The verifier will create simulated measurement registers and, for each event, extend the event digest into its corresponding simulated register. At the end, the verifier compares the simulated register values against the actual quoted measurement register values from the first step.</p> <p>Intel provides a Python library called <a href="https://github.com/canonical/tdx/tree/main/tests/lib/tdx-tools/src/tdxtools">tdx-tools</a> to verify TD measurements. It consists of the following three steps. For the step-by-step flow, please refer to class <code class="language-plaintext highlighter-rouge">VerifyActor</code> defined in tdx-tools.</p> <ul> <li>Get TD event log from CCEL ACPI table.</li> <li>Get TDREPORT via Linux attestation driver.</li> <li>Compare RTMR value from TDREPORT and RTMR value replayed via event log.</li> </ul> <p>The two values are expected to be identical, which means the measured contents are not tampered with.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">VerifyActor</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Actor to verify the RTMR
    </span><span class="sh">"""</span>

    <span class="k">def</span> <span class="nf">_verify_single_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">rtmr_value_1</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">,</span>
        <span class="n">rtmr_value_2</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>

        <span class="k">if</span> <span class="n">rtmr_value_1</span> <span class="o">==</span> <span class="n">rtmr_value_2</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] passed the verification.</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">error</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] did not pass the verification</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">verify_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Get TD report and RTMR replayed by event log to do verification.
        </span><span class="sh">"""</span>
        <span class="c1"># 1. Read CCEL from ACPI table at /sys/firmware/acpi/tables/CCEL
</span>        <span class="n">ccelobj</span> <span class="o">=</span> <span class="n">CCEL</span><span class="p">.</span><span class="nf">create_from_acpi_file</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">ccelobj</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">return</span>

        <span class="c1"># 2. Get the start address and length for event log area
</span>        <span class="n">td_event_log_actor</span> <span class="o">=</span> <span class="nc">TDEventLogActor</span><span class="p">(</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_start_address</span><span class="p">,</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_minimum_length</span><span class="p">)</span>

        <span class="c1"># 3. Collect event log and replay the RTMR value according to event log
</span>        <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>

        <span class="c1"># 4. Read TD REPORT via TDCALL.GET_TDREPORT
</span>        <span class="n">td_report</span> <span class="o">=</span> <span class="n">TdReport</span><span class="p">.</span><span class="nf">get_td_report</span><span class="p">()</span>

        <span class="c1"># 5. Verify individual RTMR value from TDREPORT and recalculated from
</span>        <span class="c1">#    event log
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">0</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_0</span><span class="p">)))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">1</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_1</span><span class="p">)))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">2</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">2</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_2</span><span class="p">)))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">3</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">3</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_3</span><span class="p">)))</span>
</code></pre></div></div> <h3 id="event-parsing">Event Parsing</h3> <p>Event parsing is the process of pulling information from the events. The verifier uses the output to make verification decisions against the appraisal policy, endorsements, and reference values.</p> <p>In <a href="https://github.com/google/go-eventlog">go-eventlog</a>, <code class="language-plaintext highlighter-rouge">ReplayAndExtract</code> parses a CC event log and replays the parsed event log against the RTMR bank specified by hash (the second step). It then extracts event info from the verified log into a FirmwareLogState. In this FirmwareLogState, includes details about the firmware, secure boot configuration, bootloaders (such as grub) and kernel (also includes command line and initramfs).</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// FirmwareLogState extracts event info from a verified TCG PC Client event</span>
<span class="c">// log into a FirmwareLogState.</span>
<span class="c">//</span>
<span class="c">// It is the caller's responsibility to ensure that the passed events have</span>
<span class="c">// been replayed (e.g., using `tcg.ParseAndReplay`) against a verified measurement</span>
<span class="c">// register bank.</span>
<span class="k">func</span> <span class="n">FirmwareLogState</span><span class="p">(</span><span class="n">events</span> <span class="p">[]</span><span class="n">tcg</span><span class="o">.</span><span class="n">Event</span><span class="p">,</span> <span class="n">hash</span> <span class="n">crypto</span><span class="o">.</span><span class="n">Hash</span><span class="p">,</span> <span class="n">registerCfg</span> <span class="n">registerConfig</span><span class="p">,</span> <span class="n">opts</span> <span class="n">Opts</span><span class="p">)</span> <span class="p">(</span><span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">var</span> <span class="n">joined</span> <span class="kt">error</span>
	<span class="n">tcgHash</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">tpm2</span><span class="o">.</span><span class="n">HashToAlgorithm</span><span class="p">(</span><span class="n">hash</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
	<span class="p">}</span>

	<span class="n">platform</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">PlatformExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">sbState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">SecureBootState</span><span class="p">(</span><span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">efiState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">EfiState</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">)</span>

	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>

	<span class="k">var</span> <span class="n">grub</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">GrubState</span>
	<span class="k">var</span> <span class="n">kernel</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">LinuxKernelState</span>
	<span class="k">if</span> <span class="n">opts</span><span class="o">.</span><span class="n">Loader</span> <span class="o">==</span> <span class="n">GRUB</span> <span class="p">{</span>
		<span class="n">grub</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">GRUBExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>

		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
		<span class="n">kernel</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">LinuxKernelStateFromGRUB</span><span class="p">(</span><span class="n">grub</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="o">&amp;</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">{</span>
		<span class="n">Platform</span><span class="o">:</span>    <span class="n">platform</span><span class="p">,</span>
		<span class="n">SecureBoot</span><span class="o">:</span>  <span class="n">sbState</span><span class="p">,</span>
		<span class="n">Efi</span><span class="o">:</span>         <span class="n">efiState</span><span class="p">,</span>
		<span class="n">RawEvents</span><span class="o">:</span>   <span class="n">tcg</span><span class="o">.</span><span class="n">ConvertToPbEvents</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">),</span>
		<span class="n">Hash</span><span class="o">:</span>        <span class="n">pb</span><span class="o">.</span><span class="n">HashAlgo</span><span class="p">(</span><span class="n">tcgHash</span><span class="p">),</span>
		<span class="n">Grub</span><span class="o">:</span>        <span class="n">grub</span><span class="p">,</span>
		<span class="n">LinuxKernel</span><span class="o">:</span> <span class="n">kernel</span><span class="p">,</span>
	<span class="p">},</span> <span class="n">joined</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="reference-measurements">Reference Measurements</h3> <p>The remaining topic is a quite complex challenge: how to generate reference measurements for FirmwareLogState. Now it is still a open issue <a href="https://github.com/canonical/tdx/issues/263">Calculate measurements outside of TDX #263</a> in the tdx repo. Inspired by a tool called <a href="https://github.com/cc-api/cvm-image-rewriter">cvm-image-rewriter</a> in cc-api repo, we may design a plugin to calculate reference measurements of the VM image.</p> <p>The cvm-image-rewriter tool is plugin-based and used to customize the confidential VM guest including guest image, config, OVMF firmware etc. The following table shows some existing plugins.</p> <table> <thead> <tr> <th>Name</th> <th>Descriptions</th> </tr> </thead> <tbody> <tr> <td>01-resize-image</td> <td>Resize the input qcow2 image</td> </tr> <tr> <td>02-motd-welcome</td> <td>Customize the login welcome message</td> </tr> <tr> <td>03-netplan</td> <td>Customize the netplan.yaml</td> </tr> <tr> <td>04-user-authkey</td> <td>Add auth key for user login instead of password</td> </tr> <tr> <td>05-readonly-data</td> <td>Fix some file permission to ready-only</td> </tr> <tr> <td>06-install-tdx-guest-kernel</td> <td>Install MVP TDX guest kernel</td> </tr> <tr> <td>07-device-permission</td> <td>Fix the permission for device node</td> </tr> <tr> <td>08-ccnp-uds-directory-permission</td> <td>Fix the permission for CCNP UDS directory</td> </tr> <tr> <td>60-initrd-update</td> <td>Update the initrd image</td> </tr> <tr> <td>97-sample</td> <td>plugin customization example</td> </tr> <tr> <td>98-ima-enable-simple</td> <td>Enable IMA (Integrity Measurement Architecture) feature</td> </tr> </tbody> </table> <p>After customising the VM image, a plugin (possibly named reference-measurement-calculator) can read the grub and kernel, then compute the digest of the images and configurations. Therefore reference measurements of VM image is obtained for final verification.</p> <p>Next question is how to get reference measurements about the firmware. According to TDVF Binary Layout, we can parse the virtual firmware, locate the BFV and CFV, and then compute their reference measurements. Here, we recommend referring to this tool named <a href="https://github.com/edgelesssys/contrast/blob/main/tools/tdx-measure/tdvf/tdvf.go">tdx-measure</a> in contrast repo.</p> <p>The tool also attempts another attestation approach. It does RTMR precalulation for given firmware and kernel file (see <code class="language-plaintext highlighter-rouge">newRtMrCmd()</code> function), and compare RTMR value from TDREPORT and pre-computed RTMR value. This improves verification efficiency by eliminating the need for the event log. It is important to note that this method is suitable for relatively fixed scenarios. The event logs are still necessary in most scenarios, because firmware, bootloaders, OS and applications are free to append measurements for any event, in any order.</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">newRtMrCmd</span><span class="p">()</span> <span class="o">*</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span> <span class="p">{</span>
	<span class="n">cmd</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span><span class="p">{</span>
		<span class="n">Use</span><span class="o">:</span>   <span class="s">"rtmr -f OVMF.fd -k bzImage [0|1|2|3]"</span><span class="p">,</span>
		<span class="n">Short</span><span class="o">:</span> <span class="s">"calculate the RTMR for a firmware and kernel file"</span><span class="p">,</span>
		<span class="n">Long</span><span class="o">:</span> <span class="s">`Calculate the RTMR for a firmware and kernel file.

		This will parse the firmware according to the TDX Virtual Firmware Design Guide
		and/or hash the kernel and pre-calculate a given RTMR.`</span><span class="p">,</span>
		<span class="n">Args</span><span class="o">:</span>      <span class="n">cobra</span><span class="o">.</span><span class="n">MatchAll</span><span class="p">(</span><span class="n">cobra</span><span class="o">.</span><span class="n">ExactArgs</span><span class="p">(</span><span class="m">1</span><span class="p">),</span> <span class="n">cobra</span><span class="o">.</span><span class="n">OnlyValidArgs</span><span class="p">),</span>
		<span class="n">ValidArgs</span><span class="o">:</span> <span class="p">[]</span><span class="kt">string</span><span class="p">{</span><span class="s">"0"</span><span class="p">,</span> <span class="s">"1"</span><span class="p">,</span> <span class="s">"2"</span><span class="p">,</span> <span class="s">"3"</span><span class="p">},</span>
		<span class="n">RunE</span><span class="o">:</span>      <span class="n">runRtMr</span><span class="p">,</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"f"</span><span class="p">,</span> <span class="s">"OVMF.fd"</span><span class="p">,</span> <span class="s">"path to firmware file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"fd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">,</span> <span class="s">"k"</span><span class="p">,</span> <span class="s">"bzImage"</span><span class="p">,</span> <span class="s">"path to kernel file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">,</span> <span class="s">"i"</span><span class="p">,</span> <span class="s">"initrd.zst"</span><span class="p">,</span> <span class="s">"path to initrd file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"cmdline"</span><span class="p">,</span> <span class="s">"c"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"kernel command line"</span><span class="p">)</span>
	<span class="k">return</span> <span class="n">cmd</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="appendix">Appendix</h2> <p><span id="jumpA"></span></p> <h3 id="tdvf-configurations">TDVF Configurations</h3> <p><strong>Config-A</strong>:</p> <ul> <li>Merge the <em>basic</em> TDVF feature to existing <code class="language-plaintext highlighter-rouge">OvmfX64Pkg.dsc</code>. (Align with existing SEV)</li> <li>Threat model: VMM is NOT out of TCB. (We don’t make things worse)</li> <li>The <code class="language-plaintext highlighter-rouge">OvmfX64Pkg.dsc</code> includes SEV/TDX/normal OVMF basic boot capability. The final binary can run on SEV/TDX/normal OVMF.</li> <li>No changes to existing OvmfPkgX64 image layout.</li> <li>No need to remove features if they exist today.</li> <li>PEI phase is NOT skipped in either TD or Non-TD.</li> <li>RTMR based measurement is supported.</li> <li>External inputs from Host VMM are measured, such as TdHob, CFV.</li> <li>Other external inputs are measured, such as FW_CFG data, os loader, initrd, etc.</li> </ul> <p><strong>Build the TDVF (Config-A) target:</strong></p> <p><a href="https://git.codelinaro.org/linaro/dcap/edk2/-/commit/4d37059d8e1eeda124270a158416795605327cbd">OvmfPkg: Support Tdx measurement in OvmfPkgX64</a></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">source </span>edksetup.sh
build.sh <span class="nt">-p</span> OvmfPkg/OvmfPkgX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC5
</code></pre></div></div> <p><strong>Config-B</strong>:</p> <ul> <li>Add a standalone <code class="language-plaintext highlighter-rouge">IntelTdx.dsc</code> to a TDX specific directory for a <em>full</em>  feature TDVF.(Align with existing SEV)</li> <li>Threat model: VMM is out of TCB. (We need necessary change to prevent attack from VMM)</li> <li><code class="language-plaintext highlighter-rouge">IntelTdx.dsc</code> includes TDX/normal OVMF basic boot capability. The final binary can run on TDX/normal OVMF.</li> <li>It might eventually merge with AmdSev.dsc, but NOT at this point of time. And we don’t know when it will happen. We need sync with AMD in the community after both of us think the solutions are mature to merge.</li> <li>Need to add necessary security feature as mandatory requirement, such as RTMR based Trusted Boot support.</li> <li>Need to measure the external input from Host VMM, such as TdHob, CFV.</li> <li>Need to measure other external input, such as FW_CFG data, os loader, initrd, etc.</li> <li>Need to remove unnecessary attack surfaces, such as network stack.</li> </ul> <p><strong>Build the TDVF (Config-B) target:</strong></p> <p><a href="https://git.codelinaro.org/linaro/dcap/edk2/-/commit/44a53a3bdd9c76e37f1750b5aa6a745de5d77391">OvmfPkg: Introduce IntelTdxX64 for TDVF Config-B</a></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">set </span><span class="nv">PACKAGES_PATH</span><span class="o">=</span>/path/to/edk2/OvmfPkg
<span class="nb">source </span>edksetup.sh
build.sh <span class="nt">-p</span> OvmfPkg/IntelTdx/IntelTdxX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC5
</code></pre></div></div> <p><span id="jumpB"></span></p> <h3 id="uefi-boot-phase">UEFI Boot Phase</h3> <p>UEFI has six main boot phases, which are all critical in the initialization process of the platform. The combined phases are referred to as the Platform Initialization or PI.</p> <p><em>1.</em> Security (SEC)</p> <p>This phase is the primary stage of the UEFI boot process, and will generally be used to: initialize a temporary memory store, act as the root of trust in the system and provide information to the Pre-EFI core phase. This root of trust is a mechanism that ensures any code that is executed in the PI is cryptographically validated (digitally signed), creating a “secure boot” environment.</p> <p><em>2.</em> Pre-EFI Initialization (PEI)</p> <p>This is the second stage of the boot process and involves using only the CPU’s current resources to dispatch Pre-EFI Initialization Modules (PEIMs). These are used to perform initialization of specific boot-critical operations such as memory initialization, whilst also allowing control to pass to the Driver Execution Environment (DXE).</p> <p><em>3.</em> Driver Execution Environment (DXE)</p> <p>The DXE phase is where the majority of the system initialization occurs. In the PEI stage, the memory required for the DXE to operate is allocated and initialized, and upon control being passed to the DXE, the DXE Dispatcher is then invoked. The dispatcher will perform the loading and execution of hardware drivers, runtime services, and any boot services required for the operating system to start.</p> <p><em>4.</em> Boot Device Selection (BDS)</p> <p>Upon completion of the DXE Dispatcher executing all DXE drivers, control is passed to the BDS. This stage is responsible for initializing console devices and any remaining devices that are required. The selected boot entry (OS loader) is then loaded and executed in preparation for the Transient System Load (TSL).</p> <p><em>5.</em> Transient System Load (TSL)</p> <p>In this phase, the PI process is now directly between the boot selection and the expected hand-off to the main operating system phase. Here, an application such as the UEFI shell may be invoked, or (more commonly) a boot loader will run in order to prepare the final OS environment. The boot loader is usually responsible for terminating the UEFI Boot Services via the ExitBootServices() call. However, it is also possible for the OS itself to do this, such as the Linux kernel with CONFIG_EFI_STUB.</p> <p><em>6.</em> Runtime (RT)</p> <p>The final phase is the runtime one. Here is where the final handoff to the OS occurs. The UEFI compatible OS now takes over the system. The UEFI runtime services remain available for the OS to use, such as for querying and writing variables from NVRAM.</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[How to build a trusted chain when launch TD guest using grub boot.]]></summary></entry><entry xml:lang="zh"><title type="html">Intel TDX: Measured Boot and Attestation in Grub Boot</title><link href="https://mahaocheng.me/blog/2025/tdx-measure-boot/zh/" rel="alternate" type="text/html" title="Intel TDX: Measured Boot and Attestation in Grub Boot"/><published>2025-01-12T00:00:00+00:00</published><updated>2025-01-12T00:00:00+00:00</updated><id>https://mahaocheng.me/blog/2025/tdx-measure-boot/tdx-measure-boot.zh</id><content type="html" xml:base="https://mahaocheng.me/blog/2025/tdx-measure-boot/zh/"><![CDATA[<p>Intel Trust Domain Extension（TDX）允许用户部署硬件隔离的虚拟机，称为 Trust Domain（TD）。TD 与虚拟机管理器（VMM）、hypervisor 以及宿主平台上其他非 TD 软件相互隔离。TD 的整个内存内容通过多密钥加密方式加密。Intel TDX 将宿主平台排除在 TD 的可信计算基（TCB）之外。</p> <p>Intel TDX 的工作负载主要是租户希望在云环境中以 TD 形式运行的 VM 镜像。要在 TD 中运行 VM，需要由 CSP 提供的特殊 Open Virtual Machine Firmware（OVMF），例如 TDX Virtual Firmware（TDVF）。TDVF 是一个基于 EDK2 的项目，为基于 TDX 的虚拟机提供 UEFI 支持，用于启动 TD。TDVF 允许两种不同特性的配置，详见 <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/IntelTdx/README.md">Configurations and Features</a> 中的 <a href="#jumpA">Appendix: TDVF Configurations</a>。</p> <p>TD 启动过程中，对 TCB 的哈希链式度量会扩展到若干安全寄存器中（即度量启动）。这些寄存器的值会组合为一份报告，最终由证明密钥（attestation key）签名，生成一份 quote。在向工作负载提供数据之前，依赖方通过远程证明来核实软件确实运行在真实 Intel TDX 系统的 TD 内，并达到指定的安全级别。</p> <p><em>注：此处 TCB 指的是系统中提供安全环境的所有硬件、固件和软件组件，包括 CPU、SEAM 固件等硬件信息，以及 OVMF、bootloader（shim/grub）、内核等 guest 组件。</em></p> <p><strong>以上是 Intel TDX 的简要描述。在接下来的系列文章中，我们将聚焦于 TD 的度量启动与远程证明。</strong></p> <h2 id="td-启动类型">TD 启动类型</h2> <p>下图展示了 TD 启动类型与启动流程。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_guest_boot_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. TD Guest Boot Process </div> <p><strong>Direct boot</strong> 是指系统不经过中间引导加载器直接进入 OS 的启动过程。也可以称为带固件的直接内核启动：固件通过 Qemu 提供的 FwCfg 设备加载 kernel 和 initrd。</p> <p><strong>Grub boot</strong> 使用 Grub 引导加载器，提供更丰富的启动菜单选项，可选择不同的操作系统并自定义启动配置。也可称为 firmware-only boot：固件从磁盘镜像中启动一个 bootloader。</p> <p>不同 TD 启动方式的详细流程见图 2。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/detailed_flow_for_different_td_boot.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. Detailed Flow for Different TD Boot </div> <p><strong>今天我们深入讨论 Grub Boot 下的度量启动。</strong></p> <p>在虚拟固件（OVMF）中，通过 <code class="language-plaintext highlighter-rouge">CoreLoadImageCommon()</code> 加载 EFI 镜像时会触发镜像处理函数 <code class="language-plaintext highlighter-rouge">DxeTpmMeasureBootHandler</code>。该处理函数将 FV、QEMU CFG、VMM Hob、Variable 等对象度量到度量寄存器（MR）中。随后在 bootloader ShimX64.efi 中，<code class="language-plaintext highlighter-rouge">TpmMeasureVariable()</code> 将 Secure Boot 证书度量到 MR 中；如果未启用 Secure Boot，VM 镜像中可能不包含 ShimX64.efi。最终，bootloader GrubX64.efi 将 kernel 二进制及其命令行、initrd 二进制以及 grub 的模块度量到 MR 中。</p> <p>上述过程借鉴了 TCG 可信启动链。不同之处在于：它不信任 hypervisor，并避免使用可变的非易失性存储（否则会导致 MR 变化）；而且其信任链可追溯至支持 TDX 的硬件。</p> <h2 id="uefi-启动时序">UEFI 启动时序</h2> <p>UEFI 允许通过加载 UEFI 驱动和 UEFI 应用镜像来扩展平台固件。UEFI 驱动与应用加载后可以访问所有 UEFI 定义的运行时服务和启动服务。启动时序见下图 3。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/uefi_booting_sequence.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 3. UEFI Booting Sequence </div> <p>UEFI 可将 OS loader 和平台固件的启动菜单整合为一个统一的平台固件菜单。这些菜单允许从任意启动介质的任意分区选择任意 UEFI OS loader，只要 UEFI 启动服务支持该介质。</p> <p>OS loader 作为 UEFI 应用运行，通过 Boot Services（BS）和 Runtime Services（RT）调用 UEFI 服务。随后调用 <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 终止 BS 并释放其资源，之后 OS 仅能使用 RT。</p> <p>UEFI 启动阶段的详细介绍见 <a href="https://secret.club/2020/05/26/introduction-to-uefi-part-1.html#uefi-boot-phases">UEFI boot phases</a> 中的 <a href="#jumpB">Appendix: UEFI Boot Phase</a>。</p> <h2 id="度量启动组件">度量启动组件</h2> <p>如图 4，内核之前的 pre-boot 环境包括 TDVF/OVMF 阶段以及 bootloader 阶段（shim 和 grub）。整条启动链通过 <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> 被度量进 Runtime Measurement Register（RTMR）。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/td_measurement_process.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 4. TD Measurement Process </div> <p>类似于 TCG event log，<code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code> 将事件记入 confidential computing event log（CCEL）ACPI 表，并将度量哈希扩展到对应的 RTMR 寄存器。CCEL 表中的事件日志可在 TD guest 内回放，以校验 RTMR 的值。</p> <p>EDK2 中的度量启动组件如下。</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" sizes="95vw"/> <img src="/assets/img/2025-01-12-tdx-measure-boot/measured_boot_component_in_edk2.png" class="img-fluid rounded z-depth-0 mx-auto d-block" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 5. Measured Boot Component in EDK2 </div> <p><a href="https://github.com/tianocore/edk2/tree/master/OvmfPkg/IntelTdx/TdxHelperLib"><code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code></a> 在 SEC 阶段提供度量函数。 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Protocol/CcMeasurement.h"><code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code></a> 协议抽象了 UEFI guest 环境中的机密计算（CC）度量操作。 <a href="https://github.com/tianocore/edk2/blob/master/OvmfPkg/Tcg/TdTcg2Dxe/TdTcg2Dxe.c">TdTcg2Dxe.c</a> DXE 驱动处理 DXE 阶段的度量。 <a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeTpm2MeasureBootLib"><code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootLib</code></a> 处理 PE 镜像度量和 GPT 度量。所有事件类型的定义见 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">UefiTcgPlatform.h</code></a>。</p> <p>下面根据 <a href="https://uefi.org/specs/UEFI/2.10/38_Confidential_Computing.html#">Confidential Computing in UEFI Specification</a> 简述 <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code>。</p> <p>如果具备 CC 能力的虚拟固件支持度量，则它应提供使用新 GUID <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL_GUID</code> 的 <code class="language-plaintext highlighter-rouge">EFI_CC_MEASUREMENT_PROTOCOL</code>，用以上报事件日志并提供哈希能力。</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define EFI_CC_MEASUREMENT_PROTOCOL_GUID  \
  { 0x96751a3d, 0x72f4, 0x41a6, { 0xa7, 0x94, 0xed, 0x5d, 0x0e, 0x67, 0xae, 0x6b }}
</span><span class="k">extern</span> <span class="n">EFI_GUID</span>  <span class="n">gEfiCcMeasurementProtocolGuid</span><span class="p">;</span>

<span class="cp"># Protocol Interface Structure
</span><span class="k">typedef</span> <span class="k">struct</span> <span class="n">_EFI_CC_MEASUREMENT_PROTOCOL</span> <span class="p">{</span>
  <span class="n">EFI_CC_GET_CAPABILITY</span>          <span class="n">GetCapability</span><span class="p">;</span>
  <span class="n">EFI_CC_GET_EVENT_LOG</span>           <span class="n">GetEventLog</span><span class="p">;</span>
  <span class="n">EFI_CC_HASH_LOG_EXTEND_EVENT</span>   <span class="n">HashLogExtendEvent</span><span class="p">;</span>
  <span class="n">EFI_CC_MAP_PCR_TO_MR_INDEX</span>     <span class="n">MapPcrToMrIndex</span><span class="p">;</span>
<span class="p">}</span> <span class="n">EFI_CC_MEASUREMENT_PROTOCOL</span><span class="p">;</span>
</code></pre></div></div> <p>该协议接口包含四个成员：</p> <ul> <li>GetCapability：提供协议能力信息与状态信息。</li> <li>GetEventLog：允许调用方获取指定事件日志的地址及其末尾条目。</li> <li>HashLogExtendEvent：允许调用方扩展并可选地记录事件，无需了解实际的 CC 命令。</li> <li>MapPcrToMrIndex：向调用方提供 TPM PCR 到 CC MR 的映射信息。</li> </ul> <p><strong>Intel TDX 的映射关系</strong></p> <p>下表展示了 TPM Platform Configuration Register（PCR）索引映射以及 Intel TDX 下 CC event log MR 索引的含义。MRTD 指 Trust Domain Measurement Register，RTMR 指 Runtime Measurement Register。TD MR 数量少于 TPM PCR，典型映射如下：</p> <table> <thead> <tr> <th>TPM PCR Index</th> <th>Typical Usage of Measurement Registers</th> <th>TDX MR Index</th> </tr> </thead> <tbody> <tr> <td>0</td> <td>FirmwareCode (BFV, including init page table)</td> <td>MRTD</td> </tr> <tr> <td>1</td> <td>FirmwareData (CFV, TD Hob, ACPI Table, Boot Variable)</td> <td>RTMR[0]</td> </tr> <tr> <td>2</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>3</td> <td>Option ROM code</td> <td>RTMR[1]</td> </tr> <tr> <td>4</td> <td>OS loader code</td> <td>RTMR[1]</td> </tr> <tr> <td>5</td> <td>GUID partition table (GPT)</td> <td>RTMR[1]</td> </tr> <tr> <td>6</td> <td>N/A</td> <td>N/A</td> </tr> <tr> <td>7</td> <td>Secure Boot Configuration</td> <td>RTMR[0]</td> </tr> <tr> <td>8~15</td> <td>TD OS measurement</td> <td>RTMR[2]</td> </tr> </tbody> </table> <p><em>注：RTMR[3] 保留用于特殊用途，例如 virtual TPM。如果这些专用场景不需要 RTMR[3]，用户可以灵活使用。</em></p> <p>MRTD 与 RTMR 的典型用途如下，更多细节见 <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">8.1 Measurement Register Usage in TD</a>：</p> <ul> <li>MRTD 用于 TDVF 代码（对应 PCR[0]）。</li> <li>RTMR[0] 用于 TDVF 配置（对应 PCR[1,7]），应遵循 TCG Platform Firmware Profile（PFP）规范。</li> <li>RTMR[1] 用于 TDVF 加载的组件，例如 OS loader（对应 PCR[4,5]），应遵循 TCG PFP 规范。</li> <li>RTMR[2] 用于 OS 组件，例如 OS kernel、initrd 与应用程序（对应 PCR[8~15]），使用方式由 OS 决定。</li> <li>RTMR[3] 仅保留给特殊用途。</li> </ul> <h2 id="度量启动流程">度量启动流程</h2> <p>总体而言，事件在启动时序图中的过渡点（虚线处）被度量。由此建立一条信任链：虚拟固件 → bootloader → OS → 应用。</p> <p><strong>度量 TdHob 与 Configuration FV（Cfv）</strong></p> <p><a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">4.2 TD Hand-Off Block (HOB)</a> 和 <a href="https://cdrdv2.intel.com/v1/dl/getContent/733585">3.2 Configuration Firmware Volume (CFV)</a> 是 Host VMM 提供的外部数据，TD guest 默认不信任。因此必须对其进行校验、度量并扩展到 TD 的 RTMR 寄存器。与此同时会创建两个 <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT_HOB</code>，这两个 GUID HOB 分别携带 TdHobList 和 Configuration FV 的哈希值。在 DXE 阶段，可基于这两个 GUID HOB 创建 <code class="language-plaintext highlighter-rouge">EFI_CC_EVENT</code>。</p> <p>Configuration Firmware Volume 包含所有预置数据，该区域只读。一种可能用途是存放 UEFI Secure Boot 变量内容，例如 PK、KEK、db、dbx。</p> <p>TD HOB list 用于从 VMM 向 TDVF 传递信息。TD HOB 必须包含 PHIT HOB 和 Resource Descriptor HOB，其他 HOB 可选。</p> <ul> <li>TD HOB 的第一个 HOB 必须是 PHIT HOB。EfiMemoryTop、EfiMemoryBottom、EfiFreeMemoryTop、EfiFreeMemoryBottom 都应为 0。</li> <li>TD HOB 必须至少包含一个 Resource Description HOB，用以声明物理内存资源。</li> </ul> <p><strong><em>0.</em></strong> <code class="language-plaintext highlighter-rouge">SecTdxHelperLib</code> 是 TdxHelperLib 的 SEC 实例。它在 SEC 阶段为 TDX 提供以下功能：</p> <ul> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureTdHob</code> 度量/扩展 TdHob，并把度量值存入 workarea。</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperMeasureCfvImage</code> 度量/扩展 Configuration FV 镜像，并把度量值存入 workarea。</li> <li><code class="language-plaintext highlighter-rouge">TdxHelperBuildGuidHobForTdxMeasurement</code> 为 TDX 度量构建 GuidHob。</li> </ul> <p><strong>度量 Secure Boot Policy 到 PCR[7]/RTMR[0]</strong></p> <p><em>1.</em> UEFI Debug Mode。</p> <p>如果平台提供固件调试模式，则必须以 <code class="language-plaintext highlighter-rouge">EV_EFI_ACTION</code> 度量字符串 “UEFI Debug Mode”。该逻辑在 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code> 中实现，依据 <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/SecurityPkg.dec"><code class="language-plaintext highlighter-rouge">PcdFirmwareDebuggerInitialized</code></a> 判定。</p> <p><em>2.</em> <code class="language-plaintext highlighter-rouge">SecureBoot</code> 变量的内容。</p> <p><em>3.</em> <code class="language-plaintext highlighter-rouge">PK</code> 变量的内容。</p> <p><em>4.</em> <code class="language-plaintext highlighter-rouge">KEK</code> 变量的内容。</p> <p><em>5.</em> <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> 变量（DB）的内容。</p> <p><em>6.</em> <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE1</code> 变量（DBX）的内容。</p> <p><em>7.</em> 如存在且非空，则度量 <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE2</code>（DBT）变量的内容。</p> <p>UEFI Secure Boot 相关变量——”SecureBoot”、”PK”、”KEK”、”db”、”dbx”——由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">ReadAndMeasureSecureVariable()</code> 无条件度量，事件类型为 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code></a>。若变量不存在，则度量一条零长度的 UEFI 变量条目。”dbt” 与 “dbr” 变量在存在时才被 <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code> 度量。</p> <p><em>8.</em> Separator。</p> <p>PCR7 的 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_SEPARATOR</code></a> 在 UEFI 变量就绪后由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">MeasureSecureBootPolicy()</code> 处理，位于 <code class="language-plaintext highlighter-rouge">MeasureAllSecureVariables()</code> 之后、<code class="language-plaintext highlighter-rouge">ReadyToBoot</code> 事件信号之前。原因是 PCR7 的 <code class="language-plaintext highlighter-rouge">EV_SEPARATOR</code> 必须位于 SecureBootPolicy（Configure）与 ImageVerification（Authority）之间。</p> <p><strong>度量 Boot Variable 到 PCR[1]/RTMR[0]</strong></p> <p><em>9.</em> UEFI BootOrder 变量和 Boot#### 变量（仅 device path）。</p> <p>UEFI 启动相关变量（例如 “BootOrder” 和 “Boot####”）由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">ReadAndMeasureBootVariable()</code> 度量，事件类型为 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_BOOT</code></a>。<code class="language-plaintext highlighter-rouge">MeasureAllBootVariables()</code> 在变量存在时将其度量。</p> <p><strong>选择启动设备后，</strong></p> <p><em>10.</em> 启动动作 “Calling EFI Application from Boot Option”，表示 Boot Manager 正在尝试从某个启动选项执行代码（<strong>PCR[4]/RTMR[1]</strong>）。</p> <p>该启动动作由 TdTcg2Dxe.c 的 <code class="language-plaintext highlighter-rouge">OnReadyToBoot()</code> 度量。调用启动选项之前，度量 “Calling EFI Application from Boot Option”；启动选项返回之后，度量 “Returning from EFI Application from Boot Option”。</p> <p><em>11.</em> Separator，用以分隔离开 pre-boot 环境与进入 post-boot 环境（<strong>PCR[0~6]/RTMR[1]</strong>）。</p> <p><em>12.</em> <strong>[可选] 若启用 UEFI Secure Boot</strong>，度量 <code class="language-plaintext highlighter-rouge">EFI_IMAGE_SECURITY_DATABASE</code> 中用于校验 UEFI 镜像的那一条条目（<strong>PCR[7]/RTMR[0]</strong>）。</p> <p>启用 UEFI Secure Boot 时，<a href="https://github.com/tianocore/edk2/tree/master/SecurityPkg/Library/DxeImageVerificationLib"><code class="language-plaintext highlighter-rouge">DxeImageVerificationLib</code></a> 依据镜像签名数据库中 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_LIST</code></a> 里的 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/Guid/ImageAuthentication.h"><code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code></a> 来校验 PE 镜像签名。若用某个 <code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code> 校验了镜像，则 <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeImageVerificationLib/Measurement.c">Measurement.c</a> 的 <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> 会以 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_AUTHORITY</code></a> 度量该 <code class="language-plaintext highlighter-rouge">EFI_SIGNATURE_DATA</code>。</p> <p><em>13.</em> GUID Partition Table（GPT）磁盘布局（<strong>PCR[5]/RTMR[1]</strong>）。</p> <p>当系统从磁盘中某个 GUID 命名分区启动时，需要度量 GPT 磁盘布局。由 <a href="https://github.com/tianocore/edk2/blob/master/SecurityPkg/Library/DxeTpm2MeasureBootLib/DxeTpm2MeasureBootLib.c">DxeTpm2MeasureBootLib.c</a> 的 <code class="language-plaintext highlighter-rouge">Tcg2MeasureGptTable()</code> 在 <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code> 中完成。</p> <p><em>14.</em> 选中的 UEFI 应用代码 PE/COFF 镜像，即 OS loader（<strong>PCR[4]/RTMR[1]</strong>）。</p> <p>第三方 UEFI 应用（例如 UEFI shell 工具、标准 OS loader 或 OEM 启动选项）由 DxeTpm2MeasureBootLib.c 的 <code class="language-plaintext highlighter-rouge">Tcg2MeasurePeImage()</code> 在 <code class="language-plaintext highlighter-rouge">DxeTpm2MeasureBootHandler()</code> 中度量，事件类型为 <a href="https://github.com/tianocore/edk2/blob/master/MdePkg/Include/IndustryStandard/UefiTcgPlatform.h"><code class="language-plaintext highlighter-rouge">EV_EFI_BOOT_SERVICES_APPLICATION</code></a>。若某 UEFI 应用是 DXE 阶段分派的 FV，则无论该 FV 本身是否被度量，都会度量到 PCR4。</p> <p><strong>随后 OS loader —— <a href="https://git.savannah.gnu.org/cgit/grub.git/">Grub2</a> —— 将可信启动链从虚拟固件延伸到 OS。</strong></p> <p><em>注：此处不讨论 <a href="https://github.com/rhboot/shim">Shim</a> 组件，因为我们只关注度量启动。Shim 用于将 UEFI Secure Boot 概念扩展到 Linux。</em></p> <p><em>15.</em> Grub2 度量配置文件（例如 grub.cfg）、grub 命令、kernel 二进制、kernel 命令行和 initrd 二进制（<strong>PCR[8, 9]/RTMR[2]</strong>）。PCR[8] 对应命令行字符串，PCR[9] 对应文件二进制，如下表所示。</p> <p>为支持机密计算平台上的度量，两个补丁已合入上游：</p> <ul> <li><a href="https://git.savannah.gnu.org/cgit/grub.git/commit/?id=4c76565b6cb885b7e144dc27f3612066844e2d19">efi/tpm: Add EFI_CC_MEASUREMENT_PROTOCOL support</a></li> <li><a href="https://git.savannah.gnu.org/cgit/grub.git/commit/?id=86df79275d065d87f4de5c97e456973e8b4a649c">commands/efi/tpm: Re-enable measurements on confidential computing platforms</a></li> </ul> <table> <thead> <tr> <th>PCR Index</th> <th>PCR Usage</th> </tr> </thead> <tbody> <tr> <td>8</td> <td>Grub command line: All executed commands (including those from configuration files) will be logged and measured as entered with a prefix of “grub cmd:”</td> </tr> <tr> <td> </td> <td>Kernel command line: Any command line passed to a kernel will be logged and measured as entered with a prefix of “kernel cmdline:”</td> </tr> <tr> <td> </td> <td>Module command line: Any command line passed to a kernel module will be logged and measured as entered with a prefix of “module cmdline:”</td> </tr> <tr> <td>9</td> <td>Files: Any file read by GRUB will be logged and measured with a descriptive text corresponding to the filename.</td> </tr> </tbody> </table> <p><a href="https://github.com/rhboot/grub2/blob/master/grub-core/commands/tpm.c">tpm.c</a> 将 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> 和 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> 注册到 grub_file_verifier 结构体。它们由 <a href="https://github.com/rhboot/grub2/blob/master/grub-core/commands/verifiers.c">verifiers.c</a> 中的 <code class="language-plaintext highlighter-rouge">grub_verify_string()</code> 和 <code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> 调用。</p> <p>当 grub2 执行诸如 <code class="language-plaintext highlighter-rouge">GRUB_VERIFY_MODULE_CMDLINE</code>、<code class="language-plaintext highlighter-rouge">GRUB_VERIFY_KERNEL_CMDLINE</code>、<code class="language-plaintext highlighter-rouge">GRUB_VERIFY_COMMAND</code> 等命令行，或 <a href="https://github.com/rhboot/grub2/blob/master/grub-core/lib/cmdline.c">cmdline.c</a> 中的 <code class="language-plaintext highlighter-rouge">grub_create_loader_cmdline()</code> 时，会调用 <code class="language-plaintext highlighter-rouge">grub_verify_string()</code>。最终 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_string()</code> 调用 <code class="language-plaintext highlighter-rouge">grub_tpm_measure</code>，然后由 <code class="language-plaintext highlighter-rouge">grub_cc_log_event</code> 将字符串度量到 <strong>PCR[8]/RTMR[2]</strong>。</p> <p><code class="language-plaintext highlighter-rouge">grub_verifiers_open()</code> 注册为 <a href="https://github.com/rhboot/grub2/blob/master/include/grub/file.h"><code class="language-plaintext highlighter-rouge">file.h</code></a> 中的 grub_file_filter 之一。每当 grub 调用 <a href="https://github.com/rhboot/grub2/blob/master/grub-core/kern/file.c">file.c</a> 的 <code class="language-plaintext highlighter-rouge">grub_file_open()</code> 时，都会触发该过滤器。最终 <code class="language-plaintext highlighter-rouge">grub_tpm_verify_write()</code> 调用 <code class="language-plaintext highlighter-rouge">grub_tpm_measure</code>，然后由 <code class="language-plaintext highlighter-rouge">grub_cc_log_event</code> 将文件二进制度量到 <strong>PCR[9]/RTMR[2]</strong>。</p> <p><em>16.</em> 启动动作 “Exit Boot Services Invocation”，表示 Boot Manager 已向 UEFI 发出结束 Boot Services 的调用（<strong>PCR[5]/RTMR[1]</strong>）。</p> <p><em>17.</em> 启动动作 “Exit Boot Services Returned with Success”，表示 UEFI 成功退出 Boot Services，pre-OS 环境已终止（<strong>PCR[5]/RTMR[1]</strong>）。</p> <p>ExitBootServices 动作由 TdTcg2Dxe.c 度量。成功时调用 <code class="language-plaintext highlighter-rouge">OnExitBootServices()</code>，失败时调用 <code class="language-plaintext highlighter-rouge">OnExitBootServicesFailed()</code>。</p> <p><strong>若 Security Boot Policy 在首次度量之后、<code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 完成之前发生变更，</strong></p> <p><em>18.</em> 平台必须重启，或把这些变量重新度量到 <strong>PCR[7]/RTMR[0]</strong>。另外，对任何 Secure Boot 变量的常规更新都应在 PCR[7] 的首次度量之前，或 <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 调用完成之后进行。</p> <p>UEFI Secure Boot 变量的更新在 Variable <a href="https://github.com/tianocore/edk2/tree/master/MdeModulePkg/Universal/Variable/RuntimeDxe"><code class="language-plaintext highlighter-rouge">RuntimeDxe</code></a> 中度量。如果上述任一 Secure Boot 相关变量被更新，<a href="https://github.com/tianocore/edk2/blob/master/MdeModulePkg/Universal/Variable/RuntimeDxe/Measurement.c">Measurement.c</a> 的 <code class="language-plaintext highlighter-rouge">MeasureVariable()</code> 会以 <code class="language-plaintext highlighter-rouge">EV_EFI_VARIABLE_DRIVER_CONFIG</code> 度量新的数据。</p> <h2 id="远程证明">远程证明</h2> <p>我们从验证方视角分析如何校验 TD Quote，不涉及 Quote 生成或与证明方的交互。总体可分为：Quote 校验、事件日志回放、事件解析。</p> <h3 id="quote-校验">Quote 校验</h3> <p>度量启动根信任（RoT）可以发布一份 MR 报告，通常称为 quote 或 attestation report。该 quote 通常是对 MR 摘要的数字签名。</p> <p>验证方随后确认 MR 摘要是由可信密钥——RoT for Reporting（RTR）——签发。此 RTR 即证明密钥（attestation key），通常是一把经过认证的密钥，用于对 MR 报告签名。这种认证在 IETF RATS 架构中称为 Endorsement。更多 quote 校验的细节见 <a href="https://github.com/intel/SGXDataCenterAttestationPrimitives">Intel SGX-based Data Center Attestation Primitives (DCAP)</a>。</p> <h3 id="事件日志回放">事件日志回放</h3> <p>事件日志回放是把原始事件日志反序列化后，利用其中的事件重新计算所有度量寄存器的值。每个事件包含一个摘要和度量寄存器索引。验证方创建一组模拟度量寄存器，对每个事件将其摘要扩展到对应的模拟寄存器中。最后将模拟寄存器的值与第一步中 quote 上报的真实度量寄存器值比对。</p> <p>Intel 提供了一个 Python 库 <a href="https://github.com/canonical/tdx/tree/main/tests/lib/tdx-tools/src/tdxtools">tdx-tools</a> 用于校验 TD 度量。其流程分为三步，完整步骤可参考 tdx-tools 中定义的 <code class="language-plaintext highlighter-rouge">VerifyActor</code> 类。</p> <ul> <li>从 CCEL ACPI 表获取 TD event log。</li> <li>通过 Linux attestation driver 获取 TDREPORT。</li> <li>对比 TDREPORT 中的 RTMR 值与事件日志回放得到的 RTMR 值。</li> </ul> <p>两者应当完全一致，表示被度量的内容未被篡改。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">VerifyActor</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Actor to verify the RTMR
    </span><span class="sh">"""</span>

    <span class="k">def</span> <span class="nf">_verify_single_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">rtmr_value_1</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">,</span>
        <span class="n">rtmr_value_2</span><span class="p">:</span> <span class="n">RTMR</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>

        <span class="k">if</span> <span class="n">rtmr_value_1</span> <span class="o">==</span> <span class="n">rtmr_value_2</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] passed the verification.</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">LOG</span><span class="p">.</span><span class="nf">error</span><span class="p">(</span><span class="sh">"</span><span class="s">RTMR[%d] did not pass the verification</span><span class="sh">"</span><span class="p">,</span> <span class="n">rtmr_index</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">verify_rtmr</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        Get TD report and RTMR replayed by event log to do verification.
        </span><span class="sh">"""</span>
        <span class="c1"># 1. Read CCEL from ACPI table at /sys/firmware/acpi/tables/CCEL
</span>        <span class="n">ccelobj</span> <span class="o">=</span> <span class="n">CCEL</span><span class="p">.</span><span class="nf">create_from_acpi_file</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">ccelobj</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">return</span>

        <span class="c1"># 2. Get the start address and length for event log area
</span>        <span class="n">td_event_log_actor</span> <span class="o">=</span> <span class="nc">TDEventLogActor</span><span class="p">(</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_start_address</span><span class="p">,</span>
            <span class="n">ccelobj</span><span class="p">.</span><span class="n">log_area_minimum_length</span><span class="p">)</span>

        <span class="c1"># 3. Collect event log and replay the RTMR value according to event log
</span>        <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>

        <span class="c1"># 4. Read TD REPORT via TDCALL.GET_TDREPORT
</span>        <span class="n">td_report</span> <span class="o">=</span> <span class="n">TdReport</span><span class="p">.</span><span class="nf">get_td_report</span><span class="p">()</span>

        <span class="c1"># 5. Verify individual RTMR value from TDREPORT and recalculated from
</span>        <span class="c1">#    event log
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">0</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_0</span><span class="p">)))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">1</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_1</span><span class="p">)))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">2</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">2</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_2</span><span class="p">)))</span>

        <span class="n">self</span><span class="p">.</span><span class="nf">_verify_single_rtmr</span><span class="p">(</span>
            <span class="mi">3</span><span class="p">,</span>
            <span class="n">td_event_log_actor</span><span class="p">.</span><span class="nf">get_rtmr_by_index</span><span class="p">(</span><span class="mi">3</span><span class="p">),</span>
            <span class="nc">RTMR</span><span class="p">(</span><span class="nf">bytearray</span><span class="p">(</span><span class="n">td_report</span><span class="p">.</span><span class="n">td_info</span><span class="p">.</span><span class="n">rtmr_3</span><span class="p">)))</span>
</code></pre></div></div> <h3 id="事件解析">事件解析</h3> <p>事件解析是从事件中提取信息的过程。验证方据此对照评估策略（appraisal policy）、endorsements 和 reference values 做出判断。</p> <p>在 <a href="https://github.com/google/go-eventlog">go-eventlog</a> 中，<code class="language-plaintext highlighter-rouge">ReplayAndExtract</code> 解析一条 CC event log，并依据哈希算法在指定的 RTMR bank 上回放事件日志（即第二步）。随后从已校验的日志中抽取事件信息写入 FirmwareLogState。该 FirmwareLogState 包含固件、Secure Boot 配置、bootloader（例如 grub）以及 kernel（含命令行和 initramfs）的详细信息。</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// FirmwareLogState extracts event info from a verified TCG PC Client event</span>
<span class="c">// log into a FirmwareLogState.</span>
<span class="c">//</span>
<span class="c">// It is the caller's responsibility to ensure that the passed events have</span>
<span class="c">// been replayed (e.g., using `tcg.ParseAndReplay`) against a verified measurement</span>
<span class="c">// register bank.</span>
<span class="k">func</span> <span class="n">FirmwareLogState</span><span class="p">(</span><span class="n">events</span> <span class="p">[]</span><span class="n">tcg</span><span class="o">.</span><span class="n">Event</span><span class="p">,</span> <span class="n">hash</span> <span class="n">crypto</span><span class="o">.</span><span class="n">Hash</span><span class="p">,</span> <span class="n">registerCfg</span> <span class="n">registerConfig</span><span class="p">,</span> <span class="n">opts</span> <span class="n">Opts</span><span class="p">)</span> <span class="p">(</span><span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">var</span> <span class="n">joined</span> <span class="kt">error</span>
	<span class="n">tcgHash</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">tpm2</span><span class="o">.</span><span class="n">HashToAlgorithm</span><span class="p">(</span><span class="n">hash</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
	<span class="p">}</span>

	<span class="n">platform</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">PlatformExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">sbState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">SecureBootState</span><span class="p">(</span><span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">efiState</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">EfiState</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">,</span> <span class="n">registerCfg</span><span class="p">)</span>

	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>

	<span class="k">var</span> <span class="n">grub</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">GrubState</span>
	<span class="k">var</span> <span class="n">kernel</span> <span class="o">*</span><span class="n">pb</span><span class="o">.</span><span class="n">LinuxKernelState</span>
	<span class="k">if</span> <span class="n">opts</span><span class="o">.</span><span class="n">Loader</span> <span class="o">==</span> <span class="n">GRUB</span> <span class="p">{</span>
		<span class="n">grub</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">registerCfg</span><span class="o">.</span><span class="n">GRUBExtracter</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">)</span>

		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
		<span class="n">kernel</span><span class="p">,</span> <span class="n">err</span> <span class="o">=</span> <span class="n">LinuxKernelStateFromGRUB</span><span class="p">(</span><span class="n">grub</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="n">joined</span> <span class="o">=</span> <span class="n">errors</span><span class="o">.</span><span class="n">Join</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
		<span class="p">}</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="o">&amp;</span><span class="n">pb</span><span class="o">.</span><span class="n">FirmwareLogState</span><span class="p">{</span>
		<span class="n">Platform</span><span class="o">:</span>    <span class="n">platform</span><span class="p">,</span>
		<span class="n">SecureBoot</span><span class="o">:</span>  <span class="n">sbState</span><span class="p">,</span>
		<span class="n">Efi</span><span class="o">:</span>         <span class="n">efiState</span><span class="p">,</span>
		<span class="n">RawEvents</span><span class="o">:</span>   <span class="n">tcg</span><span class="o">.</span><span class="n">ConvertToPbEvents</span><span class="p">(</span><span class="n">hash</span><span class="p">,</span> <span class="n">events</span><span class="p">),</span>
		<span class="n">Hash</span><span class="o">:</span>        <span class="n">pb</span><span class="o">.</span><span class="n">HashAlgo</span><span class="p">(</span><span class="n">tcgHash</span><span class="p">),</span>
		<span class="n">Grub</span><span class="o">:</span>        <span class="n">grub</span><span class="p">,</span>
		<span class="n">LinuxKernel</span><span class="o">:</span> <span class="n">kernel</span><span class="p">,</span>
	<span class="p">},</span> <span class="n">joined</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="参考度量值">参考度量值</h3> <p>最后还有一个相当棘手的问题：如何为 FirmwareLogState 生成参考度量值。目前 tdx 仓库中该问题仍未关闭，见 <a href="https://github.com/canonical/tdx/issues/263">Calculate measurements outside of TDX #263</a>。受 cc-api 仓库中 <a href="https://github.com/cc-api/cvm-image-rewriter">cvm-image-rewriter</a> 工具的启发，我们可以设计一个插件来计算 VM 镜像的参考度量值。</p> <p>cvm-image-rewriter 基于插件架构，用于定制机密计算 VM guest，包括 guest 镜像、配置、OVMF 固件等。现有插件如下表所示。</p> <table> <thead> <tr> <th>Name</th> <th>Descriptions</th> </tr> </thead> <tbody> <tr> <td>01-resize-image</td> <td>Resize the input qcow2 image</td> </tr> <tr> <td>02-motd-welcome</td> <td>Customize the login welcome message</td> </tr> <tr> <td>03-netplan</td> <td>Customize the netplan.yaml</td> </tr> <tr> <td>04-user-authkey</td> <td>Add auth key for user login instead of password</td> </tr> <tr> <td>05-readonly-data</td> <td>Fix some file permission to ready-only</td> </tr> <tr> <td>06-install-tdx-guest-kernel</td> <td>Install MVP TDX guest kernel</td> </tr> <tr> <td>07-device-permission</td> <td>Fix the permission for device node</td> </tr> <tr> <td>08-ccnp-uds-directory-permission</td> <td>Fix the permission for CCNP UDS directory</td> </tr> <tr> <td>60-initrd-update</td> <td>Update the initrd image</td> </tr> <tr> <td>97-sample</td> <td>plugin customization example</td> </tr> <tr> <td>98-ima-enable-simple</td> <td>Enable IMA (Integrity Measurement Architecture) feature</td> </tr> </tbody> </table> <p>定制完 VM 镜像后，一个插件（可命名为 reference-measurement-calculator）可以读取 grub 和 kernel，计算镜像与配置的摘要。由此得到 VM 镜像的参考度量值，用于最终校验。</p> <p>接下来是如何获取固件的参考度量值。根据 TDVF Binary Layout，可以解析虚拟固件，定位 BFV 和 CFV，然后计算它们的参考度量值。这里推荐参考 contrast 仓库中的工具 <a href="https://github.com/edgelesssys/contrast/blob/main/tools/tdx-measure/tdvf/tdvf.go">tdx-measure</a>。</p> <p>该工具还尝试了另一种证明方式：对给定的固件和 kernel 文件进行 RTMR 预计算（见 <code class="language-plaintext highlighter-rouge">newRtMrCmd()</code> 函数），再将 TDREPORT 中的 RTMR 与预计算 RTMR 比对。这种方式省去了事件日志，从而提升校验效率。需要指出，这种方式仅适用于相对固定的场景。在大多数场景中，事件日志仍不可或缺，因为固件、bootloader、OS 和应用都可以按任意顺序追加任意事件的度量。</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">newRtMrCmd</span><span class="p">()</span> <span class="o">*</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span> <span class="p">{</span>
	<span class="n">cmd</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">cobra</span><span class="o">.</span><span class="n">Command</span><span class="p">{</span>
		<span class="n">Use</span><span class="o">:</span>   <span class="s">"rtmr -f OVMF.fd -k bzImage [0|1|2|3]"</span><span class="p">,</span>
		<span class="n">Short</span><span class="o">:</span> <span class="s">"calculate the RTMR for a firmware and kernel file"</span><span class="p">,</span>
		<span class="n">Long</span><span class="o">:</span> <span class="s">`Calculate the RTMR for a firmware and kernel file.

		This will parse the firmware according to the TDX Virtual Firmware Design Guide
		and/or hash the kernel and pre-calculate a given RTMR.`</span><span class="p">,</span>
		<span class="n">Args</span><span class="o">:</span>      <span class="n">cobra</span><span class="o">.</span><span class="n">MatchAll</span><span class="p">(</span><span class="n">cobra</span><span class="o">.</span><span class="n">ExactArgs</span><span class="p">(</span><span class="m">1</span><span class="p">),</span> <span class="n">cobra</span><span class="o">.</span><span class="n">OnlyValidArgs</span><span class="p">),</span>
		<span class="n">ValidArgs</span><span class="o">:</span> <span class="p">[]</span><span class="kt">string</span><span class="p">{</span><span class="s">"0"</span><span class="p">,</span> <span class="s">"1"</span><span class="p">,</span> <span class="s">"2"</span><span class="p">,</span> <span class="s">"3"</span><span class="p">},</span>
		<span class="n">RunE</span><span class="o">:</span>      <span class="n">runRtMr</span><span class="p">,</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"f"</span><span class="p">,</span> <span class="s">"OVMF.fd"</span><span class="p">,</span> <span class="s">"path to firmware file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"firmware"</span><span class="p">,</span> <span class="s">"fd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">,</span> <span class="s">"k"</span><span class="p">,</span> <span class="s">"bzImage"</span><span class="p">,</span> <span class="s">"path to kernel file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"kernel"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">,</span> <span class="s">"i"</span><span class="p">,</span> <span class="s">"initrd.zst"</span><span class="p">,</span> <span class="s">"path to initrd file"</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">cmd</span><span class="o">.</span><span class="n">MarkFlagFilename</span><span class="p">(</span><span class="s">"initrd"</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="n">cmd</span><span class="o">.</span><span class="n">Flags</span><span class="p">()</span><span class="o">.</span><span class="n">StringP</span><span class="p">(</span><span class="s">"cmdline"</span><span class="p">,</span> <span class="s">"c"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"kernel command line"</span><span class="p">)</span>
	<span class="k">return</span> <span class="n">cmd</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="附录">附录</h2> <p><span id="jumpA"></span></p> <h3 id="tdvf-configurations">TDVF Configurations</h3> <p><strong>Config-A</strong>：</p> <ul> <li>将<em>基础</em> TDVF 特性合入现有的 <code class="language-plaintext highlighter-rouge">OvmfX64Pkg.dsc</code>。（与既有 SEV 对齐）</li> <li>威胁模型：VMM 不排除在 TCB 之外。（不改变现状）</li> <li><code class="language-plaintext highlighter-rouge">OvmfX64Pkg.dsc</code> 包含 SEV/TDX/normal OVMF 的基本启动能力，最终二进制可在 SEV/TDX/normal OVMF 上运行。</li> <li>不修改 OvmfPkgX64 现有镜像布局。</li> <li>无需移除现有特性。</li> <li>TD 与 Non-TD 下均不跳过 PEI 阶段。</li> <li>支持基于 RTMR 的度量。</li> <li>度量来自 Host VMM 的外部输入，例如 TdHob、CFV。</li> <li>度量其他外部输入，例如 FW_CFG 数据、os loader、initrd 等。</li> </ul> <p><strong>构建 TDVF（Config-A）目标：</strong></p> <p><a href="https://git.codelinaro.org/linaro/dcap/edk2/-/commit/4d37059d8e1eeda124270a158416795605327cbd">OvmfPkg: Support Tdx measurement in OvmfPkgX64</a></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">source </span>edksetup.sh
build.sh <span class="nt">-p</span> OvmfPkg/OvmfPkgX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC5
</code></pre></div></div> <p><strong>Config-B</strong>：</p> <ul> <li>在 TDX 专用目录中新增独立的 <code class="language-plaintext highlighter-rouge">IntelTdx.dsc</code>，提供<em>完整</em>特性的 TDVF。（与既有 SEV 对齐）</li> <li>威胁模型：VMM 排除在 TCB 之外。（需要必要改动以防御来自 VMM 的攻击）</li> <li><code class="language-plaintext highlighter-rouge">IntelTdx.dsc</code> 包含 TDX/normal OVMF 的基本启动能力，最终二进制可在 TDX/normal OVMF 上运行。</li> <li>将来可能与 AmdSev.dsc 合并，但目前不合并；具体时间未知。只有 Intel 与 AMD 双方都认为各自方案足够成熟时，才会在社区中协调合并。</li> <li>必须新增必要的安全特性作为强制要求，例如基于 RTMR 的可信启动支持。</li> <li>必须度量来自 Host VMM 的外部输入，例如 TdHob、CFV。</li> <li>必须度量其他外部输入，例如 FW_CFG 数据、os loader、initrd 等。</li> <li>必须移除不必要的攻击面，例如网络协议栈。</li> </ul> <p><strong>构建 TDVF（Config-B）目标：</strong></p> <p><a href="https://git.codelinaro.org/linaro/dcap/edk2/-/commit/44a53a3bdd9c76e37f1750b5aa6a745de5d77391">OvmfPkg: Introduce IntelTdxX64 for TDVF Config-B</a></p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/edk2
<span class="nb">set </span><span class="nv">PACKAGES_PATH</span><span class="o">=</span>/path/to/edk2/OvmfPkg
<span class="nb">source </span>edksetup.sh
build.sh <span class="nt">-p</span> OvmfPkg/IntelTdx/IntelTdxX64.dsc <span class="nt">-a</span> X64 <span class="nt">-t</span> GCC5
</code></pre></div></div> <p><span id="jumpB"></span></p> <h3 id="uefi-boot-phase">UEFI Boot Phase</h3> <p>UEFI 有六个主要启动阶段，每一阶段都是平台初始化过程中的关键环节。这些阶段合称 Platform Initialization（PI）。</p> <p><em>1.</em> Security（SEC）</p> <p>UEFI 启动的第一个阶段，通常用于：初始化临时内存、充当系统信任根，并向 Pre-EFI 核心阶段提供信息。该信任根确保 PI 中执行的任何代码都经过密码学校验（数字签名），从而建立 “secure boot” 环境。</p> <p><em>2.</em> Pre-EFI Initialization（PEI）</p> <p>启动过程的第二阶段，仅使用 CPU 当前可用的资源调度 Pre-EFI Initialization Modules（PEIM）。PEIM 用于完成启动关键操作的初始化（例如内存初始化），并将控制权交给 Driver Execution Environment（DXE）。</p> <p><em>3.</em> Driver Execution Environment（DXE）</p> <p>DXE 阶段完成大部分系统初始化。DXE 运行所需的内存在 PEI 阶段分配并初始化；控制权交给 DXE 后，DXE Dispatcher 被触发，负责加载并执行硬件驱动、运行时服务以及 OS 启动所需的各类启动服务。</p> <p><em>4.</em> Boot Device Selection（BDS）</p> <p>DXE Dispatcher 执行完所有 DXE 驱动后，控制权交给 BDS。BDS 负责初始化控制台设备及其他尚未初始化的设备；随后加载并执行所选启动项（OS loader），为 Transient System Load（TSL）做准备。</p> <p><em>5.</em> Transient System Load（TSL）</p> <p>此阶段中，PI 流程位于启动项选择与交付给主操作系统之间。可以调用如 UEFI shell 之类的应用，或（更常见地）运行 bootloader 以准备最终 OS 环境。通常由 bootloader 通过 <code class="language-plaintext highlighter-rouge">ExitBootServices()</code> 终止 UEFI Boot Services；但 OS 本身也可以做此动作，例如启用 <code class="language-plaintext highlighter-rouge">CONFIG_EFI_STUB</code> 的 Linux kernel。</p> <p><em>6.</em> Runtime（RT）</p> <p>最后一个阶段，控制权正式交给 OS。兼容 UEFI 的 OS 接管系统。UEFI Runtime Services 仍对 OS 可用，例如用于从 NVRAM 查询和写入变量。</p>]]></content><author><name></name></author><category term="security"/><summary type="html"><![CDATA[如何在使用 grub 启动 TD guest 时构建可信链。]]></summary></entry></feed>